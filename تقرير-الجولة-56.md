# تقرير الجولة 56 — ست طلبات من الملاك

**النطاق:** ملاحظات المالك عبر الجلسة المشتركة، بعد r55.

---

## 1) شاشة الدخول: إنشاء الحساب أولاً للمرة الأولى

**المشكلة:** «شاشة الدخول يجب أن تعرض خيار إنشاء حساب في المرة الأولى، بدل خيار
واحد فقط: الدخول ثم إنشاء الحساب… وجدت الخيار المُفعّل تلقائياً هو الدخول، ونص
الرقم التسلسلي في الأسفل يُحذف.»

**ما نُفّذ:**
- الزيارة الأولى على جهاز جديد (لا بريد محفوظ) تفتح على **«حساب جديد»** كخيار
  مُحدّد أساساً؛ الأجهزة العائدة (زارت قبلها بحساب) تفتح على «تسجيل الدخول».
- الزرّان صارا كبيرين بأيقونات: «حساب جديد» أولاً (الأول في ترتيب القراءة) ثم
  «تسجيل الدخول»، والمُحدَّد منهما يحمل الهوية الأساسية بوضوح.
- **تبديل ذكي بلمسة واحدة:** فشل الدخول ببيانات غير موجودة يظهر تلميحاً
  «أنشئ حسابك بنفس الاسم والبريد مباشرة»، ورسالة «البريد مسجّل مسبقاً» عند
  إنشاء الحساب تظهر «سجّل الدخول به» — في الاتجاهين يُحتفظ بالاسم والبريد.
- **نص الرقم التسلسلي حُذف نهائياً** من أسفل النموذج (كان يقول «سيُمنح لك رقمك
  التسلسلي تلقائياً») وحلّ محله التلميش القابل للتنفيذ أعلاه.

**التحقق:** متصفح حقيقي — أول زيارة: «حساب جديد» مُحدّد؛ بعد خروج: «دخول»
مُحدّد؛ مسارا التلميح كلاهما يعملان ويحفظان المدخلات. لقطات
`download/r56/login-*.png`.

---

## 2) الإعدادات: إزالة التكرار وإضافة الجديد

**المشكلة:** «الإعدادات فقط تكرارات لحسابي ولا جديد فيها.»

**أزيل (تكرار صريح):**
- شبكة الموقع الأكاديمي + أزرار الاختصارات الثلاثة (فتح حسابي/الإعلانات/فوجي)
  من بطاقة الحساب — كلها تعيش في شاشاتها.
- بطاقة «المساعدة والدعم» كلها (زرّاها كانا مجرد تنقّل إلى حسابي وتصفح الأفواج).

**أضيف (غير موجود في أي شاشة أخرى):**
| الإعداد | الوصف |
|---|---|
| **حجم الواجهة والخط** | عادي / كبير / أكبر — يقيس جذر الصفحة (16/18/20px) فيتكبير كل شيء معاً؛ محفوظ على الجهاز ويُطبّق عند كل إقلاع |
| **نمط العرض: تلقائي** | فاتح / داكن / **تلقائي (يتبع نظام جهازك)** — فُعّل `enableSystem` فعلياً في مزوّد الثيم |
| **الإشعارات خارج المتصفح** | صف حالة + زر تفعيل (انظر §5) |
| **بيانات جهازك** | مسح محادثات المساعد الذوي + ذاكرة الواجبات المنجزة من هذا الجهاز، مع تأكيد |

**التحقق:** الشاشة الجديدة مصوّرة؛ حجم الخط تغيّر فعلياً (16↔18px) وبقي بعد
إعادة التحميل؛ «تلقائي» يحلّ إلى light/dark حسب النظام.

---

## 3) المساعد الذكي: تصميم يوحي بوجوده

**المشكلة:** «تصميم قسم المساعد الذكي لا يوحي بوجوده — لم يعجبني.»

**ما نُفّذ:**
- **بطاقة رئيسية في الشاشة الرئيسية** (أول ما يظهر بعد لوحة الطالب): تدرج
  البنفسجي المميز، أيقونة الشرارات، زر «اسأل الآن»، وثلاث شرائح سريعة
  (لخّص لي درساً / اشرح ببساطة / اختبرني).
- **رابط عميق حقيقي:** لمسة الشريحة تفتح المحادثة مباشرة داخل أدواتي **مع ملء
  المحرّر بالطلب جاهزاً** — من الشاشة الرئيسية إلى محادثة جاهزة للإرسال بلمسة
  واحدة. (آلية: `talib-open-ai` + حدث نافذة يغطي حالتي التنقّل والبقاء).
- **بطاقة أدواتي أُعيد تصميمها:** معاينة محادثة مصغّرة (فقاعتان حقيقيتان: سؤال
  الطالب وجواب المساعد) + زر «ابدأ محادثة» — لا يمكن الخلط بينها وبين صف أدوات
  عادي.

**التحقق:** لقطة الشريحة من الرئيسية فتحت المحادثة والمحرّر مملوء بـ«اشرح
بالبساطة وبمثال ما يلي:»؛ البطاقة سليمة على 390px و1280px وفي الوضعين
(مراجعة VLM بلا ملاحظات). `download/r56/home-ai-*.png` و `ai-chat-prefilled.png`.

---

## 4) إشعار حلّ التبليغ

**المشكلة:** «عندما أبلّغ عن مشكلة أكتب تبليغاً ولا يصلني إشعار عند حلّه.»

**ما نُفّذ:**
- عمود `reporter_id` جديد في `student_issue_reports` (Prisma +
  `download/supabase_report_reporter.sql` — آمن للتكرار؛ الصفوف القديمة تسقط
  إلى مطابقة الاسم الكامل).
- `PATCH /api/issues` يقرأ الحالة **قبل** التحديث ويستدعي `notifyReportResolved`
  عند التغيّر الفعلي فقط: «تم حل تبليغك» / «أُعيد تبليغك للمراجعة»، بنص
  يشكر الطالب ويذكر عنوان تبليغه.
- النوع `report_resolved` **غير قابل للكتم أبداً** (نتيجة فعل خاص بالمستخدم،
  مثل نتائج طلبات الانضمام)، بأيقونة ✓✓ زمردية، والنقر عليه يقود إلى الرئيسية.

**التحقق (طبقة API كاملة):** طالبة أرسلت تبليغاً → المالك حلّه → وصل للطالبة
«تم حل تبليغك — انتهت مراجعة تبليغك ‹ملف محاضرة معطوب…›» في لوحة الإشعارات،
والنقر علّمه مقروءاً وتنقّل للرئيسية. `download/r56/report-resolved-*.png`.

---

## 5) + 6) الإشعارات خارج المتصفح

**المشكلة:** «أريد إشعارات خارج المتصفح — إما مفعّلة تلقائياً أو تتطلب إذناً في
الجلسة الرابعة.»

**ما نُفّذ (Web Push كامل البنية):**
- **Service Worker** (`public/sw.js`): يستقبل push من الخادم حتى **والتطبيق
  غير مفتوح**، ويعرض تنبيه نظام عربياً RTL، والنقر يركّز تبويب التطبيق أو
  يفتحه.
- **مُرسِل VAPID** (`src/lib/push.ts`): كل إشعار يُنشأ في الخادم (نتائج
  الطلبات، التبليغات، المحتوى الجديد، التذكيرات…) يخرج **أيضاً** كـ Web Push؛
  الاشتراكات الميتة (404/410) تُقصّ تلقائياً.
- **التخزين**: جدول `push_subscriptions` (Prisma +
  `download/supabase_push_subscriptions.sql`)، تحديث/حذف عبر
  `/api/push/subscribe`.
- **تفعيل تلقائي (أ)**: إن كان إذن المتصفح ممنوحاً مسبقاً → تسجيل الـ SW
  والاشتراك **بصمت عند الدخول** — بلا أي نافذة.
- **الجلسة الرابعة (ب)**: تُعدّ الجلسات على الجهاز؛ من **الجلسة الرابعة** (لا
  قبلها — الثلاث الأولى للتهيئة والجولة الإلزامية) تظهر بطاقة مصمّمة
  «لا تفوّت نتيجة تبليغك أو إعلاناً مهماً» بزرّي «تفعيل الإشعارات»/«ليس الآن»
  (الرفض يُحفظ للأبد؛ الباب يبقى مفتوحاً من الإعدادات).
- **الإعدادات**: صف «الإشعارات خارج المتصفح» يعرض الحالة ويتيح التفعيل اليدوي.

**التحقق:** الأنبوب الخادمي كاملاً على خادم حقيقي: اشتراك مزيف حُفظ → حلُّ
تبليغ أرسل Web Push فعلياً (توقيع VAPID بالمفتاح الخاص المحلي) → الخادم ردّ
404 → الاشتراك قُصّ. بطاقة الجلسة الرابعة ظهرت عند العدّاد=4 وأُغلقت بالزرّين.
**قيّد صادق:** متصفح بلا واجهة يرفض إذن الإشعارات تلقائياً، فتعذّر تصوير تنبيه
النظام نفسه — المسار مغطى كودياً وبطبقة الخادم.

---

## خطوات المالك (مرة واحدة، مجانية)

1. **`VAPID_PRIVATE_KEY` في Vercel** — المفتاح الخاص سُلّم مع هذا التقرير/الجلسة.
   (المفتاح العام مضمّن في الشيفرة — تعريفياً علني.)
2. **نفّذ سكربتَي SQL** في محرر Supabase (آمنان للتكرار):
   `download/supabase_push_subscriptions.sql` و
   `download/supabase_report_reporter.sql`.
3. **Redeploy.**

بدون الخطوات أعلاه: التطبيق يعمل كاملاً كما قبل (الإشعارات داخل التطبيق
وخلفية التبويب تعملان)، وتوجيه حلّ التبليغ يسقط لمطابقة الاسم.

---

## جودة البناء

- `tsc --noEmit`: **0 أخطاء**.
- `eslint .`: **0 أخطاء** (أضيفت `public/**` إلى التجاهل — ملف pdf.worker
  المُصدَّر كان يلوّث العدّ منذ r31؛ 4 تحذيرات قديمة بلا تغيير).
- `next build`: **أخضر 69/69** (مسار جديد: `/api/push/subscribe`).
- بيانات الاختبار نُظّفت (حساب عابر + تبليغ تجريبي + اشتراكات)؛ حسابا r52
  باقيان للجولات القادمة.
- لقطات: `download/r56/` (13 لقطة: دخول أول مرة/عائد/تبديل/تلميح، الرئيسية
  فاتح/داكن/موبايل/سطح مكتب، المحادثة المملوءة، الإعدادات الجديدة وحجم الخط،
  بطاقة الجلسة الرابعة، حلّ التبليغ في اللوحة، حوار المسح).
