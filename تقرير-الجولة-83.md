# تقرير الجولة 83 — استوديو الصور: توليد الصور بالذكاء الاصطناعي داخل «أدواتي»

**التاريخ:** 16 سبتمبر 2026 · **الحالة:** منجزة ومختبرة محلياً (28/28) ودُفعت إلى `main`

## ما طُلب

طلب المالك صراحةً إضافة **توليد الصور** إلى التطبيق ضمن تطوير قدرات الذكاء الاصطناعي
(«توليد الصور وغيره من العملاء الآليين يبدو مثيراً إن أضفناه إلى التطبيق»)، مع قاعدة
حدودية ثابتة: **الذكاء الاصطناعي لا يقترب أبداً من كلمات المرور ولا البيانات الحساسة**.

## ما أُنجز

### 1. طبقة مزوّدي الصور — `src/lib/ai/image.ts` (جديد)

سلسلة مزوّدين حقيقية على نمط `lib/ai/providers.ts` الخاصة بالمحادثة:

- **Gemini أولاً** (المفتاح الوحيد المتحقَّق من عمله حياً على الإنتاج): سلسلة نماذج
  `gemini-2.5-flash-image` ← `gemini-2.0-flash-exp-image-generation`، قابلة للتجاوز
  بمتغير `GEMINI_IMAGE_MODEL`.
- **Grok ثانياً** (إن وُجد مفتاح `xai-`): `grok-2-image-1212` ← `grok-2-image`
  عبر `XAI_IMAGE_MODEL`.
- **Groq مستبعد عمداً** — لا يملك واجهة صور أصلاً.
- اكتشاف تلقائي لمفتاح Grok الموضوع خطأً في خانة `GROQ_API_KEY` (نمط r44 نفسه).
- **تصنيف أخطاء صادق**: 401/403 = `auth` (تُسقط المزوّد كله)، 429 = `rate`،
  400/404 مع «API key not valid» = `auth` (جوجل ترجعها 400 لا 401!)، مع كلمات
  safety/prohibited = رفض محتوى صادق لا يُعاد محاولته عبثاً على نموذج آخر.
- **الأبعاد**: `1:1 / 4:3 / 3:4 / 16:9` عبر `imageConfig.aspectRatio`، مع إعادة
  محاولة واحدة بدونها للنماذج الأقدم التي ترفضها — بلا حلقات أبداً.

### 2. المسار — `src/app/api/ai/image/route.ts` (جديد)

على عقد `/api/ai` نفسه حرفياً:

- جلسة مطلوبة (401 بلا جلسة)، وصف ٣–٦٠٠ حرف، `maxDuration = 60` (سقف Hobby).
- **حد استخدام يليق بالصور**: فاصل ١٠ ثوان + سقف ١٢ صورة/يوم/مستخدم (المحادثة
  ١٥٠/يوم — كانت ستحرق حصة صور مجانية في دقائق).
- بلا مفاتيح صور ← `200 { needsConfig: true }` (اتفاقية بطاقة الإعداد نفسها).
- أخطاء عربية صادقة للطالب + تلميح تقني للمالك فقط (أي مزوّد، أي نموذج، ماذا يفعل).
- **الحدود الأمنية للمالك**: وصف يطلب كلمات مرور/بطاقات/مفاتيح يُرفض محلياً برسالة
  صريحة **قبل** استدعاء أي مزوّد — الذكاء الاصطناعي لا يُربط بالمحسوس أبداً.
- **الخصوصية**: لا صف قاعدة بيانات، لا تخزين، لا سجل محتوى — الوصف يسافر للمزوّد
  لطلب واحد ثم يُنسى (نفس عقد المساعد الذكي).

### 3. الواجهة — `src/components/talib/tools/image-studio-tool.tsx` (جديد)

- **الموضع**: بطاقة مميزة ثانية تحت المساعد الذكي مباشرة (عائلة أرجوانية-
  فوشيا) — **ليست داخل شبكة الأدوات المحلية** حتى يبقى لافت «أدوات تعمل داخل
  جهازك» صادقاً ١٠٠٪ (نمط r43 نفسه للمساعد).
- صناديق أسلوب حتمية (رسم توضيحي/بطاقة مراجعة/ملصق تحفيزي/غلاف ملخص) بلواحق
  وصف ثابتة — **بلا استدعاء نموذج إضافي** ولا إعادة كتابة خفية للوصف.
- كل الحالات: فارغ (٤ أفكار جاهزة) / توليد (مؤقت ثوانٍ صادق + هيكل بالأبعاد
  المختارة) / خطأ (إعادة محاولة + تلميح للمالك) / نجاح (تنزيل أو مشاركة، نسخة
  أخرى، نسخ الوصف) / غير متصل (رسالة فورية — درس r61) / needsConfig.
- معرض جلسة في الذاكرة (٦ صور كحد أقصى) يختفي عند الإغلاق — مع تنبيه صريح
  «نزّل ما يعجبك».
- بحث «أدواتي» يشمل الاستوديو عبر سلسلة مطابقة خاصة (نمط المساعد) فلا تظهر
  رسالة «لا توجد أداة» كاذبة.

### 4. بحث الإنتاج الحي (بمفاتيح المالك هذه الجلسة)

- مشروع الإنتاج `ntdzvujhujnbazaqzuvo` حي؛ ٤٧ جدولاً مكشوفة؛ **سياسات كتابة
  `telegram_topics` مفعّلة فعلاً** (تحقق بمسار ذاتي التنظيف: إدراج anon ناجح
  ثم حذف عبر service key) — إجراء المالك المعلَّق من r73 **منجز**.
- لا تزال ناقصة (تتحل بأناقة): `bot_config` و`push_subscriptions` و
  `course_materials` — ملفاتها جاهزة في `download/` متى شئت.

## الاختبارات — `scripts/r83-check.ts` (جديد)

**٢٨/٢٨ ✓** بدون أي مفتاح حقيقي:

- منطق الأبعاد والاكتشاف (بما فيها xai- في خانة Groq، ومفتاح Groq وحده = لا صور).
- مسار الخطأ الحقيقي: مفتاح Gemini مزيف ← 400 من جوجل نفسها ← تصنيف `auth`
  لا `model` — فلا إعادة محاولة عبثية على بقية النماذج.
- المسار عبر جلسة مؤقتة (نمط r73، حُذفت فوراً وتحقق الحذف): 401 بلا جلسة،
  400 للوصف الفارغ، **400 لحد كلمات المرور**، 200 `needsConfig` محلياً.
- ترابط الواجهة: البطاقة والحالة والاستيراد وسلسلة البحث.

كذلك: `tsc` صفر أخطاء، `eslint` نظيف على كل ملفات الجولة، و`next build` ناجح
(٨٤/٨٤ صفحة، `/api/ai/image` مسار ديناميكي مسجَّل).

> ملاحظة نزاهة: `bun run lint` الكامل كان **أحمر على r82 نفسها قبل أي تغيير
> منا** (٦ أخطاء `require()` قديمة في scripts/r60–r63) — الجولات السابقة كانت
> تنظّف الملفات المتغيرة فقط، وهو ما التزمناه هنا أيضاً.

## فحص المتصفح الحي (خادم التطوير المحلي)

- المالك: جولة تعريف مكتملة ← أدواتي ← البطاقة الجديدة ظاهرة ← الاستوديو كامل
  (وصف/أسلوب/أبعاد/أفكار) ← «ولّد الصورة» ← بطاقة الإعداد الصادقة مع تعليمات
  المفاتيح، **وبلا أي تسريب للطالب**.
- طالبة حقيقية: نفس المسار ← «يجهّز فريق المنصة هذه الخدمة حالياً» — لا كلمة
  Vercel ولا اسم مفتاح يظهر أبداً (تحقق آلي `ownerLeak=false`).
- كل جلسات الفحص المؤقتة حُذفت وتحقق أنها لم تعد صالحة (204 + 401 بعدها).

## ماذا يحدث على الإنتاج بعد الدفع — تحقق حي (r83b + r83c)

فحصنا المسار على الإنتاج ثلاث مرات بجلسات مؤقتة ذاتية التنظيف:

- **البطاقة تعمل والحدود تعمل**: 401 بلا جلسة، أخطاء عربية صادقة، تلميح المالك
  يظهر بحسابه فقط.
- **مفتاح Gemini الحالي لا يولّد صوراً**: جربنا حياً ٧ أسماء نماذج
  (3.5-flash-image و3.5-flash بمخرج صور وpreview وخط Imagen بواجهته المستقلة
  و2.5/2.0) — كلها 404/400 خلال ~٢ ثانية. هذا متسق مع r44: المفتاح مقيد
  بنموذجي نص فقط (3.5-flash و3.1-flash-lite).
- **الخلاصة العملية**: الاستوديو جاهز ومكتمل، وينطلق لحظة توفر مفتاح يستطيع
  الصور — **إجراء واحد منك** (أيٌّ من):
  1. مفتاح Google AI Studio جديد بترخيص صور (يوضع في `GEMINI_API_KEY` على
     Vercel — أو `GEMINI_IMAGE_MODEL` باسم نموذج الصور إن وجدته في قائمتك)،
  2. أو مفتاح Grok من console.x.ai (يوضع في `XAI_API_KEY`).
- فحص سلامة ختامي بعد النشر: مسار المساعد الذكي سليم (بيضة المالك 200 +
  مُحدِّد المعدل يعمل حياً 429) — نشرنا ٣ مرات متتالية دون أي أثر سلبي.

## إجراءات المالك المعلَّقة (لا تعيق شيئاً)

1. تشغيل ملفات SQL الثلاثة الناقصة متى أردت (bot_config / push_subscriptions /
   course_materials) من `download/`.
2. تصحيح قيمة `SUPABASE_SERVICE_ROLE_KEY` على Vercel (r73b) أو حذفها — التطبيق
   يعمل بأمان في الحالتين بعد سياسات r73 المؤكدة اليوم.
3. المفتاحان المرسلان `prj_…` و`vcp_…` لم يطابقا أي خدمة يعرفها التطبيق أو
   الإطار (ليس Vercel ولا Groq/Gemini/Grok/Telegram) — إن كانا لخدمة تريدها
   مدمجة فأخبرني ما هما وسأوصلها.
