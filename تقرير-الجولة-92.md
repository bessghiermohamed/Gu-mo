# تقرير الجولة 92 — «أدواتي» مضغوطة + شارة الرُتبة في قسم الفوج

**التاريخ:** 19 سبتمبر 2026
**الحالة:** منجزة — فحص r92 51/51 (بينها اختبارات وحدة حقيقية لوحدة التلعيب داخل bun)، انحدارات r91 35/35 وr90 53/53 وr88 34/34 وr87 97/97 وr86 92/92 وr85 38/38 وr81 31/31، tsc 0، `next build` ✓، ملفات الجولة الستة بلا أي ملاحظة eslint.

---

## 1) الخلاصة التنفيذية

طلبا المالك حرفياً في هذه الجولة:

1. **«سأترك دفتر الطالب في أدواتي لكن قلل عرضه وحجمه، ونفس الشيء لباقي صناديق أدواتي»** — بُقي على دفتر طالب (لم يُحذف)، وأُزيلت بطاقته المميزة كاملة العرض (بمعاينتها الوهمية للمحادثة) لتحل محله **خلية مضغوطة داخل شبكة الأدوات** بنفس حجم باقي الأدوات مع الحفاظ على هويته الزمردية، وضُغطت كل بطاقات الشبكة: أيقونات أصغر (w-9 الحاوية / w-4.5 الرمز)، حشوة p-3، عناوين 13px، فجوات 10px.
2. **«في قسم الفوج يظهر اسم بجانبه شعار مثل الألعاب يتغير حسب عدد النقاط، والنقاط تُحسب من نشاط الشخص داخل التطبيق»** — بطاقة شخصية جديدة أعلى شاشة الفوج تعرض **اسم الطالب وبجانبه شارة رُتبته اللعبية** (شعار + اسم + رقم النقاط)، من **سبع رُتب متدرجة**: مبتدئ ← ناشئ ← نشيط ← مجتهد ← مميّز ← خبير ← أسطورة. والنقاط تُحسب على جهاز الطالب من نشاطه الفعلي.
3. **«لا حاجة لكتابة شرح لهذه الميزة للطالب»** — الشارة **صامتة تماماً**: لا فقرة شرح، لا تلميح، لا tooltip. يظهر الاسم والشعار ورقم النقاط فقط، وفحص r92 يمنع نصياً أي «شرح للنقاط» في src/ بأكمله.

**ملاحظة توثيقية:** طلب الجولة السابقة المعلّق («أنشئ دفتراً باسم Summary») لم يُنفّذ — تعليمات المالك الأحدث في هذه الجولة («سأترك دفتر الطالب») هي التي حسمت القرار: بقاء دفتر طالب كما هو مع ضغطه، بلا أي بطاقة جديدة. فحص r92 يثبّت ذلك (لا هوية `"summary"` في tools-tab).

## 2) ما تغيّر بالتفصيل

### أ) «أدواتي» — ضغط البطاقات (tools-tab.tsx)

| قبل (r91) | بعد (r92) |
|---|---|
| بطاقة الدفتر مميزة بعرض كامل + معاينة محادثة وهمية + زر «افتح الدفتر» | خلية واحدة داخل الشبكة بنفس مقاس الأدوات (زمردية متدرجة، شعار مصغّر، وصف سطران كحد أقصى) |
| أيقونات الشبكة: حاوية w-11 h-11 ورمز w-6 h-6 | حاوية w-9 h-9 ورمز w-4.5 h-4.5 |
| حشوة البطاقة p-3.5 وعنوان text-sm | p-3 وعنوان 13px ووصفه أقرب (mt-0.5) |
| فجوة الشبكة gap-3 (12px) | gap-2.5 (10px) |
| عدّاد «الكل» = 9 | يُحصي الدفتر أيضاً: «الكل» = 10 و«أدوات الدراسة» = +1 |

**ما لم يُمسّ عمداً** (عقود r91 كاملة survived): مصفوفة TOOLS بتسع أدوات بلا أي هوية جديدة، بحث normalizeArabic وسطر `noResults` حرفياً نفسه، توجيه `activeTool === "notebook"` و`<NotebookTool`، وعدد `<motion.button` ما يزال اثنان (خلية الدفتر + قالب الشبكة). لافتة «أدوات تعمل داخل جهازك» وحقل البحث ورقائق التصنيف كما هي — الطلب كان عن «الصناديق» (بطاقات الأدوات).

### ب) التلعيب — lib/gamification.ts (وحدة نقية بلا React)

- **مصادر النقاط (كلها من حالة الجهاز المحلية القائمة أصلاً — لا تتبّع جديد ولا شبكة):**
  - يوم نشاط (دفتر حضور محلي `talib-activity-v1`، كتابة idempotent واحدة يومياً من غلاف التطبيق، مع قصّ تلقائي لأقدم من 180 يوماً) ← **5 نقاط**
  - واجب مُنجز (`talib-assignments-completed` — نفس مفتاح شاشتي الواجبات والفوج) ← **20 نقطة**
  - ملاحظة شخصية في ملفاتي (`talib-notes`) ← **10 نقاط**
  - مصدر في دفتر طالب (تُجمع `sources` من كل مفاتيح `talib-notebook-v1-*` وتتجاهل المفاتيح التالفة) ← **10 نقاط**
- **الرُتب السبع** بعتبات 0 / 100 / 250 / 500 / 800 / 1200 / 2000، ولكل رُتبة شعارها ولونها (sprout/book/zap/medal/star/trophy/crown بأسماء رمزية تُحوَّل إلى أيقونات lucide في الشارة — الوحدة نقية قابلة للاختبار بلا DOM).
- `rankFor()` آمنة مع القيم غير السليمة (سالبة/NaN ← مبتدئ)، و`computeActivity()` لا ترمي حتى بلا localStorage (الوضع الخاص).

### ج) الشارة وشاشة الفوج (rank-badge.tsx جديد + group-screen.tsx)

- بطاقة شخصية أعلى شاشة الفوج **قبل شرط الإلحاق بالفوج** — تظهر لكل طالب حتى «بدون فوج»: الحرف الأول في دائرة + الاسم الكامل + الشارة في نهاية الصف.
- الشارة: شعار الرُتبة بلونها + اسمها + رقم النقاط بأرقام `tabular-nums` باتجاه LTR داخل RTL. حجم عادي وحجم `compact` جاهز لمواضع أخرى.
- الخطّاف `useActivityRank` بنمط التطبيق الموحّد: تسوية مؤجلة عبر `setTimeout(0)` — بلا كتابة حالة متزامنة في effect، وبلا hydration mismatch (الخادم وأول رسم يتفقان على null).
- الغلاف (`app/page.tsx`): استدعاء `recordDailyVisit()` واحد في تأثير الإقلاع القائم — نقطة إدخال وحيدة، بلا أي ربط جديد في الأدوات.

### د) الخصوصية — العقد نفسه

النقاط والرُتب **محسوبة ومخزّنة على جهاز الطالب حصراً** (localStorage: `talib-activity-v1` الجديد + المفاتيح القائمة)، ولا يغادر أي رقم إلى أي خادم — توافقاً حرفياً مع لافتة «أدوات تعمل داخل جهازك» وعقود r87/r88. النقاط تُبنى من بيانات نشاط كانت محفوظة أصلاً؛ لم تُضف أي آلية تتبّع جديدة للأفعال.

## 3) فحص r92 (51/51) — `bun scripts/r92-check.ts`

- **(أ) 17 فحصاً** لأدواتي: خلية الدفتر داخل الشبكة وبوابة (الكل/الدراسة)، إزالة البطاقة القديمة ومعاينتها، الأحجام الجديدة (w-4.5 ×11، بلا w-6 ولا w-11 ولا p-3.5، عناوين 13px، gap-2.5)، وبقاء عقود r91 (٩ أدوات، noResults حرفياً، توجيه الدفتر، motion.button ×2، key="notebook")، وعدّادات تحسب الدفتر، ولا هوية `"summary"`.
- **(ب) 18 فحصاً — اختبارات وحدة فعلية داخل bun** على الوحدة النقية: عتبات الرُتب السبع صاعدة، كل حدّ يفتح رُتبته ويُغلق قبله (99←مبتدئ، 1199←مميّز…)، القيم غير السليمة آمنة، `computeActivity` بلا localStorage لا ترمي، ثم **محاكاة تخزين كاملة**: 12 يوماً + 3 واجبات منجزة من 4 + ملاحظتان + 4 مصادر عبر مستخدمين اثنين مع مفتاح تالف ← النقاط 180 بالضبط (12×5 + 3×20 + 2×10 + 4×10).
- **(ج) 9 فحوص** للشارة وشاشة الفوج: التوصيل، الشعار/الاسم/النقاط فقط، لا `<p>` ولا `<div>` داخل الشارة، موقعها قبل شرط الفوج وبجانب الاسم، و**مسح 214 ملفاً في src/ بلا أي عبارة شرح للنقاط** (قائمة ممنوعة: «كيف تحسب نقاطك» و«اجمع النقاط» وأخواتها).
- **(د) 3 فحوص** للغلاف: الاستدعاء والاستيراد وداخل تأثير الإقلاع بلا فواصل زمنية.
- **(هـ) 4 فحوص** تأمين عابر للجولات: ads.txt حرفياً، AdsenseGate بلا pagead2، سطح API (الدفتر حي/الاستوديو غائب)، وجود فحصي r90/r91.

**بوابة الجودة الكاملة:** `tsc --noEmit` = 0 أخطاء، eslint نظيف للملفات الستة المتغيرة (الخطأ العام السباعي في سكربتات r60/r61/r63 قديم وموثّق على الشجرة النظيفة)، `next build` ✓ — سطح `/api` لم يتغير: `/api/ai/notebook` وحيدة كما في r91.

## 4) الصدق والحدود

- **النقاط جهازية بالتصميم:** لا يمكن رؤية نقاط زملائك ولا مقارنتهم — المالك طلب شارة «تحسب من نشاط الشخص»، والتنفيذ الوحيد المتوافق مع عقد الخصوصية المحلي هو النشاط على هذا الجهاز. أي لوحة صفّ لاحقة تتطلب خادماً وقرار خصوصية صريحاً.
- **أيام النشاط تُحسب من أول تركيب لهذه الجولة:** الأجهزة الحالية تبدأ بصفر أيام في الدفتر الجديد `talib-activity-v1` (أما الواجبات والملاحظات والمصادر فتُحتسب فوراً من مفاتيحها القائمة).
- **طلب «Summary» السابق لم يُنفّذ عمداً** — تعليمات المالك الأحدث («سأترك دفتر الطالب») تحسمها؛ إن رغب المالك في بطاقة «ملخص» لاحقاً فالمسار جاهز بنمط TOOLS.
- **الأداء:** مسح localStorage بحثاً عن مفاتيح الدفتر عملية O(n) على مفاتيح الجهاز وتُجرى مرة واحدة عند فتح شاشة الفوج فقط — أثرها مهمل، ولا يوجد أي استقصاء دوري جديد.

## 5) خطوات المالك (خارج الكود)

لا شيء مطلوب في هذه الجولة — الميزتان تعملان تلقائياً بعد النشر. ملفات المالك المعلّقة من r90 ما زالت قائمة (التحقق من الهوية/PIN، مراجعة قائمة المواقع `gu-mo.vercel.app` حرفياً، طلب إعادة المراجعة).
