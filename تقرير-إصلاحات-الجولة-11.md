# تقرير إصلاحات الجولة 11 — دمج تبويبات لوحة الإشراف + نقل المقاييس + مدخل التبليغ للطالب

**التاريخ:** 2026-09-04 · **النطاق:** طلبات المالك الأربعة بعد اختبار الجولة 10

## الخلاصة

نفّذت هذه الجولة طلبات المالك الأربعة حرفياً وبمنطق «النظام الكامل» (وثيقة المراجعة الشاملة):

1. **دمج تبويبات اللوحة** — «الهيكل» + «الملامح» + «السنوات» كانت ثلاثة تبويبات لسلسلة أكاديمية واحدة تُدار في أماكن متقطعة؛ أصبحت **تبويباً واحداً «الهيكل»** بأقسام فرعية بترتيب التسلسل: المؤسسات → التخصصات → الملامح → السنوات. وكذلك «المجموعات» و«الأفواج» (الفوج يعيش داخل مجموعة!) أصبحتا **تبويباً واحداً «المجموعات والأفواج»** بقسمين فرعيين. نمط الأقسام الفرعية هو نفسه النمط المعمول به في مدير تيليجرام (قائمة Tabs داخلية).
2. **حذف المقررات من لوحة الإشراف** — تبويب «المقررات» أُزيل من اللوحة بالكامل، ونُقلت قدراته (إضافة + تعديل + حذف مقاييس) إلى **شاشة المقررات التي يتصفحها الطلبة فعلاً**: كل مقياس يصبح له زرا تعديل/حذف لأصحاب الصلاحية، مع نفس حراس الخادم (الحذف محظور ما دامت اختبارات/واجبات/نقاط/محاضرات مرتبطة بالمقياس، والرسالة تشرح ما يجب تنظيفه).
3. **إزالة السحابة** — تبويب «السحابة» (اختبار اتصال Supabase — أداة بنية تحتية وليست أداة عمل يومية) أُزيل من اللوحة مع مكوّنه `CloudManager`، مع تنظيف مفاتيح الترجمة الميتة `files.tabCloud` من كلا ملفي اللغتين.
4. **تحسين تصميم الواجهة** — شبكة التبويبات انتقلت من 13 تبويباً متلاصقاً إلى **8 وجهات** (مركز التحكم + 7 صناديق)، وصف واحد نظيف على الشاشات الأكبر (7 أعمدة)، مع حماية من استعادة تبويب محذوف من `talib-admin-tab` في الجلسة (يعود آمناً إلى مركز التحكم).

## الإبلاغ عن مشكلة — زر «إرسال التبليغ» للطالب العادي

نظام التبليغ كان قائماً (`POST /api/issues` يقبل كل مستخدم مسجّل) لكن مدخله الوحيد كان **أيقونات علم صغيرة** داخل بطاقات المقررات والواجبات — سهلة التغاضي عنها وغير موجودة أصلاً لأي محتوى آخر (ملفات، جدول، امتحانات). في هذه الجولة:

- مدخل واضح دائم **«الإبلاغ عن مشكلة»** في شاشة «حسابي» — **ظاهر لكل الأدوار ومن بينها الطالب العادي (STUDENT)** بلا أي بوابية أدوار.
- أنواع التبليغ الأربعة المصممة منذ البداية (مفاتيح `reportIssue.*` كانت موجودة في ملفات الترجمة منذ الجولة الأولى دون أي واجهة تستخدمها): ملف تالف / خطأ في الجدول / موعد امتحان غير دقيق / أخرى.
- موضوع اختياري + وصف إلزامي، وزر **«إرسال التبليغ»** بحالات تحميل/تعطيل واضحة، ورسالة نجاح، وتصريف الحقول بعد الإرسال.
- التبليغ يصل للمشرفين في تبويب «التبليغات» كما هو، وأُحدّثت نصوص اللوحة لتذكر المداخل الثلاثة (المقررات، الواجبات، حسابي).

## إصلاح مصاحب — تعارض صلاحيات قديم على المقاييس

زر «مقياس جديد» في شاشة المقررات كان يظهر لكل من يملك `canManageRoles` (بما فيهم ممثل الفوج) بينما الخادم يفرض `canCreateModules` (المالك/مسؤول التخصص فقط) — أي أن الممثل كان يرى زراً يفشل دائماً بخطأ 403. الآن الواجهة تستخدم `canCreateModules` نفسه المفروض في `/api/courses` (قاعدة الوثيقة: المنطق يُفرض في طبقة البيانات، والواجهة تعكسه).

## تفاصيل تقنية

| الملف | التغيير |
|---|---|
| `src/components/talib/screens/admin-panel-screen.tsx` | −244 سطراً: حذف `ModulesManager` و`CloudManager`؛ دمج «الهيكل/الملامح/السنوات» في `StructureManager` بأقسام فرعية؛ دمج «المجموعات/الأفواج» في `GroupsAndCohortsManager`؛ تنظيف مستوردات أيقونات وصلاحيات ميتة |
| `src/components/talib/screens/courses-screen.tsx` | نقل إضافة/تعديل/حذف المقاييس (`EditCourseDialog` + تأكيد حذف مع رسالة الحارس) ببوابة `canCreateModules` المطابقة للخادم |
| `src/components/talib/screens/profile-screen.tsx` | مكوّن `ReportIssueDialog` — مدخل التبليغ العام لكل الأدوار |
| `src/messages/ar.json` + `en.json` | حذف `files.tabCloud` (مفتاح ميت بعد إزالة السحابة) |

## التحقق

- `bunx eslint` على الملفات الثلاثة المعدّلة: **نظيف ✓**
- `bunx tsc --noEmit`: لا أخطاء جديدة في الملفات المعدّلة ✓ (أخطاء قديمة مسبقة في 3 ملفات لم تُمَس: `api/announcements/mark-read`، `api/telegram/setup`، `course-detail-screen` — مخفاة عن البناء عبر `ignoreBuildErrors` كما في الجولات السابقة، ويُقترح معالجتها في جولة تنظيف لاحقة)
- `bun run build`: بناء إنتاج ناجح ✓
- تشغيل الخادم (`next start`) واستجابة `/` بـ HTTP 200 ✓

## مؤجّل (قرارات منتج)

- بنية البيانات نفسها (Specialty/Track/Year وGroup/Cohort) لم تُمَس — الدمج كان في **طبقة الواجهة والإدارة** كما طلب المالك؛ أي تغيير في المخطط يتطلب ترحيل Supabase وقراراً صريحاً.
- نقل ترميز `course-detail-screen` للـ `moduleId` المفقود (خطأ tsc قديم) إلى جولة الإصلاحات القادمة.
