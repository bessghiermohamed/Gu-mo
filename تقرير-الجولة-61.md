# تقرير الجولة 61 — تجربة أوفلاين حقيقية لطالب

## بلاغ المالك

«قمت بقطع الاتصال بالإنترنت وحاولت تسجيل الدخول. ظهرت رسالة "أنت غير متصل"، ولم أتمكن من الدخول بعد ذلك.»

## التشخيص

كانت الجولة 58 قد أضافت شريط حالة الاتصال في شاشة الدخول، لكن التصميم آنذاك توقف عند الإبلاغ فقط:

1. **الدخول محجوب بالكامل دون اتصال** — زر الدخول يعرض رسالة خطأ ولا يحاول شيئاً.
2. **الأخطر**: الطالب المسجَّل دخوله الذي يُعيد تحميل الصفحة دون اتصال كان يُطرد إلى شاشة الدخول (فشل طلب `/api/auth/me` يُفسَّر كخروج)، فيخسر وصوله كلياً بمجرد انقطاع الشبكة.

بما أن تسجيل الدخول في طالب يعتمد على الاسم والبريد فقط (بلا كلمة سر)، فإن التحقق دون اتصال ممكن بنفس مستوى الأمان: مطابقة البيانات مع آخر دخول ناجح على نفس الجهاز.

## ما أُنجز

### 1. الدخول دون إنترنت (بلاغ المالك حرفياً)
- بعد كل دخول ناجح، يتذكّر الجهاز البيانات التي نجحت (`talib-last-login`) وصورة الحساب المؤكدة من الخادم (`talib-cached-user`).
- عند المحاولة دون اتصال بنفس الاسم والبريد → يُمنح доступаً بوضع «عدم الاتصال» مع رسالة: «تم الدخول في وضع عدم الاتصال — بياناتك المحفوظة متاحة وستتزامن تلقائياً عند عودة الإنترنت».
- بيانات خاطئة → رسالة واضحة: الدخول دون اتصال متاح فقط بآخر بيانات استُخدمت على هذا الجهاز.
- إنشاء حساب جديد دون اتصال → سبب دقيق: يحتاج اتصالاً لأنه يكتب في قاعدة البيانات.
- الحقول تُعبَّأ مسبقاً عند العودة للجهاز، والشريط الأصفر يضيف سطراً: «يمكنك الدخول الآن بآخر بيانات استخدمتها على هذا الجهاز…».

### 2. استرجاع الجلسة عند إعادة التحميل دون اتصال
- إعادة تحميل الصفحة والشبكة مقطوعة لم تعد تُخرج الطالب: تُستعاد الجلسة من ذاكرة الجهاز مع شريط داخل التطبيق: «أنت في وضع عدم الاتصال — الجلسة والبيانات المعروضة محفوظة على جهازك، وكل شيء يتزامن تلقائياً عند عودة الشبكة».
- شرط الاسترجاع هو **فشل الشبكة فعلياً** (فشل الطلب نفسه)، لا مؤشر `navigator.onLine` — لأن الأخير يكذب خلف بوابات الأسِرّة المعزولة وشبكات VPN المقطوعة.

### 3. التطبيق نفسه يعمل دون إنترنت (Service Worker)
- `sw.js` يخزّن هيكل التطبيق بأسلوب «الشبكة أولاً ثم الذاكرة»: المتصل يحصل دائماً على أحدث بناء (لا خطر تقادم)، والمقطوع عنه يحصل على آخر نسخة رآها جهازه — فيُقلع التطبيق فعلاً من دون اتصال بدل صفحة خطأ المتصفح.
- التسجيل يتم الآن مع أول زيارة لـ /app (سابقاً بعد التهيئة فقط).

### 4. البيانات المحفوظة
- شاشات القراءة الرئيسية (المقاييس، الجدول، الاختبارات، الإعلانات) تقرأ آخر نسخة ناجحة من ذاكرة الجهاز عند انقطاع الشبكة، مع شارة: «بيانات محفوظة على جهازك — آخر تحديث: [التاريخ]».
- فور عودة الاتصال: يختفي الشريط والشارة، تُجلب البيانات جديدة، وتُعاد مزامنة الجلسة والإشعارات تلقائياً مع إشعار «عاد الاتصال بالإنترنت».

### 5. أمان وضبط الخروج
- الخروج الصريح لا يُبعَث: علامة محلية تمنع استرجاع الجلسة بعده، والخروج أثناء انقطاع الشبكة يترك علامة «معلّقة» تقتل ملف تعريف الارتباط عند أول مزامنة بعد العودة.
- من دخل بوضع عدم الاتصال ثم عادت الشبكة ووجدنا جلسته منتهية → إعادة دخول صامتة بنفس بيانات الجهاز (نفس مستوى التحقق، بلا كلمة سر أصلاً).
- المساعد الذكي يحتاج الخادم فعلاً — عند الانقطاع يظهر فوراً: «أنت غير متصل — المساعد الذكي يحتاج اتصالاً ليعمل. تصفّح بياناتك المحفوظة حتى تعود الشبكة».

## الاختبار

- **محلياً على بناء الإنتاج** (`scripts/r61-offline-test.js`): 15/15 — يشمل محاكاة انقطاع حقيقية (Playwright `setOffline`)، إقلاع التطبيق من ذاكرة الـService Worker، استرجاع الجلسة، شارات البيانات المحفوظة، سيناريو المالك حرفياً، رفض البيانات الخاطئة، إعادة المزامنة، وقتل الجلسة «الزومبي». لقطات: `download/r61/`.
- **على الإنتاج المباشر** (`scripts/r61-prod-smoke.js`): 9/9 بحساب مؤقت يُحذف بعد الفحص — من gu-mo.vercel.app نفسه.
- `tsc` صفر أخطاء، `eslint` بلا ملاحظات جديدة، البناء 69/69، والنشر اكتمل في ~45 ثانية.

## ملحوظة الاستخدام

أول استخدام دون اتصال على جهاز جديد يحتاج زيارة واحدة سابقة بتوصّل (تسخّن الذاكرة وتهيّئ الـService Worker) — وهذا نمط الاستخدام الطبيعي لأي طالب عائد.
