# تقرير إصلاحات الجولة ٤٠ — زر الرفع في كل مقياس، وDrive كمساحة مشتركة

**الطلب الأصلي (مترجَم):** «في بعض المقاييس لا توجد أزرار رفع؟ مثلاً إذا رفع مشرف ملفاً لمقياس النحو، أين يُخزَّن؟ إنه على Google Drive. جهاز Drive الخاص بي يصبح مساحة مشتركة — متاحة لأي طالب.»

**التشخيص:** مسار الرفع إلى Drive كان موجوداً ويعمل فعلياً منذ الجولة ٣٢ (المتصفح ← حساب Drive الخاص بالمشرف ← مشاركة «أي شخص لديه الرابط» ← صف بيانات وصفية فقط في قاعدة البيانات)، لكن **زر الرفع كان مدفوناً داخل تبويب المواد فقط**، والمقياس يُفتح افتراضياً على تبويب الدروس — فيرى المشرف الذي يفتح المقياس شاشة بلا أي زر رفع، ومن هنا انطباع «بعض المقاييس بلا أزرار رفع». (أما الطالب فلا يرى أزرار رفع أصلاً — وهذا مقصود: الرفع للمشرفين فقط.)

## ما نُفِّذ (ملفان)

### ١. بطاقة رفع على مستوى المقياس نفسه — في كل تبويب، في كل مقياس
- بطاقة جديدة **فوق تبويبات المحتوى مباشرة** لعناوين المقياس (الدروس/المواد/الاختبارات/الواجبات)، تظهر للمشرفين فقط (OWNER / SPECIALTY_ADMIN / REPRESENTATIVE بصلاحية نطاق) مهما كان التبويب المفتوح — لم يعد الزر خاسراً في تبويب واحد.
- العنوان: «رفع ملف لهذا المقياس» مع زر مدمج «رفع ملف (Drive)» يفتح نفس حوار النشر المرتبط بالمقياس (`moduleId` مضبوط تلقائياً — المادة تظهر في تبويب مواد هذا المقياس تحديداً).
- **سطر التخزين الصريح** تحت العنوان: «إلى Google Drive الخاص بك — مساحة مشتركة يحمّل منها الطلبة الملفات مباشرة، دون أن يُخزَّن شيء على السيرفر» — إجابة سؤال «أين يُخزَّن الملف؟» داخل الواجهة نفسها.
- الطالب لا يرى هذه البطاقة إطلاقاً (`canManage` يتحكم في العرض كما كان).

### ٢. حوار النشر — صياغة «المساحة المشتركة»
- تذييل وضع الرفع أُعيدت صياغته: «يُرفع الملف إلى مجلد **📚 مكتبة طالب** في حسابك على Drive (١٥ ج.ب) — **Drive الخاص بك يصبح مساحة مشتركة**: أي طالب يرى المادة في المقياس وينزّلها مباشرة من Drive، **ولا يُخزَّن أي بايت على Supabase**».
- زر الحوار أصبح يقبل نمط عرض مرناً (`triggerClassName`) — مدمج في البطاقة، وكامل العرض داخل تبويب المواد كما كان.

### ٣. الحالة الفارغة في المواد
- تلميح المشرف الآن يشير إلى الزر الجديد: «استخدم زر «رفع ملف (Drive)» أعلى الصفحة — يُحفظ الملف في حساب Drive الخاص بك ويصبح متاحاً لكل الطلبة للتنزيل المباشر».

## نموذج التخزين (كما هو مطبَّق — بدون تغييرات هذه الجولة)
```
متصفح المشرف ──الرفع المباشر──► Google Drive الخاص به
                                   └─ مجلد «طالب — Talib/📚 مكتبة طالب»
                                   └─ مشاركة: أي شخص لديه الرابط (قارئ)
قاعدة البيانات (Supabase) ◄── صف بيانات وصفية فقط: العنوان + رابط التنزيل المباشر + حجم الملف + معرّف ملف Drive + معرّف المقياس
طالب المقياس ◄── ينزّل الملف مباشرة من Drive المُشترَك (زر «تنزيل» + شارة «على Drive»)
```
- بايتات الملف **لا تلمس** سيرفراتنا ولا Supabase إطلاقاً — حدّ Vercel 4.5MB على هيئات الطلبات لا يعترضها لأن الرفع يمر من المتصفح إلى googleapis.com مباشرة.
- المساحة المستهلكة هي من الـ15 جيجابايت المجانية في حساب Drive الخاص بالمشرف/المالك.

## التحقق
- tsc: صفر أخطاء. eslint: نظيف. `next build`: أخضر.
- **جولة متصفح حقيقية 390×844 بحسابين متوازيين على SQLite محلي:**
  - **مالك** (مالك الجولة أربعين): فتح مقياس «علم اللغة العام» → بطاقة «رفع ملف لهذا المقياس» ظاهرة فوق التبويبات **في التبويبات الأربعة كلها** (الدروس/المواد/الاختبارات/الواجبات — عدّاد التحقق 1 في كل تبويب)؛ في تبويب المواد ظهر الزران معاً (بطاقة الرأس + «إضافة مادة للمقياس» داخل التبويب)؛ الحوار يفتح على وضع «رفع ملف (Drive)» افتراضياً (محلياً تظهر بطاقة الإعداد الكهرمانية لأن `NEXT_PUBLIC_GOOGLE_CLIENT_ID` غير مضبوط محلياً — المُنتَج يحمل المعرّف مثبتاً في الحزمة كما تحقّقنا في الجولة ٣٨).
  - **طالب** (طالب الجولة أربعين): نفس المقياس → **صفر** أزرار رفع/إضافة في التبويبات الأربعة؛ يرى المادة المزروعة كمرجع نقر (عنوانه رابط) بلا أزرار إدارة.
  - `scrollWidth = 390` في الجلستين (لا فائض أفقي)، صفر أخطاء console. لقطات في `download/verify-390-r40/`.
- **النشر:** دفع `4f5a021..f26d331` → Vercel → chunk المُنتَج `a06bd8ef0c3d1e8c.js` يحتوي «مساحة مشتركة» و«رفع ملف لهذا المقياس» معاً — الكود حيّ على gu-mo.vercel.app.

## ملاحظة تشغيلية لربط المواد بالمقاييس في المُنتَج (Supabase)
رفع الملفات يعمل في كل الأحوال (إن لم توجد أعمدة إضافية يُدرج الصف الأساس دون فشل)، لكن **ظهور المادة داخل تبويب مواد المقياس** يحتاج عموداً واحداً في قاعدة بيانات المُنتَج. إن ظهرت عندك بطاقة كهرمانية في تبويب المواد تفيد بذلك، نفّذ مرة واحدة في محرر SQL داخل Supabase:
```sql
ALTER TABLE library_references ADD COLUMN IF NOT EXISTS module_id INTEGER;
ALTER TABLE library_references ADD COLUMN IF NOT EXISTS storage_path TEXT;
ALTER TABLE library_references ADD COLUMN IF NOT EXISTS file_size BIGINT;
```
(لو نفّذت سكربتات الجولتين ٣٢/٣٣ سابقاً فالأعمدة موجودة وأنت جاهز.)
