# تقرير الجولة 49 — إعادة تصميم تجربة AdSense بالكامل

## الطلب

> «redesign AdSense» — إعادة تصميم عنصر إعلانات AdSense في الموقع العام،
> وملاحظات على بقية عناصر المشروع التي لم تُعاد تصميمها بعد.

## تشخيص الوضع القديم

فحص حقيقي للمواضع الستة كشف خمس مشاكل:

| المشكلة | التفصيل |
|---|---|
| **قفز التخطيط (CLS)** | `<ins>` بصيغة auto بدون أي ارتفاع محجوز — عند ظهور الإعلان يقفز المحتوى تحته |
| **صناديق ميتة دائمة** | الوحدات غير المملوءة (الموقع قيد المراجعة حالياً) تترك صندوقاً فارغاً بحدود في كل صفحة — 5 صناديق ميتة عبر الموقع |
| **بدون تسمية مرئية** | «مساحة إعلانية» موجودة كـ aria-label فقط — الزائر المبصر لا يميّز المحتوى المدفوع (مخالف ل أفضل ممارسات سياسة AdSense) |
| **خمس تغليفات مختلفة** | كل صفحة تغلّف الوحدة بطريقتها: قسم عريض، صندوق p-3، rounded-xl على الوحدة نفسها… لا اتساق |
| **حالة تحميل غائبة** | لا شيء يوضح أن المساحة قيد التعبئة — فراغ صامت |

**ملاحظة تقنية حاسمة اكتُشفت أثناء التحقق الحي:** وحدة AdSense **غير
المملوءة** تُدرج `<iframe>` قياس ظاهر (280px) رغم أن Google نفسها تعلّمها
`data-ad-status="unfilled"` — أي أن منطق «وجود iframe = إعلان ظاهر» خاطئ
جوهرياً، والإشارة الموثوقة الوحيدة هي `data-ad-status`.

## ما نُفّذ

### 1) المكوّن الجديد `AdSlot` (src/components/ads/ad-slot.tsx)
بطاقة موحّدة بهوية فيسبوك (r46) عبر المواضع الستة:

- **صف التسمية**: «إعلان» بأيقونة مكبر صوت، `text-[10px]` خافت — واضح
  بلا صراخ، يفصل المحتوى المدفوع عن التحريري للقارئ RTL.
- **ارتفاع محجوز** `min-h-[110px] sm:min-h-[140px]` طوال فترة التعبئة —
  المحتوى تحته لا يقفز (CLS). مقايضة موثّقة في الكود: تعبئة أطول من
  المحجوز (إعلان 250px) قد تدفع الصفحة قليلاً — الحالة الشائعة مغطاة.
- **هيكل عظمي نابض** خلف الوحدة أثناء التعبئة بدل الفراغ الصامت.
- **انهيار أنيق للوحدات الفارغة**: تقنية `grid-rows 0fr/1fr` بانتقال
  500ms — الوحدة غير المملوءة (مراجعة/لا إعلان/حاجب إعلانات) تنطوي
  بارتفاع صفر: **لا صناديق ميتة بعد اليوم**. عند الموافقة وتعبئة
  الإعلانات تبقى الوحدات ظاهرة والمساحة محجوزة أصلاً.
- **استرجاع التعبئة المتأخرة**: المراقب (MutationObserver) يعيش مدة
  المكوّن — إن تعبّأت الوحدة بعد الانهيار (شبكة بطيئة) تتوسع فوراً
  بدل ترك انطباع مخفي داخل حاوية منهارة.
- **كشف مبني على data-ad-status فقط** (+ مهلة 5 ثوانٍ للحالة الثالثة:
  السكربت لم يُحمّل أصلاً) — انظر الملاحظة التقنية أعلاه.
- **أوضاع المالك**: `adTest` (شارة كهرمانية) و`showSlotId` (رقم الوحدة)
  على صفحة الاختبار المخفية فقط.
- **وضع ليلي تلقائي** عبر الرموز: بطاقة #242526 ونص #B0B3B8 (متطابق مع
  الوضع الداكن الرسمي لفيسبوك).

### 2) مصدر وحيد لرقم الوحدة
`ADSENSE_SLOT_MIMO` في `src/lib/ads.ts` — كان الرقم السحري «4214645931»
مكرراً في 6 مواضع.

### 3) توحيد المواضع الستة
| الصفحة | قبل | بعد |
|---|---|---|
| الهبوط | قسم max-w-5xl + rounded-xl على الوحدة | عمود max-w-3xl محاذٍ لعمود الأسئلة الشائعة أسفله (إيقاع بصري) |
| المميزات | صندوق p-3 داخل القسم 3 | AdSlot بغلاف mt-2 |
| المدونة | صندوق p-3 نهاية القائمة | AdSlot بغلاف mt-10 |
| مقال المدونة | صندوق p-3 بعد القسم 1 | AdSlot بغلاف mt-2 |
| الدليل | صندوق p-3 بعد القسم 4 | AdSlot بغلاف mt-2 |
| صفحة اختبار الإعلانات | ترويسة يدوية + AdUnit | AdSlot مع adTest + showSlotId |

حُذف `ad-unit.tsx` القديم (لا مستورِدات متبقية — grep نظيف).

## التحقق (متصفح حقيقي + إنتاج محلي)

- `tsc --noEmit` صفر أخطاء · `eslint` نظيف على الملفات الثمانية ·
  `next build` أخضر (68/68 مسار).
- **الحالة الفارغة**: الهبوط والمدونة على 1280 و390px — الوحدة تنطوي
  إلى 0px مع `aria-hidden`، لا صندوق متبقٍ، scrollWidth=390 بلا تجاوز
  أفقي.
- **الاسترجاع المتأخر**: محاكاة `data-ad-status="filled"` يدوياً على
  وحدة منهارة → توسّعت فوراً (326px، aria-hidden=false). المسار
  الحرج: لن يبقى انطباع مخدوم غير مرئي.
- **الوضع الليلي** (فرض `.dark`): بطاقة rgb(36,37,38) ونص rgb(176,179,184)
  — مطابقة رموز r46.
- **صفحة الاختبار**: تعمل بوضع adTest (على localhost تظهر «unfilled»
  لأن Google لا يقدّم إلا للمجالات المسجّلة — على gu-mo.vercel.app
  سيظهر اللافتة التجريبية). الترويسة الكهرمانية للصفحة بقيت كما هي.
- لا أخطاء كونسول صفحة، ولا أخطاء adsbygoogle push.
- لقطات: `download/r48/` — 01 هيكل عظمي، 02 انهيار، 03 صفحة الاختبار،
  04 مدونة سطح مكتب، 05/07 مدونة جوال، 06 وضع ليلي.

## قرارات مقصودة

- **لا مواضع جديدة** — كثافة الإعلانات كما هي (سياسة AdSense: أقل
  وأفضل؛ المواضع الحالية بعيدة عن الأزرار وال CTAs).
- **الاعتماد على data-ad-status** لا على iframe — اكتشاف الـiframe
  القياسي في الوحدات الفارغة وثّق في الكود لمن يعدّله لاحقاً.
- **مهلة 5 ثوانٍ** — تغطي حاجبات الإعلانات والسكربت المحجوب؛ أقصر
  منها يقتل تعبئة الشبكات البطيئة، أطول يطيل الصندوق الفارغ بلا داعٍ.
- **الإعلانات الحقيقية على الصفحات العامة كما هي** (بدون بوابة env
  جديدة) — إن كان المالك يريد إيقافها حتى الموافقة، متغير env واحد
  مستقبلاً يكفي؛ لم نوسّع النطاق.

## خطوات مقترحة على المالك (خارج الكود)

1. **أرقام وحدات لكل موضع**: إنشاء وحدات منفصلة في لوحة AdSense
   (banner للهبوط، in-article للمقالات) → تقارير أدق لكل موضع؛
   تُضاف كثوابت في `src/lib/ads.ts`.
2. **موافقة زوّار EEA (إن وجدت حركة أوروبية)**: سياسة Google تتطلب
   رسالة موافقة (CMP) للإعلانات المخصصة — حالياً الجمهور جزائري
   بالأساس، لكن إن نما الترافيك الأوروبي فهذا بند امتثال مطلوب.

## النشر

- كوميت واحد `r48` — الملفات: ad-slot.tsx (جديد)، ad-unit.tsx (محذوف)،
  ads.ts، والصفحات الست. مرفوع إلى origin/main بنفس الجلسة؛ Vercel
  ينشر تلقائياً.
