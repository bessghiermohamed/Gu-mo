# تقرير الجولة 85 — استوديو HTML في بوت طالب (مولّد الصفحات العربية)

**التاريخ:** 16 سبتمبر 2026
**الحالة:** منجزة — فحص 38/38، تحقّق أنواعي (tsc) نظيف، فحص لين (eslint) نظيف.

---

## 1) الخلاصة التنفيذية

تضيف هذه الجولة أمر `/html` إلى المحادثة الخاصة مع بوت طالب: يكتب الطالب وصفاً قصيراً لما يريد، فيبني له البوت **صفحة ويب عربية كاملة (HTML/Tailwind، RTL)** ويستلمها ملفاً `.html` يفتحها في متصفحه. البناء يمرّ بخط أنابيب ذي ممرّين مستلهم من تقنيتي «النمط كتوليد مقيّد» و«خط التوليد–النقد» في منصات التوليد الحديثة (Alborihi AI Studio نموذجاً)، مطبّقتين على هوية «طالب» البصرية وبمفاتيح المنصة نفسها — بلا أي خدمة خارجية جديدة ولا أي تخزين.

هذه الجولة تُنفَّذ بعد تراجع الجولة 83 (استوديو الصور) بطلب المالك — الشجرة انطلقت من حالة r82 تماماً (r84).

## 2) كيف تعمل — خط الممرّين

- **الممرّ 1 (توليد):** صفحة HTML كاملة تحت قيود النمط المختار (archetype): لوحة ألوان صريحة hex، سلّم خطوط، قواعد مسافات، ومفردات مكوّنات محدّدة — فتنحصر قرارات التصميم مسبقاً ويبقى للنموذج المحتوى والبناء فقط. عقد مخرجات صارم: مستند واحد يبدأ بـ `<!DOCTYPE html>` وينتهي بـ `</html>`، عربي RTL، Tailwind عبر CDN، بلا صور خارجية ولا جافاسكربت خارجي ولا نماذج إرسال، متجاوب من 360px.
- **الممرّ 2 (نقد خماسي):** الناتج يُقيَّم عبر خمسة أبعاد (الخطوط، التنسيق، تناسق الألوان، أمانة المحتوى، الإتاحة) بحكم صارم: PASS يُشحن، REFINE يعيد التوليد **مرة واحدة كحد أقصى** مع ملاحظات الناقد — بلا حلقات أبداً.
- **ميزانية وقت صارمة:** 52 ثانية إجمالاً (توليد 26ث، نقد 13ث، والتحسين يتطلب 32ث متبقية على الأقل) — تبقى داخل سقف دالة الويبهوك (60ث) حتى مع جولة التحسين.

## 3) الأنماط الأربعة (archetypes)

| النمط | متى يُختار | مثال الأمر |
|---|---|---|
| بطاقة مراجعة (الافتراضي) | تلخيص مركّز قبل المحاضرة أو الامتحان | `/html المبتدأ والخبر` |
| صفحة درس | درس كامل بأقسام وأمثلة وأخطاء شائعة | `/html درس المقاومة الكهربائية` |
| ملخص امتحان | كثيف منظم لليلة الامتحان: جداول وصيغ | `/html امتحان قوانين كيرشوف` |
| صفحة عرض | تقديم هادئ أنيق بمساحات سخية | `/html عرض عن الذكاء الاصطناعي` |

الاختيار بأول كلمة من الأمر (عربي أو لاتيني)، وبلا نمطٍ صريح يُعتمد «بطاقة مراجعة».

## 4) حدود الاستخدام والخصوصية

- **حدود مستقلة عن الدردشة:** فاصل 20 ثانية بين الصفحات، وسقف 8 صفحات يومياً لكل مستخدم — كل صفحة 2 إلى 4 نداءات مزوّد (أثقل بكثير من رسالة دردشة).
- **صفر تخزين:** الطلب يسافر للمزوّد ثم يُنسى؛ الصفحة تُسلَّم ملفاً في المحادثة ولا تلمس قاعدة البيانات إطلاقاً (الوحدة تستورد `providers` فقط — قاعدة bot-api النقية).
- **تسليم صادق:** التذييل يصرّح بوضوح: هل اجتازت الصفحة النقد الخماسي من الجولة الأولى، أم بعد جولة تحسين، أم أن النقد لم يتاح هذه المرة.

## 5) حدود المالك الأمنية (قاعدة مالك لا ميزة جولة)

حرس محلي يعمل **قبل أي مزوّد وقبل احتساب أي حصة**:
- أي طلب صفحة لالتقاط/جمع/إرسال كلمات مرور أو بيانات بطاقات أو مفاتيح → رفض فوري برسالة تشرح الحدود وتدعو لطلب مادة دراسية بديلة.
- أي طلب صفحة تصيّد أو تسجيل دخول مزيف → رفض نهائي، مع اقتراح بديل تعليمي (مثلاً: صفحة تشرح كيف يكتشف الطالب مواقع التصيّد).
- الحرس عبارات عربية طبيعية شائعة (تجمع/ترسل/حفظ + كلمات المرور/بيانات الدخول/المفاتيح…) — فُحصت بأمثلة واقعية.

## 6) تعديلات المزوّدين (providers.ts)

`chatComplete` يقبل الآن خيارات اختيارية `ChatCompleteOptions { maxTokens?, temperature? }` للنداءات الخاصة (توليد الصفحات يحتاج سقف مخرجات 8192 بدل 2048، والناقد حرارة أثبت 0.2). الافتراضيات هي قيم المحادثة نفسها — **سلوك قائم لا يتغير إطلاقاً** (المساعد، المصنّف، البوت كلها على الافتراضي).

## 7) البنية والملفات

| الملف | الدور |
|---|---|
| `src/lib/ai/html-studio.ts` (جديد) | الأنماط، محلّل الأمر، الحرس الأمني، بناة الممرّين، استخراج HTML، تحليل حكم الناقد، المنسّق، نصوص المساعدة والتذييل |
| `src/lib/ai/providers.ts` | خيارات `maxTokens/temperature` الاختيارية لـ `chatComplete` |
| `src/lib/telegram/bot-api.ts` (جديد) | `sendDocumentWith` — رفع multipart إلى sendDocument بنفس عهدة الوحدة: لا رمي، مهلة 30ث، بلا تسريب أخطاء |
| `src/lib/telegram/bot-chat.ts` | أمر `/html` + `handleHtmlCommand` + حدود الاستوديو + تحديث الترحيب و/help |
| `scripts/r85-check.ts` (جديد) | 38 فحصاً ببُنٍّ معقّم (bun + fetch مُعترَض) |

## 8) الفحص — 38/38

- **المحلّل:** فارغ/مسافات → مساعدة؛ الافتراضي؛ أسماء الأنماط عربي/لاتيني؛ الوصف القصير والطويل؛ كلمة النمط وحدها.
- **الحرس الأمني:** حصاد كلمات المرور رفض؛ تصيّد رفض؛ طلب دراسي بريء يمر.
- **الاستخراج:** مستند عادي، داخل أسوار كود، تالف، قزم، غير صفحة.
- **الناقد:** JSON سليم، JSON مدفون، بضاعة فاسدة، تقييد الدرجات وتطبيع الحكم.
- **خط الأنابيب:** PASS من أول جولة (نداءان)؛ REFINE ثم PASS (4 نداءات مع ملاحظات الناقد في نداء التحسين)؛ مولّد يعيد غير HTML → خطأ عربي صادق.
- **الربط بالبوت:** بلا مفاتيح → رسالة صادقة بلا أي نداء مزوّد؛ `/html` وحدها → مساعدة بلا كلفة؛ تشغيل سليم → sendDocument باسم `.html` وتذييل يصرّح بالنمط والخصوصية.
- **انحدارات:** r81 (13/13) — سلوك البوت القائم سليم.

## 9) ملاحظات للتشغيل

- الحدود في الذاكرة لكل نسخة خادم (نفس منهج حدود الدردشة) — تقريبية بحكمها الطبيعي.
- الصفحة تحتاج اتصالاً عند الفتح لتحميل Tailwind CDN — هذا مذكور في تذييل التسليم.
- أوامر البوت عند تيليجرام لاتينية حصراً، لكن `/html` تصل نصاً من أي واجهة — والبوت يفهمها من أول كلمة.

## 10) خطوات قادمة ممكنة (r86+)

توسيع عائلة الأدوات المستلهمة من منصات التوليد داخل البوت: مخططات Mermaid، استوديو ترجمة أكاديمي، محلّل لغة عربي، كاشف كتابة آلية، ومراجع كود — كلها بنمط الوحدة النقية نفسها (ممرّ واحد، حدود مستقلة، صفر تخزين).
