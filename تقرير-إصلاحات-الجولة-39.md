# تقرير إصلاحات الجولة 39 — أزرار حقيقية داخل المقياس

**الطلب الأصلي (مترجَم):** «ماذا عن المقاييس المتوفرة؟ فيها مواد ودروس؛ اجعل المواد مراجع لا مجرد كلمات، وأضف الأزرار اللازمة. لا يوجد زر واحد داخل المقياس — لا أقصد خارجه، بل داخله حيث الدروس والمذكرات والاختبارات والواجبات.»

**التشخيص:** شاشة تفاصيل المقياس (`course-detail-screen.tsx`) كانت تعرض المحتوى كبطاقات ساكنة:
- **الدروس:** زرّ أيقونة شبح فقط (Ghost icon) لا يلاحظه أحد، بلا معاينة للصور وبلا حجم الملف.
- **المواد:** زر تنزيل/فتح فقط عند وجود رابط — والمرجع بلا رابط يظهر «كلمات ميتة» بلا أي إجراء أو حتى إشارة صادقة.
- **الاختبارات:** بطاقات ساكنة تماماً — صفر أزرار.
- **الواجبات:** بطاقات ساكنة تماماً — صفر أزرار، والوصف مقصوص عند 3 أسطر بلا سبيل لقراءته كاملاً.

## ما نُفِّذ (ملف واحد رئيسي + إصلاح رفق في الجدول)

### ١. الدروس (منشورات تيليجرام المرتبطة بالمقياس)
- **معاينة مصغّرة** للمنشورات الصورية عبر وكيل `/api/telegram/file` (نمط شاشة دروس تيليجرام نفسه، مع fallback آمن عند فشل التحميل).
- **زر «فتح» ظاهر** بحدود وعنوان لكل منشور، بدل الأيقونة الشبح.
- عرض **حجم الملف** منسّقاً عند توفره.

### ٢. المواد (مراجع المكتبة المرتبطة بالمقياس)
- **العنوان نفسه صار مرجعاً**: نقرته يفتح الملف مباشرة (بلون primary وتسطير عند التمرير) — «مراجع لا كلمات» حرفياً.
- **زر «نسخ الرابط»** بجانب كل مادة (clipboard + toast).
- المرجع **بلا رابط** يُظهر شارة صادقة «بدون رابط» بدل الادعاء بأنه قابل للفتح.
- **للمشرفين:** تعديل المادة (حوار كامل: عنوان/مؤلف/صيغة/تصنيف/رابط/وصف — نفس مسار PATCH /api/library في شاشة المكتبة) وحذفها بحوار تأكيد.

### ٣. الاختبارات
- **«أضف إلى جدولي»** لكل مستخدم: يضيف الاختبار إلى الجدول الشخصي كحصة من نوع «امتحان» في يوم تاريخه من الأسبوع (الأحد=١ … السبت=٧)، مع القاعة والوقت، والتاريخ الكامل في الملاحظات. الزر يتحول إلى «أُضيف إلى جدولي» بعلامة خضراء.
- **للمشرفين:** تعديل الاختبار (عنوان/تاريخ/وقت/قاعة/معامل — PATCH /api/exams) وحذفه بحوار تأكيد — قوة كانت محصورة في شاشة الاختبارات المنفصلة.

### ٤. الواجبات
- **تبديل الإنجاز** (checkbox): يخزَّن بنفس مفتاح `talib-assignments-completed` الذي تستخدمه شاشة الواجبات — إنجاز الواجب من داخل المقياس يظهر منجزاً هناك أيضاً (حالة لكل جهاز، كالسلوك القائم).
- **حوار «التفاصيل»**: الوصف الكامل بلا قصّ + العلامة القصوى + تاريخ التسليم منسّق عربياً.
- **زر «تبليغ»** عن خطأ في الواجب (نفس مسار /api/issues في شاشة الواجبات).
- **للمشرفين:** تعديل (عنوان/تسليم/وصف — PATCH /api/assignments) وحذف بحوار تأكيد.

### ٥. إصلاحات رفق
- **شريط معلومات المقياس:** عمود رابع «المواد» بعد المعامل/الدروس/الاختبارات.
- **شاشة الجدول:** بطاقتا «الجمعة» و«السبت» تظهران **فقط عند وجود حصص فيهما** — قبلها كان أي امتحان يقع يوم سبت يُضاف للجدول الشخصي ثم يختفي عن العرض (خريطة الأيام كانت ١–٥ فقط). أُضيف مفتاحا i18n `schedule.friday/saturday` للغتين.
- تواريخ الاختبارات والواجبات داخل المقياس تُعرض الآن بالعربية (`ar-DZ`) بدل ISO الخام.

## التحقق (كامل على SQLite محلي ثم بناء إنتاجي)
- tsc: صفر أخطاء. eslint: نظيف على الملفين المعدّلين. `next build`: أخضر (الصفحات العامة ساكنة كما هي).
- **جولة متصفح حقيقية 390×844 بحسابين متوازيين:**
  - **مالك:** الدروس ← زر «فتح» ظاهر؛ المواد ← عنوان-رابط + فتح + نسخ + تعديل (حوار كامل الحقول، حفظ فعلي بتوست «تم تعديل المادة») + حذف؛ الاختبارات ← «أضف إلى جدولي» (توست + صف ظهر فعلياً في الجدول الشخصي: يوم ٧=السبت، القاعة، النوع «امتحان») + تعديل + حذف (حوار تأكيد → حذف فعلي تأكد عبر API)؛ الواجبات ← تبديل إنجاز (توست + خط يشطب العنوان) + تفاصيل (النص الكامل بثلاثة أسطره) + تبليغ + تعديل/حذف.
  - **طالب:** نفس الأقسام بلا أي زر إدارة (لا تعديل ولا حذف ولا إضافة) — فتح/تنزيل/نسخ/تفاصيل/تبديل/تبليغ/أضف إلى جدولي فقط؛ أضاف الامتحان إلى **جدوله هو** (صف userId منفصل).
  - **الجدول:** بطاقة «السبت» تظهر للمالك والطالب بحصة الامتحان الشخصية المُعلَّمة «شخصية».
  - `scrollWidth = 390` في الجلستين (لا فائض أفقي)، صفر أخطاء console.
- **تنظيف:** حُذفت حسابا التجربة (owner39/student39) وكل محتواهما (مقياس + امتحانان + واجب + مادة + درس + حصصا جدول شخصيتان) — القاعدة: users 0، وكل جداول المحتوى 0، وهيكل القباس سليم (2/2/10/6/20). السكربت: `scripts/r39-seed.ts`.

## ملاحظات
- زر «أُضيف إلى جدولي» حالة لكل فتح صفحة (per-mount) وليس محفوظاً — نفس أسلوب شاشة الجدول في عدم التحقق المسبق من التكرار؛ الجدول الشخصي يقبل التكرار ويمكن حذف الصف من هناك.
- معاينة الصور في الدروس تعمل حين يكون منشور تيليجرام صورةً وبه `file_id` (وكيل البوت)؛ محلياً لا توكن بوت فظهر fallback الأيقونة — نفس نمط شاشة تيليجرام المجرَّب.
