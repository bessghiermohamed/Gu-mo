# تقرير الجولة 57 — ثلاث ملاحظات من المالك

**النطاق:** ملاحظات المالك بعد معاينة الجولة 56 على الإنتاج.

---

## 1) بطاقة «وصف المقياس» لا تظهر إلا عند وجود وصف

**المشكلة:** «المقاييس التي لا وصف لها لا تحتاج بطاقة الوصف» — كل مقياس بلا
وصف كان يعرض بطاقة كاملة بنص بديل «لا يوجد وصف متاح لهذا المقياس بعد —
يمكن للمشرفين إضافته لاحقاً»، فتمتلئ صفحة المقياس ببطاقات فارغة بلا قيمة.

**ما نُفّذ (course-detail-screen.tsx):**
- البطاقة تُعرض **فقط** عندما يحتوي المقياس على وصف فعلي
  (`description?.trim()`) — بلا وصف، لا تُرسم أي بطاقة أصلاً (لا العنوان
  ولا النص البديل).
- الوصف الحقيقي صار يُعرض بـ `whitespace-pre-wrap` — الأسطر المتعددة التي
  يكتبها المشرف تحترم فواصلها بدل أن تُدمج في سطر واحد.

**التحقق:** متصفح حقيقي — مقياس «البلاغة» (بلا وصف): لا بطاقة ولا نص بديل؛
مقياس «النحو والتطبيق» (بوصف): البطاقة تُعرض بنصها. لقطتا
`download/r57/course-no-desc-card.png` و `course-with-desc-card.png`.

---

## 2) إزالة بانر المساعد الذكي من الواجهة الرئيسية

**المشكلة:** «أزل بانر المساعد الذكي من الواجهة» — البطاقة البنفسجية التي
أضافتها الجولة 56 أعلى الرئيسية كانت تأخذ حيّزاً بارزاً من شاشة تخصّ
الخدمات الأكاديمية.

**ما نُفّذ (home-screen.tsx):**
- البطاقة البنفسجية بشرائحها («لخّص لي درساً / اشرح ببساطة / اختبرني»)
  وزر «اسأل الآن» أُزيلت بالكامل من الرئيسية.
- الرئيسية تعود متمركزة على: الترحيب، الإحصاءات (المعدل/المقاييس/حصص
  اليوم)، ثم شبكة «الخدمات الأكاديمية».
- **المساعد يبقى متاحاً** من مكانه الطبيعي: بطاقته المميزة أول أداة في
  «أدواتي» (بمعاينة فقاعات المحادثة وزر «ابدأ محادثة») — مسار واحد بلا
  ازدواجية.

**التحقق:** متصفح حقيقي — نص «المساعد الذكي» غائب عن الرئيسية تماماً
(0 بطاقات) في الفاتح والداكن؛ من «أدواتي» تفتح المحادثة بلمسة واحدة.
لقطات `download/r57/home-no-ai-banner.png` و
`home-no-ai-banner-dark.png` و `tools-ai-featured.png` و
`ai-chat-opens.png`.

---

## 3) تنظيف جسر الانتقال الميت من الجولة 56

**المشكلة:** حذف البانر خلّف كوداً ميتاً — جسري الانتقال العميق اللذان
بُنيا له (`talib-open-ai` عبر sessionStorage + حدث window، و
`talib-ai-prefill` لتعبئة المحرّر مسبقاً) لم يعد لهما مُشغِّل.

**ما نُفّذ (tools-tab.tsx + ai-assistant-tool.tsx):**
- مستمع الحدث وقراءة sessionStorage في «أدواتي» حُذفا — البطاقة المميزة
  تفتح المساعد مباشرة عبر `setActiveTool("ai")` بلا وسيط.
- تأثير التعبئة المسبقة في المحادثة حُذف — **المحرّك يفتح فارغاً دائماً**
  (كانت الشرائح المحذوفة تملؤه بقوالب طويلة).
- `sessionStorage` نظيف بعد الجلسة: لا مفاتيح متبقية (`BRIDGE KEYS CLEAN`).

**التحقق:** فتح المحادثة من «أدواتي» — المحرّر فارغ وجاهز، ولا آثار للمفاتيح
القديمة.

---

## فحوصات الجودة

| الفحص | النتيجة |
|---|---|
| `tsc --noEmit` | صفر أخطاء |
| `eslint` على الملفات الأربعة | صفر أخطاء/تحذيرات |
| `next build` | ✓ 69/69 صفحة |
| المتصفح | 7 لقطات في `download/r57/` — كلها مطابقة |

**صافي التغيير:** 4 ملفات، ‎+21 / ‎−126 سطراً (الجولة حذف أكثر مما أضاف —
إزالة ميزة + إخفاء بطاقة).

---

## الرفع والنشر

- **الجولة 56 كانت محفوظة محلياً فقط** في نهاية جلستها السابقة (فشل الدفع
  لغياب رمز GitHub) — الفحص اليوم أكّد أن مُرسى `origin/main` أصبح
  `5961052` (كيّا ر56 كاملَين على GitHub) وأن **بناء r56 منشور فعلاً على
  الإنتاج** (نصوصها موجودة في الحُزم المُقدَّمة من gu-mo.vercel.app).
- **r57**: التزم (`3ce8c22`) ودُفع إلى GitHub بنجاح
  (`5961052..3ce8c22 main → main`) — **ونُشر على الإنتاج وتَمّ التأكد منه**:
  حالة GitHub للالتزام تقول «Deployment has completed» (Vercel، بعد
  دقيقتين من الدفع)، وفحص الحُزم المُقدَّمة فعلياً من gu-mo.vercel.app
  يُظهر: نص البانر المحذوف («قبل الامتحان») غائب، مفتاح جسر
  `talib-ai-prefill` غائب، وبطاقة المساعد في أدواتي («ابدأ محادثة» /
  «ويجيب أسئلتك») حاضرة — أي أن الزائر الحقيقي يرى r57 الآن.
  (ملاحظة منهجية: مقارنة أسماء الحُزم المذكورة في HTML وحدها مُضلِّلة —
  شاشات SPA تُحمَّل عبر حُزم وقت التشغيل لا تظهر في HTML.)
- r57 **لا يحتاج أي إعداد جديد من المالك**: لا متغيرات بيئة جديدة؛ كل
  مفاتيح r56 (VAPID الخاص وغيرها) تبقى كما هي.

---

## ما لم يتغيّر (عمداً)

- بطاقة المساعد المميزة داخل «أدواتي» كما هي من r56 (المعاينة + «ابدأ
  محادثة») — لأنها مدخل المساعد الوحيد الآن.
- إشعارات Web Push وربط التبليغات بصاحبها (r56) لم تُمَس.
- بيانات البذور المحلية (r52 + r55) أعيدت لتشغيل التحقق فقط، ولا علاقة
  لها بالإنتاج.
