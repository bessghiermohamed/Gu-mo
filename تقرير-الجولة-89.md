# تقرير الجولة 89 — «استوديو المولّدات»: خدمات التوليد تنتقل من البوت إلى التطبيق

**التاريخ:** 17 سبتمبر 2026
**الحالة:** منجزة — فحص r89 84/84، انحدارات r88 34/34 وr87 97/97 وr86 92/92 وr85 38/38 وr81 31/31، tsc نظيف، eslint نظيف، `next build` ✓.

---

## 1) الخلاصة التنفيذية

سؤال المالك بعد جولتي r85–r87 كان حاسماً: «رأيتَ قدرات تلك المنصات — أفلا تستطيع نسخها؟» ولماذا لم يجد الخدمات التي أشار إليها في التطبيق. الإجابة في هذه الجولة: **«استوديو المولّدات»** — بطاقة مميزة ثالثة في «أدواتي» بهوية سماوية/نيلية تجلب إلى الويب ما كان حصراً في بوت تيليجرام، وتضيف قدرتين جديدتين مستلهمتين من كتالوج المنصات: **بحث موسّع** و**قارن النماذج**.

| الأداة | المصدر | ماذا تفعل |
|---|---|---|
| **بحث موسّع** (جديدة) | كتالوج المنصات: Deep Research | خطّان: مخطط محاور (٣–٦) ثم تقرير مفصّل — بأمانة معلنة: من معرفة النموذج بتاريخ قطعه، لا يتصفح الإنترنت، ويُلزَم بقسمَي «ما يجب التحقق منه» و«مصادر مقترحة للتوثيق» بلا روابط مخترعة |
| **قارن النماذج** (جديدة) | كتالوج المنصات: Model Arena | سؤالك يجيب عنه مزوّدان مختلفان بالتوازي (`chatWithProvider` جديد في providers) — بطاقتان جنباً إلى جنب بأسماء صادقة، والحكم للطالب بلا فائز تلقائي |
| مخطط Mermaid | بوت r86 | ٨ أنواع + فحص بنوي + تصحيح واحد + معاينة mermaid.live |
| ترجمة أكاديمية | بوت r86 | اكتشاف تلقائي عربي↔فرنسي + ١١ لغة |
| تحليل عربي | بوت r86 | تصحيح/تشكيل/إعراب/صرف/معانٍ في خمسة أقسام |
| كشف كتابة آلية | بوت r86 | سقف 85٪، بلا إشارتين لا حكم، تنبيه لا دليل |
| مراجعة كود | بوت r86 | ما يفعله، مشاكل بلا اختلاق، نسخة محسّنة عند الاستحقاق |
| صفحة HTML | بوت r85 | ٤ أنماط، خط توليد–نقد ذو ممرّين، معاينة داخلية + تنزيل |

**حدود النسخ الصادقة (بطلب المالك يستحق الوضوح):** قدرات الصور/الفيديو/الموسيقى/الكوميكس/ليب-سينك في تلك المنصات تعتمد مزوّدين توليديين متخصصين لا تتوفر لهم مفاتيح على المنصة — وr83 علّمت أن أي ميزة على مزوّد غير متاح تُبنى على رمل. تُبنى هذه الجولة كاملة على سلسلة النصوص المجرَّبة (Groq ← Gemini ← Grok).

## 2) مبادئ البناء

- **إعادة استخدام لا نسخ:** الستّ أدوات تستدعي نفس ممرات البوت (`runDiagramStudio`… `runHtmlStudio`) حرفياً — أي إصلاح مستقبلي في الوحدات النقية يصل البوت والويب معاً.
- **`chatWithProvider` (providers.ts):** نداء لمزوّد محدّد بالاسم عبر استخراج مُساعد مشترك `callAttempt` من `chatComplete` — لا يتغير سلوك السلسلة إطلاقاً، والقارن يستدعي مزوّدين اثنين بـ`Promise.allSettled` مع صراحة أخطاء كل ساق.
- **الحصص: خمس برك مستقلة** بنفس حدود البوت (بحث 20ث/10، قارن 15ث/20، مخطط 15ث/12، html 20ث/8، خفيفة 10ث/24) — عبر ليمتر الدفتر المُجرَّب (97/97) بحقن ساعة للاختبار.
- **الحرس الأمني قبل أي مزوّد وأي حصة** — نفس منطق البوت حرفياً: مخطط `studyGuard`، html `credentialsGuard`، مراجعة `studyGuard(code+note)`.
- **أمانة البحث المعمّرة:** مدقق التقرير يرفض غياب «المقدمة» أو «ما يجب التحقق منه» أو «مصادر مقترحة» أو الطول تحت ١٢٠٠ حرف؛ وملاحظة الأمانة تُعرض بجانب النتيجة لا في سطر منسي.
- **صفر تخزين في الويب:** نتيجة الاستوديو تعيش في الذاكرة حتى مغادرة الشاشة — لا localStorage ولا صفوف قاعدة.

## 3) تفاصيل تقنية لطيفة

- **مُحلّل مخطط متسامح متدرج:** ينزع طبقات الترقيم/الرموز المتكدسة («1. - عنوان») ثلاث مرات، ويسقط تمهيد النموذج («إليك مخططاً مقترحاً:») الذي لا يحمل سؤالاً فرعياً — وإن قلّت المحاور عن ٣ جاء فشل عربي صادق.
- **قارن بلا إجبار:** إن فشل مزوّد وحيد يظهر خطأه وحده والآخر مكتمل؛ وإن لم يتوفر إلا مزوّد واحد جاءت رسالة تسمّيه بالاسم بدل تجربة وهمية.
- **معاينة HTML آمنة:** `iframe srcDoc` بـ`sandbox="allow-same-origin"` بلا سكربتات (قيد المولّد أصلاً) + تنزيل ملف + نسخ.
- **واجهة متسقة:** رقائق أنواع المخطط (٨ + تلقائي)، رقائق أنماط الصفحة (٤)، قائمة لغات الترجمة (١١ + تلقائي)، عدّاد أحرف بحدود كل أداة، وشاشة needsConfig بعُرف r44 تطمّن: «إن كان الدفتر يعمل فهذا يعمل».

## 4) الفحص — 84/84 (+ انحدارات 34/34 و97/97 و92/92 و38/38 و31/31)

- **r89-check (جديد):** ثمانية أقسام — الكتالوج، التحقق من الطلب (قبول/رفض)، الحرس (أمثلة حصاد/تصيّد نفسها في فحوص البوت)، البحث (المُحلّل المتدرج والمدقق)، القارن (اختيار المزوّدين ورسالة الوحيد)، البرك الخمس بساعة محقونة، عقد الشبكة (٨ أفعال + الحرس قبل الحصص)، وعقد الواجهة (البطاقة، البحث، صفر التخزين، الدفتر باقٍ).
- **tsc نظيف، eslint نظيف، `next build` ✓** مع تسجيل `/api/ai/studio` و`/api/ai/notebook` معاً و84/84 صفحة.

## 5) الملفات

| الملف | العملية |
|---|---|
| `src/lib/ai/studio.ts` | **جديد** (~430 سطراً) — كتالوج ٨ أدوات + تحقق + حرس + بحث + قارن + برك |
| `src/app/api/ai/studio/route.ts` | **جديد** — النقطة الواحدة للأدوات الثماني |
| `src/lib/ai/providers.ts` | `callAttempt` مشترك + `chatWithProvider` — بلا تغيير سلوك |
| `src/components/talib/tools/studio-tool.tsx` | **جديد** (~775 سطراً) — الاستوديو الكامل RTL |
| `src/components/talib/tools/tools-tab.tsx` | البطاقة المميزة الثالثة + توجيه + بحث |
| `scripts/r89-check.ts` | **جديد** — 84 فحصاً |

## 6) حدود معروفة (صادقة)

- «بحث موسّع» لا يتصفح الإنترنت — هذه ليست عيباً خفياً بل هو القسم الثالث من مخرجاته؛ نسخة بحث حي تنتظر فحصاً حياً لدعم Gemini لرسوّ جوجل (مرشّح r90 مع طلب المالك).
- القارن يستهلك حصة مزوّدين اثنين في كل سؤال — حدوده (٢٠ يومياً) أخفض من الدردشة لهذا السبب.
- استوديو HTML أبطأ الأدوات (توليد + نقد + تحسين حتى 52ث) — حدّه الأدنى (٨ يومياً) ويُعرض زمنه المتوقع في الواجهة.
- أدوات الصور/الفيديو/الموسيقى من كتالوج تلك المنصات غير قابلة للنسخ على المفاتيح الحالية — راجع §1.

## 7) خطوات قادمة ممكنة (r90+)

1. **البحث الحي:** فحص حي لدعم Google Search grounding في ساق Gemini ثم نسخة «بحث مع مصادر حية» خلف الممرّ والحدود نفسها.
2. **دفتر + استوديو متكاملان:** زر «ابحث عن موضوع مصادري» من الدفتر إلى البحث الموسّع.
3. **الكتاب الصوتي الطويل** (audiobook) عبر speechSynthesis بفصول — صفر مزوّد صوت.
4. توليد الصور فور توفّر مفتاح صور (المقترح المعلّق منذ r87).
