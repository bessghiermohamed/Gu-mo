# تقرير الجولة 58 — حالة عدم الاتصال عند الدخول بلا إنترنت

**النطاق:** طلب المالك: «إظهار حالة عدم الاتصال عند تسجيل الدخول بلا إنترنت» —
قبل هذا، فشل الدخول بلا شبكة كان يظهر خطأً عاماً («خطأ في الاتصال بالخادم»)
بعد انتظار لا معنى له، دون أي إشارة لمَ السبب.

---

## 1) لافتة حالة مباشرة على شاشة الدخول

**ما نُفّذ (login-screen.tsx):**
- **أداة `useOnlineStatus`**: تتبع `navigator.onLine` مع مستمعي حدثي
  `online`/`offline` — الحالة تتغير لحظياً دون إعادة تحميل. تبدأ `true`
  ثم تتبنى الحالة الحقيقية بعد الإقلاع (تجنّب تعارض الترطيب بين الخادم
  والمتصفح).
- **لافتة تحذيرية كهرمانية** تظهر بين شريط التبويب (دخول/إنشاء حساب)
  والنموذج لحظة انقطاع الاتصال:
  - نقطة نابضة («حالة حيّة») + أيقونة `WifiOff` + عنوان **«أنت غير متصل
    بالإنترنت»** وسطر ثانٍ يشرح ماذا يعني ذلك عملياً: «التحقق من بياناتك
    يحتاج اتصالاً — تحقق من شبكتك ثم أعد المحاولة.»
  - انسياب دخول/خروج بحركة (AnimatePresence) مع `role="status"` و
    `aria-live="polite"` — قارئ الشاشة يعلن التغيير.
  - الكهرماني = تحذير وليس خطأً قاتلاً: بياناتك المكتوبة آمنة بانتظار
    الاتصال (الفاتح/الداكن كلاهما مدعوم).

## 2) حماية زر الدخول من محاولة محكومة بالفشل

- الضغط على «دخول/إنشاء الحساب» أثناء الانقطاع → **رسالة فورية محددة**:
  «لا يمكن الدخول الآن: لا يوجد اتصال بالإنترنت.» — بلا دوران تحميل بلا
  هدف وبلا انتظار (الطلب لا يُرسل أصلاً).
- حالة «متصل ظاهرياً» لكن الشبكة ميتة (بوابة ملتقطة): الطلب يُرسل ويفشل،
  والمزوّد يصنّفه — انظر البند التالي.

## 3) تصنيف أدق في مزوّد المصادقة (auth-provider.tsx)

- مسارا `signIn`/`signUp`: عند فشل الشبكة، إن كان المتصفح يعلم أنه غير
  متصل → رسالة عدم الاتصال الجديدة؛ وإلا → الرسالة العامة («تحقق من
  الإنترنت») كالسابق.
- **الخروج أثناء الانقطاع صار يعمل**: `signOut` كان يرمي استثناءً غير
  معالج فلا يحدث شيء — الآن يُغلف بـ try/catch ويسجّل الخروج محلياً،
  فيهبط الطالب إلى شاشة الدخول **ومعه لافتة عدم الاتصال** (السكشنة تنتهي
  لوحدها عند أول طلب عبر الشبكة).

## 4) الترجمة

مفاتيح جديدة في ar/en معاً: `offlineTitle`، `offlineHint`، `errorOffline`،
`backOnline` — الواجهة كاملة بالعربية والإنجليزية.

---

## التحقق (متصفح حقيقي مع محاكاة انقطاع فعلية)

| السيناريو | النتيجة |
|---|---|
| فتح شاشة الدخول ثم قطع الاتصال | اللافتة تظهر فوراً بالنص الكامل |
| الضغط على «دخول» أثناء الانقطاع | توست «لا يمكن الدخول الآن…» والزر يعود دون تعليق |
| عودة الاتصال | اللافتة تنسحب بحركة + توست «عاد الاتصال بالإنترنت» |
| دخول طبيعي بعد استعادة الاتصال | نجاح — يصل إلى الرئيسية (اختبار انحدار) |
| خروج أثناء الانقطاع (الإعدادات) | يهبط إلى شاشة الدخول ولافتة الانقطاع ظاهرة |
| لقطات | 5 في `download/r58/` (فاتح/داكن/سطح المكتب/توست/خروج) |

**فحوصات:** tsc صفر · eslint صفر على الملفات المغيّرة · build ✓ 69/69.

---

## ما لم يتغيّر (عمداً)

- **لا وضع أوفلاين داخل التطبيق**: الطلب عن *حالة الدخول*؛ التطبيق نفسه
  يحتاج الشبكة لبياناته (أدواتي除外 — أدوات PDF تعمل محلياً أصلاً).
- بقاء الجلسة عند انقطاع مفاجئ: بيانات الشاشة المحمّلة سابقاً تبقى معروضة
  (لا يطردك الانقطاع من التطبيق) — تغيير ذلك قرار تصميمي آخر.
- ترجمة `continueAsGuest` القديمة غير مستعملة منذ زمن وبقيت كما هي.
