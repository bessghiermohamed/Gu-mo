# تقرير إصلاحات الجولة 27 — الحصص الشخصية في الجدول + الجولة التعريفية + تخفيف الصناديق + تنظيف تقني

**التاريخ:** 2026-09-04 · **النطاق:** بنود §7 و§15 من وثيقة المراجعة الشاملة + طلبا المالك (تقليل الصناديق، التنظيف)

## الخلاصة

أربع جبهات مستقلة، كل واحدة في **كومِت منفصل** قابل للمراجعة والحجز على حدة:

| الجزء | الكومِت | الموضوع |
|---|---|---|
| F | `a3dcf0f` | الحصص الشخصية في الجدول (مراجعة §7) |
| G | `a8072e3` | الجولة التعريفية الأولى (مراجعة §15) |
| H | `c00eadf` | تقليل الصناديق في الواجهة الرئيسية |
| I | `ede6a2e` + `ef78128` + `c20ad46` | التنظيف التقني |

---

## الجزء F — الحصص الشخصية (§7)

الطالب يستطيع الآن أن يبني **جدوله الشخصي** بنفسه: إضافة/تعديل/حذف حصصه الخاصة (اليوم، وقت البداية والنهاية، اسم المقياس، النوع محاضرة/TD/TP، المكان، ملاحظات اختيارية).

- **جدول مستقل `PersonalScheduleItem`** — بيانات شخصية لكل مستخدم، منفصلة تماماً عن `ScheduleItem` (الجدول الرسمي للتخصص الذي يديره المشرفون) حتى لا يختلط الشخصي بالرسمي أبداً — كما تشترط المراجعة حرفياً.
- **API جديد `/api/schedule/personal`** (GET/POST/PATCH/DELETE) بحراسة مزدوجة: كل استعلام مقيّد بـ `userId` للاتصال، وكل تعديل/حذف يتحقق من الملكية قبل التنفيذ. لا صلاحية مطلوبة سوى تسجيل الدخول.
- **العرض المدموج** في تبويب «يدوي»: الحصص الرسمية أولاً بنمطها المعتاد، تليها الحصص الشخصية بلون كهرماني وشارة **«شخصية»** مع سطر الملاحظات أيقونة ورقة ملاحظات — وسطر توضيحي ثابت أسفل الأزرار: «الحصص المُظلّلة بالبرتقالي هي حصصك الشخصية — تظهر لك فقط ولا تُخل بالجدول الرسمي.»
- **الأزرار:** الطالب العادي يرى زراً واحداً «حصة شخصية»؛ المشرف يرى زرين متجاورين «حصة رسمية» (كما كان) و«حصة شخصية». حذف الشخصية عبر نافذة تأكيد مثل الرسمية.
- **للإنتاج (Supabase):** ملف `download/supabase_personal_schedule.sql` جاهز للتشغيل مرة واحدة في محرر SQL — جدول جديد بالكامل، لا يمسّ أي جدول قائم، والتطبيق يتعامل مع غيابه برشاقة (قائمة فارغة) قبل ترحيله.
- تعديل الحصة الشخصية يفتح حواراً معبّأً مسبقاً بنفس الحقول، ويوضّح الحوار دائماً أن الحصة «تظهر لك فقط».

## الجزء G — الجولة التعريفية (§15)

جولة من **ثلاث خطوات** لكل مستخدم جديد (وبعد إتمام الإعداد لا قبله):

1. **شبكة «الخدمات الأكاديمية»** — كل شيء على بعد لمسة واحدة.
2. **أيقونة الترس** — الإعدادات: الإشعارات والمظهر.
3. **تبويب «حسابي»** — المعلومات الشخصية والتبليغ وتسجيل الخروج.

- **آلية البحث الضوئي:** حلقة ملوّنة فوق العنصر المستهدف مع تعتيم بقية الشاشة عبر `box-shadow` ضخم — بلا مكتبات ولا canvas.
- **مرة واحدة لكل مستخدم** (علم `talib-tour-<userId>` في localStorage — جولة هي مصدر قلق واجهة لا بيانات أكاديمية)، وقابلة للتخطي (**«تخطّي»**) من كل خطوة، ولا تحجب المحتوى إلا أثناء عرضها.
- تتحرك مع التمرير وتغيّر المقاس (مستمعو `scroll/resize`)، وتضع بطاقة الشرح في المساحة الأرحب فوق العنصر أو تحته، مع نقاط تقدّم وزر أخير «يلا نبدأ».

## الجزء H — تقليل الصناديق في الواجهة

الشاشة الرئيسية كانت تصل إلى ~12 صندوقاً مُحدَّذاً. الآن:

- **شبكة الخدمات الثمانية:** من بطاقات `Card` بحدود وظل إلى **بلاطات مسطّحة** `bg-muted/40` بلا حدود ولا ظل — نفس البنية (أيقونة + عنوان + وصف) ونفس شبكة عمودين.
- **آخر الإعلانات:** بطاقة لكل إعلان → **قائمة مسطّحة بفواصل رفيعة** `divide-y`، وحالات التحميل/الخطأ/الفراغ نصوص عادية بدل صناديق منقّطة.
- **لافتتا الانضمام و«لم تنضم إلى فوج»** وبطاقة **دروس تيليجرام** وتلميح الإعداد: حدود مُزالة، خلفية ملوّنة شفافة `rounded-2xl` مع تدرج hover هادئ بدل الظل والانزلاق.
- النتيجة: الصناديق المحدَّسة في الرئيسية من ~12 إلى **صفر** (صورة الهيرو وحدها تحتفظ بشكلها)، و`home-screen.tsx` لم يعد يستورد `Card` أصلاً.

## الجزء I — التنظيف التقني

- **إصلاح أخطاء TypeScript الثلاثة المكتومة منذ جولات سابقة:**
  - `announcements/mark-read`: إسقاط حيلتي `as never` و`skipDuplicates` غير المدعوم على SQLite — إزالة التكرار تتم في الكود أصلاً عبر مجموعة `have` مع قيد `@@unique` كصمّام أمان.
  - `telegram/setup`: `select("id", { head: true, limit: 1 })` غير صالح → `select("id", { head: true }).limit(1)`.
  - `scripts/verify-review2.ts`: تمرير `role` كـ `UserRole` بتحويل صريح في موضع الاستدعاء.
- **إزالة `ignoreBuildErrors`** من `next.config.ts` — أخطاء الأنواع تُفشل البناء من الآن، فلا يختبئ خلل جديد خلف الكرت. (مجلد `skills/` الخاص بالمنصة استُثني من tsconfig لأنه ليس جزءاً من التطبيق.)
- `tsc --noEmit` نظيف **صفر أخطاء** لأول مرة، والبناء الإنتاجي ينجح بفحص الأنواع مفعّلاً.
- **تنظيف حسابات الاختبار:** `scripts/cleanup-test-accounts.ts` يحذف حسابَي اختبار الجولتين 26 و27 — نُفّذ، وقاعدة التطوير الآن بلا مستخدمين.

## التحقق (متصفح حقيقي 390×844 + SQLite محلي)

- تسجيل مستخدم جديد عبر الواجهة فعلياً → إكمال الإعداد الست خطوات → ظهور الجولة تلقائياً في الرئيسية: الخطوات الثلاث كلها صحيحة (البحث الضوئي على الشبكة، ثم الترس، ثم تبويب حسابي) → «يلا نبدأ» → علم `talib-tour-2: done` محفوظ → **إعادة تحميل لا تُعيد الجولة**.
- الجدول: إضافة حصة شخصية (الاثنين 08:00–09:30، TD، B12، ملاحظات) → ظهرت بالشارة الكهرمانية وسطر الملاحظات → تعديل القاعة إلى C20 → حُفظ → حذف بتأكيد → اختفت. الطالب يرى «حصة شخصية» فقط، والمشرف يرى الزرين (اختبر بتقليب الدور في قاعدة البيانات كما في الجولة 26).
- الوضع الليلي: الحصة الشخصية الكهرمانية واضحة ومقروءة. لا قصّ ولا تداخل في 390px (`scrollWidth = 390` بلا تمرير أفقي) في الرئيسية والجدول، ولا أخطاء Console.
- لقطات التحقق في `download/verify-390/r27-*.png`.

## ملاحظات للمالك

- قبل نشر الإنتاج: شغّل `download/supabase_personal_schedule.sql` في محرر SQL الخاص بـ Supabase — حتى ذلك الحين يعمل كل شيء عدا الحصص الشخصية (قائمة فارغة رشاقةً).
- أرقام الأيام الشخصية تقبل 1–7 في الـ API (نفس عقيدة الجدول الرسمي) والواجهة تعرض أيام الدراسة الخمسة فقط.
