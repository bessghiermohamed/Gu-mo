# تقرير إصلاحات الجولة الثانية — بسيطة اللغة

هذا التقرير مكتوب بلغة بسيطة لغير المبرمجين، حسب قواعد العمل المعتمدة. يمكن عرضه على مراجع تقني للتحقق من التفاصيل.

---

## ماذا تم إصلاحه؟ (مطابق لتقرير الاختبار الذي أرسلته)

### أولاً: التسريب الخطير في نطاق الرؤية (أ.3 + أ.4) — الأخطر، وأُصلح أولاً

**المشكلة:** المقررات والجدول والمجموعات والأفواج كانت تظهر للجميع بغض النظر عن التخصص والسنة والملمح.

**السبب الحقيقي (تم تأكيده في الكود):**
1. استعلامات جلب المقررات والجدول كانت تفلتر بالتخصص فقط، ولا تفلتر بالسنة إطلاقاً.
2. زر إضافة مقياس في لوحة الإشراف كان يحفظ المقياس دائماً في "السنة الأولى" مهما اخترت.
3. شاشة اختيار المقررات كانت تخمّن السداسي من اسم الكود (مثلاً يبحث عن "S1" في النص) بدل قراءة حقل السداسي الحقيقي من قاعدة البيانات.
4. عند التسجيل، كانت بيانات الملمح والمؤسسة التي اخترتها لا تُحفظ أصلاً (الكود كان يتجاهلها).
5. شاشة "تصفح المجموعات" كانت تجلب كل مجموعات التخصص بلا فلترة بالسنة — لهذا طالب السنة الخامسة رأى أفواج السنة الأولى.

**الإصلاح:** كل الاستعلامات الآن تفلتر بـ (التخصص + السنة) للطالب، والسداسي صار حقلاً حقيقياً يُقرأ من قاعدة البيانات، وبيانات الملمح والمؤسسة تُحفظ عند التسجيل.

**اختبار القبول (نفس اختبارك المطلوب):** أنشأنا طالبين في بيئة اختبار: طالب في (تخصص أ، سنة 1) وطالب في (تخصص ب، سنة 3). النتيجة: **كل طالب يرى مقرراته فقط — صفر تداخل.** ✅

### ثانياً: إضافة تخصص وملمح لمؤسسة جديدة (أ.1 + أ.2)

**المشكلة:** بعد إضافة مؤسسة جديدة، لا توجد طريقة لإضافة تخصص لها، ولا ملمح للتخصص الجديد.

**الإصلاح:**
- نموذج "إضافة تخصص" صار يعرض قائمة منسدلة بأسماء المؤسسات (بدل خانة رقم يدوية مربكة)، وقائمة التخصصات تعرض المؤسسة التابع لها كل تخصص.
- تبويب جديد بالكامل اسمه **"الملامح"** في لوحة الإشراف: تختار المؤسسة والتخصص، ثم تضيف الملامح بثلاثة أزرار سريعة جاهزة (ابتدائي PEP / متوسط PEM / ثانوي PES) مع خيار "ملمح آخر" للاسم المخصص — تماماً كما اقترح البحث في تقريرك.

**اختبار القبول:** أنشأنا مؤسسة جديدة "ملحقة سوقر" ثم تخصصاً لها ثم ملمح PEP — كلها نجحت ✅

### ثالثاً: اختفاء تبويب "قبول طلبات الانضمام" (أ.5)

**السبب (تم التحقق منه):** ليس تراجعاً في المنطق، بل مشكلة عرض: التبويبات الثمانية كانت محشورة في 4 أعمدة على شاشة الهاتف، فكان تبويب "الطلبات" يخرج من الشاشة ويبدو مختفياً.

**الإصلاح:** التبويبات صارت تلتف لأسطر متعددة على الهاتف، فكل التبويبات التسعة مرئية دائماً. أيضاً اكتشفنا وأصلحنا خللاً إضافياً: زر "دور" (لترقية مستخدم إلى ممثل أو مشرف) كان يستدعي نقطة نهاية غير موجودة أصلاً (خطأ 404) — أنشأناها الآن وعملت بنجاح في الاختبار ✅

### رابعاً: تعشيش المجموعة/الفوج (ب)

**المشكلة:** الفوج كان يُنشأ بلا مجموعة أم، والمجموعة بلا مؤسسة.

**الإصلاح:** نموذج إنشاء الفوج صار يطلب بالترتيب: المؤسسة ← التخصص ← الملمح ← السنة ← **المجموعة الأم** ← اسم الفوج، ولا يقبل إنشاء فوج بلا مجموعة. ونموذج المجموعة صار يطلب المؤسسة والتخصص والسنة أيضاً.

### خامساً: الأزرار المفقودة (ج)

| الشاشة | ما أُضيف |
|---|---|
| **الاختبارات** | شاشة كاملة جديدة: تعرض الاختبارات القادمة/المنتهية من قاعدة البيانات + زر إضافة اختبار (مقياس، عنوان، تاريخ، وقت، قاعة، معامل) + حذف — للمشرفين فقط |
| **الإعلانات** | زر "+ إعلان" بعنوان ومحتوى ودرجة أهمية (عام/هام/عاجل) — للمشرفين فقط، والطالب يحاول النشر يُرفض (403) |
| **الملفات** | تبويب "المكتبة" الجديد: ملفات ومراجع التخصص (كتب، ملخصات، روابط PDF) + زر "إضافة ملف" — للمشرفين فقط |

### سادساً: تنظيم قائمة المستخدمين (د)

القائمة صارت مجمعة في قوائم قابلة للطي: **المؤسسة ← التخصص ← المستخدمون**، مع عدّاد لكل مجموعة، وإظهار سنة الطالب. البحث يعمل عبر كل المجموعات.

### أخطاء إضافية اكتشفناها وأصلحناها (لم تكن في تقريرك)

1. **ملفات الطلاب:** كل مستخدم كان يكتب فوق "ملف الطالب" نفسه (صف واحد مشترك رقم 1) — الآن لكل مستخدم ملفه الخاص.
2. **التسجيل الجديد كان مربوطاً بـ"التخصص رقم 1" بشكل ثابت** — لو تغيرت أرقام التخصصات في قاعدة البيانات يتعطل التسجيل كلياً. الآن يُختار أول تخصص حقيقي موجود.
3. **~30 خطأ برمجياً مخفياً** (كانت البنية تتجاهلها وقت البناء) — أُصلحت كلها، والبنية الآن تفحص الكود بدقة كاملة قبل النشر.
4. **جداول ناقصة في قاعدة البيانات:** كود "طلبات الانضمام" و"المجموعات" كان يستدعي جداول غير موجودة في ملفات تهيئة قاعدة البيانات — أُضيفت كلها.

---

## خطوة مطلوبة منك (مرة واحدة): تحديث قاعدة بيانات Supabase

بعض الجداول والأعمدة الجديدة غير موجودة بعد في قاعدة بياناتك الحقيقية. الملف الجاهز موجود في المشروع:

**`download/supabase_round2_fixes.sql`**

الطريقة: افتح موقع Supabase ← مشروعك ← SQL Editor ← New query ← الصق محتوى الملف ← Run.
السكربت آمن للتشغيل حتى لو بعض الجداول موجودة مسبقاً، ويُنشئ تلقائياً مجموعة افتراضية لكل (تخصص/سنة/ملمح) ويربط الأفواج الحالية بها حتى لا تختبي بياناتك القديمة.

⚠️ **رتبة التنفيذ:** شغّل السكربت بعد نشر التحديث (أو قبله بقليل — لا مشكلة).

---

## نتائج الاختبار الآلي

كُتبت مجموعة اختبارات قبول آلية (31 اختباراً) تحاكي سيناريو اختبارك الحقيقي: تسجيل مالك + طالبين في تخصصين وسنتين مختلفتين، مع فحص كل نقطة أعلاه.

**النتيجة النهائية: 31/31 ناجحة ✅** (ملف الاختبار: `scripts/acceptance-test.sh` — يمكن إعادة تشغيله مستقبلاً بعد أي تعديل للتأكد أن شيئاً لم يتعطل)

## ما لم يُنفذ بعد (من تقريرك)

- **هـ (تحسينات التصميم):** تحتاج تفاصيل أدق منك (ما الذي ينقص "حاسبة الطالب" تحديداً؟ ما الهوية البصرية المطلوبة للبنر؟) قبل التنفيذ.
