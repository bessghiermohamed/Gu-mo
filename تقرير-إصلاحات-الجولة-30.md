# تقرير الجولة 30 — إضافات Vercel الأصلية: تحليلات الويب + مؤشرات السرعة

**التاريخ:** ٥ سبتمبر ٢٠٢٦ · **النوع:** إضافات Marketplace (أصلية) · **الحالة:** مكتملة

---

## ١. ما أُضيف ولماذا

بهدف دعم مراجعة AdSense ومراقبة صحة الموقع بعد الإطلاق، أُضيفت إضافتان
أصيلتان من Marketplace الخاص بـ Vercel عبر الكود (لا تتطلبان أي إعداد):

| الإضافة | الحزمة | الوظيفة |
| --- | --- | --- |
| **Web Analytics** | `@vercel/analytics@2.0.1` | إحصاءات الجمهور: الزوار، الصفحات الأكثر زيارة، المصادر (Referrers) — دليل حقيقي على حركة المرور أثناء مراجعة AdSense |
| **Speed Insights** | `@vercel/speed-insights@2.0.0` | مؤشرات Core Web Vitals الفعلية من متصفحات الزوار (LCP/INP/CLS) — إشارة «تجربة الصفحة» التي تهم Google |

كلتاهما مجانيتان على خطة Hobby، تعملان على حافة Vercel، ولا تؤثران على
الأداء (سكربتات مؤجلة لا تحجب العرض).

## ٢. التغييرات التقنية

- `package.json` + `bun.lock`: إضافة الحزمتين أعلاه.
- `src/app/layout.tsx`: تركيب `<Analytics />` و`<SpeedInsights />` في
  الجذر (بعد سكربت AdSense) — فيسريان تلقائياً على كل الصفحات: صفحات
  المحتوى العام والمسار المحمي `/app` على حد سواء، دون أي كود إضافي.

## ٣. التحقق

- `tsc --noEmit` = 0 أخطاء.
- `next build` أخضر — كل صفحات المحتوى العام بقيت ثابتة التوليد (SSG)
  والمسار `/app` سليم.
- لا تأثير على AdSense: سكربت الإعلانات ووسم الملكية و`ads.txt` كما هي.

## ٤. خطوة واحدة متبقية للمالك

بعد اكتمال نشر Vercel: افتح لوحة Vercel ← مشروع `gu-mo` ← تبويب
**Analytics** ← فعّل **Web Analytics** (زر واحد، مجاني). عندها يبدأ
جمع البيانات فوراً. (Speed Insights يبدأ تلقائياً بعد النشر.)

## ٥. إضافات Marketplace خارجية (اختيارية — تتطلب نقرة موافقة من المتصفح)

لا يمكن تثبيت إضافات الغير من خلال التوكن البرمجي — Vercel يشترط موافقة
متصفح. روابط التثبيت بنقرة واحدة حسب الأولوية للمشروع:

- **Sentry** (مراقبة الأخطاء، مجاني): `vercel.com/marketplace/sentry`
- **Axiom** (سجلات الطلبات + رؤية زيارات Googlebot): `vercel.com/marketplace/dash0` أو Axiom من السوق
- **Resend** (بريد إشعارات، ٣ آلاف رسالة شهرياً مجاناً): `vercel.com/marketplace/resend`

قاعدة البيانات: لا حاجة لإضافة قاعدة بيانات من السوق — المشروع يستخدم
Supabase + Prisma أصلاً.
