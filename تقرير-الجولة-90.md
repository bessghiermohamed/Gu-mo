# تقرير الجولة 90 — بوابة «التصريحات النزيهة» في AdSense

**التاريخ:** 17 سبتمبر 2026
**الحالة:** منجزة — فحص r90 53/53، انحدارات r89 84/84 وr88 34/34 وr87 97/97 وr86 92/92 وr85 38/38 وr81 31/31، tsc نظيف، `next build` ✓، ملفات الجولة الستة بلا أي ملاحظة eslint (خط الأساس العام موثّق أدناه بصدق).

---

## 1) الخلاصة التنفيذية

راسلت Google AdSense المالك بإشعار سياسة **«التصريحات غير النزيهة»** (Honest declarations ضمن Misrepresentation): المعلومات التي يقدّمها الناشر لتفعيل أنظمة إعلانات Google يجب أن تكون **دقيقة وكاملة ماديًا** بلا إغفالات مضلّلة، وأمثلة الإشعار: معلومات/دفعات ناقصة، **ملف ads.txt غير دقيق**، و**«بيانات غير مكتملة أو غير دقيقة بشكل مخادع في طلبات عرض الإعلانات، مثل عناوين URL جزئية أو غير دقيقة»**.

شُخِّصت المنصة كاملة ثم أُصلحت الثغرة التقنية الوحيدة الموجودة في الكود:

- **السليم (بأدلة حية):** `ads.txt` يُقدَّم من الجذر بمحتوى صحيح حرفياً (`google.com, pub-8081529487869617, DIRECT, f08c47fec0942fa0` — HTTP 200)، ووسم الملكية `google-adsense-account` يطابق معرّف الناشر، وrobots.txt يمنع `/app` و`/api` و`/ads-test`، وصفحات المحتوى العامة حية.
- **الثغرة الحقيقية:** سكربت `adsbygoogle.js` كان يُحمَّل **بشكل غير مشروط** في `layout.tsx` — أي أن **كل رابط نشر على Vercel** (`gu-mo-<hash>.vercel.app` لكل نشر، ومعاينات كل فرع) كان يرسل طلبات إعلانات باسم الناشر من مضيفات **لم تُعلَن في حساب AdSense أبداً**. هذا حرفياً مثال الإشعار الثالث: «عناوين URL جزئية أو غير دقيقة في طلبات عرض الإعلانات».

**الإصلاح (r90):** بوابة مضيفين صارمة — الإعلانات (السكربت والحاويات معاً) لا تعمل إلا على المضيف المعلَن في الحساب (مشتق من `SITE_URL`)، مع قائمة بيضاء اختيارية للمالك، وlocalhost للتشخيص. روابط المعاينة والنشر المؤقتة **لا ترى أي آلية إعلانية إطلاقاً**.

## 2) ما تغيّر — الملفات

| الملف | التغيير |
|---|---|
| `src/lib/ads.ts` | +`parseExtraAdHosts` (مُحلّل `NEXT_PUBLIC_ADSENSE_EXTRA_HOSTS` مع تطبيع الحالة وإزالة `www.`)، +`ADS_PRIMARY_HOST` (مضيف `SITE_URL` الرسمي)، +`ADS_ALLOWED_HOSTS`، +`isAdsAllowedHost` بمطابقة **تامة** (ترفض `www.` والنطاق الفرعي الزائف واللاحقة المخادعة) |
| `src/components/ads/use-ads-host-allowed.ts` | **جديد** — خطّاف عميل على `useSyncExternalStore`: لقطة الخادم `false` (توافق HTML التام بلا أي hydration mismatch) ولقطة العميل من `window.location.hostname` — **بلا أي كتابة حالة في التأثيرات**، والاشتراك no-op صادق لأن مضيف الصفحة لا يتغير في عمرها |
| `src/components/ads/adsense-gate.tsx` | **جديد** — المكان الوحيد الذي يحق حقن `adsbygoogle.js` منه؛ يعيد `null` على المضيفين غير المعلَنين |
| `src/app/layout.tsx` | إزالة السكربت غير المشروط واستيراد `next/script`، وتركيب `<AdsenseGate />` — ووسم الملكية **يبقى في كل المواقع عمداً** (تصريح ملكية لا طلب إعلانات) |
| `src/components/ads/ad-slot.tsx` | حارس المضيف: إرجاع مبكر `null` بعد كل استدعاءات الخُطّافات (ترتيبها ثابت) — روابط المعاينة لا ترى حاوية ولا رقاقة «إعلان» ولا مساحة محجوزة |
| `scripts/r90-check.ts` | **جديد** — 53 فحصاً (§4) |

**الامتداد المستقبلي بنقرة:** عندما يشتري المالك نطاقاً مملوكاً ويضيفه إلى Vercel، يكفي وضع `NEXT_PUBLIC_SITE_URL=https://النطاق` وإعادة النشر — البوابة تتبدّل تلقائياً إلى النطاق الجديد (و`gu-mo.vercel.app` يتوقف عن طلب الإعلانات تلقائياً)، أو يضاف مضيف إضافي عمداً عبر `NEXT_PUBLIC_ADSENSE_EXTRA_HOSTS`.

## 3) لماذا `useSyncExternalStore` وليس useState+useEffect

أثناء البناء اكتشف eslint قاعدة `react-hooks/set-state-in-effect` على المسودة الأولى للخطّاف (setState متزامن داخل تأثير = رندرات متتالية)، واكتُشف أيضاً خطأ أعمق: إرجاع مبكر في `AdSlot` كان **قبل** `useEffect` — كسر لقواعد الخُطّافات (انهيار عند انقلاب الحالة). صُحِّح الاثنان: الإرجاع المبكر بعد كل الخُطّافات، والخطّاف نفسه أُعيد كتابته على `useSyncExternalStore` — النمط القياسي لقراءة قيمة خارجية بلقطة خادم، فلا setState أصلاً ولا انفصال hydration (الخادم وتمرير التوافق يريان `null`، ثم قراءة واحدة بعد الترطيب تقلب المسموح فقط).

## 4) فحص r90 (53/53)

- **[A] بوابة المضيفين (15):** المضيف الأساسي من `SITE_URL`، localhost/127.0.0.1 للتشخيص، بلا تكرار، قبول المضيف الرسمي وتطبيع الحالة والهوامش، ورفض: `www.`، نطاق فرعي مزيف (`evil.gu-mo.vercel.app`)، لاحقة مخادعة (`gu-mo.vercel.app.evil.io`)، استبدال شرطة، نطاق غريب، سلسلة فارغة.
- **[A+] المُحلّل (8):** فراغ/فاصلة وحيدة → `[]`، مضيف واحد، عدة مضيفين بفراغات، تطبيع حالة وإزالة `www.`، إزالة التكرار عبر Set.
- **[B] التوصيل (5):** `<AdsenseGate />` في layout، **لا أثر لـ pagead2 في layout**، لا `next/script` في layout، وسم الملكية باقٍ من `ADSENSE_CLIENT`.
- **[C] البوابة (9):** مكوّن عميل، Script مشروط خلف `useAdsHostAllowed`، `null` عند المنع، id/src/strategy صحيحة ومبنية من الثابت.
- **[D] الخُطّاف (5):** `useSyncExternalStore` بلا أي `useState`/`setState`، لقطة خادم `false`.
- **[E] AdSlot (4):** الخُطّاف مستخدم، الإرجاع المبكر **بعد** `useEffect` وبلا خطّافات بعده، `data-ad-client` من الثابت كما هو.
- **[F] التصريحات (7):** `ads.txt` بالمحتوى الصحيح حرفياً، وست مساحات إعلانية باقية في صفحاتها.

**الانحدارات:** r89 84/84، r88 34/34، r87 97/97 (الجرد الأخضر)، r86 92/92، r85 38/38، r81 31/31. **tsc** 0 أخطاء. **`next build`** ✓ — كل الصفحات كما كانت (○ ثابتة/● SSG/ƒ ديناميكية).

**خط أساس eslint بصدق:** `bun run lint` (أي `eslint .`) كان يفشل **قبل** هذه الجولة بـ7 أخطاء `no-require-imports` في سكربتات قديمة (`r60-ai-proxy.js`، `r61-offline-test.js`، `r61-prod-smoke.js`، `r63-e2e-local.ts`) و8 تحذيرات — وُثّق بالتجربة على الشجرة النظيفة (`git stash`). ملفات r90 الستة تمرّ بلا أي خطأ أو تحذير. (اقتراح مستقبلي: استبعاد `scripts/` من `eslint .` أو ترحيل تلك السكربتات إلى ESM.)

## 5) خطوات المالك خارج الكود (لا بديل عنها في الكود)

الإشعار يشمل أمثلة لا يمكن لأي كود إصلاحها — هذه قائمة الفعل الشخصي في حساب AdSense:

1. **الدفع والهوية (المثال الأول في الإشعار):** `المدفوعات ← التحقق من الهوية` إتماماً، و**رمز PIN** البريدي للجزائر قد يتأخر أسابيع — تأكد من صحة عنوان الدفع قبل طلب إعادة إرسال. ملف دفع ناقص = تصريح غير نزيه في نظر Google مهما كان الكود مثالياً.
2. **قائمة المواقع:** تأكد أن الموقع المعلَن هو **`gu-mo.vercel.app` حرفياً** (بلا `www`، بلا مسار، بلا خطأ إملائي) واحذف أي مدخلات قديمة أو مكررة أو مواقع لا تملكها — عدم تطابق المعلَن مع ما تراه Google في الطلبات هو مولّد هذا الإشعار الأول.
3. **اطلب إعادة المراجعة** من مركز السياسات بعد نشر r90 والتحقق من البندين 1–2 — الإشعار عادة مهلة قبل توقف خدمة الإعلانات؛ لا تؤجّل.
4. **لا حركة مدفوعة أو مصطنعة إطلاقاً** — اشتراء زيارات من أصل نطاق مجاني من أشهر مخالب هذا التصنيف.
5. **النطاق المملوك (توصية استراتيجية):** `vercel.app` نطاق مشترك تتبادل عشرات الآلاف مواقع؛ نطاق مملوك (مثل `talib-dz.com`) يقوّي التصريحات وثبات المراجعة ويُنهي هذا الصنف من المشاكل جذرياً — وبوابة r90 تجعل الانتقال تلقائياً (§2).

## 6) التحقق الحي قبل النشر

- `https://gu-mo.vercel.app/ads.txt` → 200 بالمحتوى الصحيح.
- الصفحة الرئيسية → 200، ووسم الملكية حاضر.
- بعد نشر r90: على النطاق الرسمي تعمل الإعلانات كالمعتاد (وفي متصفح المالك تظهر الوحدات ممتلئة)، وعلى أي رابط `gu-mo-<hash>.vercel.app` تجريبي لا يظهر أي أثر إعلاني إطلاقاً — هذا هو السلوك المقصود الذي يصحح التصريحات.

---

**الخلاصة:** التصريحات المقدَّمة لـ Google صارت دقيقة كالمادة: منشور واحد، مضيف معلَن واحد، وملف ads.txt مطابق حرفياً — وما تبقى من الإشعار (الدفع/الهوية/قائمة المواقع) قرارات حساب لا كود، موثّقة خطوة بخطوة في §5.
