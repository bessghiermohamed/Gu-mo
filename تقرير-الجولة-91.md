# تقرير الجولة 91 — إزالة «استوديو المولّدات» بطلب المالك

**التاريخ:** 17 سبتمبر 2026
**الحالة:** منجزة — فحص r91 35/35، انحدارات r90 53/53 وr88 34/34 وr87 97/97 وr86 92/92 وr85 38/38 وr81 31/31، tsc نظيف، `next build` ✓ (سطح API: `/api/ai/notebook` وحيدة)، ملفات الجولة الثلاثة بلا أي ملاحظة eslint.

---

## 1) الخلاصة التنفيذية

طلب المالك حرفياً: **«Delete Generator Studio»** — فأُزيل «استوديو المولّدات» (r89) إزالةً جذرياً بنفس منهجية r88 (المساعد الذكي) وr84 (استوديو الصور): لا تعطيل ولا إخفاء، بل حذف كامل مع فحص بنيوي يمنع عودته بصمت، والإبقاء التام على ما بناه r85/r86/r87.

| أُزيل (r89) | بقي كما هو |
|---|---|
| `src/lib/ai/studio.ts` (397 سطراً — كتالوج الأدوات الثماني وحرسه وبرك حصصه) | **أدوات البوت الست** (r85/r86): `html-studio.ts` + `study-tools.ts` بممراتها الحية في بوت تيليجرام |
| `src/app/api/ai/studio/route.ts` (292 سطراً) | **دفتر طالب** (r87): الوحدة النقية + `/api/ai/notebook` + المكوّن + بطاقته المميزة |
| `src/components/talib/tools/studio-tool.tsx` (775 سطراً) | **سلسلة المزوّدين** (Groq←Gemini←Grok) بسلوكها الحرفي |
| البطاقة المميزة الثالثة (سماوي/نيلي) ومدخل البحث وهوية `"studio"` | **بوابة r90 الإعلانية** (تأمين عابر للجولات في الفحص) |
| `chatWithProvider` في providers (أصبحت ميتة — لا مستعمل لها بعد الحذف؛ `callAttempt` المشتركة بقيت لأن `chatComplete` تستعملها) | |
| `scripts/r89-check.ts` و`تقرير-الجولة-89.md` (عُرف r84: فحوص الجولة المزالة وتقريرها يُرفعون معها — العمل محفوظ في git: `c0d51b0`) | |

**«أدواتي» بعد الجولة:** بطاقة مميزة واحدة (دفتر طالب، زمردية) + شبكة ٩ أدوات محلية/على الجهاز — كما كانت تماماً بعد r88.

## 2) ملاحظات الصدق

- **لماذا حُذف `chatWithProvider`؟** أضافها r89 حصراً لخدمة «قارن النماذج»؛ بعد الحذف لم يعد لها أي مستعمل في src/، وعُرف r88 هو حذف الكود الميت لا تركه يتقادم. الوظيفة المشتركة `callAttempt` (التي استُخرجت في r89 من `chatComplete`) بقيت — لأن السلسلة الأم تستعملها فعلاً.
- **لا أثر بيانات:** الاستوديو كان «صفر تخزين» من تصميم r89 — لا مفاتيح localStorage ولا جداول ولا مخلفات، فلا حاجة لتعديل مسح بيانات الجهاز في settings.
- **globals.css بلا أثر:** r89 لم يضف شيئاً لملف الأنماط (تحقّق من diff الجولة)، فلا تنظيف مطلوب.
- **البوت لم يُمسّ:** «استوديو» في ملفات البوت تعني «استوديو HTML» (r85) — فحص r91 يميّز بين الاثنين نمطياً: النمط الممنوع `lib/ai/studio` لا يطابق `lib/ai/html-studio`.

## 3) فحص r91 (35/35)

- **[A] إزالة بنيوية:** مسح 213 ملفاً في src/ بـ11 نمطاً ممنوعاً (`lib/ai/studio"`، `api/ai/studio`، `studio-tool`، «استوديو المولّدات»، `STUDIO_`، `chatWithProvider`، `matchesStudio`، `key="studio"`، `setActiveTool("studio")`، `"studio"`…) بقائمة بيضاء لتعليق تاريخ r91 في tools-tab — صفر إشارات حية.
- **[A+] الحذف الفعلي:** الملفات الخمسة المزالة غائبة من الشجرة.
- **[B] الباقي:** الممرات الست للأدوات المولّدة حية (runHtmlStudio/runDiagramStudio/runTranslateStudio/runAnalyzeStudio/runDetectStudio/runReviewStudio)، البوت موصول بهما ولا يعرف الاستوديو المحذوف، الدفتر باكياً بوحدته ومساره ومكوّنه، providers بلا `chatWithProvider` و`callAttempt`/`chatComplete`/`buildAttempts` كما هي.
- **[C] tools-tab:** لا هوية studio ولا Wand2 ولا matchesStudio، `noResults` مطبّع، توجيه الدفتر باقٍ، ٩ أدوات شبكية، بطاقة مميزة واحدة (فتحا `<motion.button` = 2: البطاقة + قالب الشبكة) وهي الدفتر.
- **[D] سطح API:** `/api/ai/notebook` مسجّل، `src/app/api/ai/studio` غائب (تأكّد `next build` أيضاً).
- **[E] تأمين r90:** `ads.txt` بالمحتوى الحرفي، `<AdsenseGate />` في layout بلا pagead2، `isAdsAllowedHost`/`ADS_PRIMARY_HOST` في lib/ads، وفحص r90 نفسه باقٍ.

**خلال البناء:** اصطدم معيارا فحص بافتراضاتي الخاطئة (أسماء تصدير r86 الفعلية `run*Studio`، وعدّاد `<motion.button` الذي يلتقط وسوم الإغلاق) — صُحّح الفحص ليطابق الواقع لا العكس.

## 4) الخطوات بعد النشر

- على الإنتاج: `/api/ai/studio` يعيد 404، وبطاقة «أدواتي» بلا الاستوديو، والدفتر وأدوات البوت كما هي.
- سجل git يحفظ r89 كاملاً (`c0d51b0`) — استرجاعه مستقبلاً ممكن بجولة بناء على عُرف r83→r84 الموثق.

---

**الخلاصة:** منصة طالب عادت لسطحها المتفق عليه: دراسة محلية خصوصية أولاً + دفتر طالب مُسنَد + بوت ذو أدوات ستّ — بلا استوديو توليد في الويب، وبفحص يضمن أنه لن يعود إلا بقرار معلن.
