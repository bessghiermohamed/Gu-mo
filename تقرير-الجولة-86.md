# تقرير الجولة 86 — حزمة الأدوات الدراسية في بوت طالب (مستلهمة من خدمات منصات التوليد)

**التاريخ:** 16 سبتمبر 2026
**الحالة:** منجزة — فحص 92/92، تحقّق أنواعي (tsc) نظيف، فحص لين (eslint) نظيف، `next build` ✓.

---

## 1) الخلاصة التنفيذية

بطلب المالك — الذي شارك لقطات لخدمات منصة Alborihi AI وقال «هذه خدمات إضافات رائعة» — تضيف هذه الجولة **خمس أدوات دراسية** إلى المحادثة الخاصة مع بوت طالب، مختارة من الخدمات التي تلائم منصة أكاديمية طلابية وتعمل على سلسلة المزوّدين النصية القائمة (Groq → Gemini → xAI) بلا أي مفتاح أو خدمة جديدة:

| الأمر | الخدمة المستلهمة منها | ماذا تفعل |
|---|---|---|
| `/مخطط` | Visualize · المخططات | تحويل فكرة إلى مخطط Mermaid (انسيابي، خريطة ذهنية، تسلسل، أصناف، كيانات، حالات، زمني، دائري) — يُسلَّم ملفاً `.mmd` بعد فحص صياغة، مع جولة تصحيح واحدة عند الكسر |
| `/ترجم` | استوديو الترجمة | ترجمة أكاديمية أمينة: اكتشاف تلقائي (عربي→فرنسي، غير عربي→عربي — السياق الجزائري) أو لغة هدف صريحة (11 لغة) |
| `/تحليل` | محلّل اللغة | تحليل نص عربي كامل: تصحيح، تشكيل علمي، إعراب، صرف (جذر/وزن/نوع)، معاني ومرادفات — خمسة أقسام ثابتة |
| `/كشف` | وكَلاء كشف الكتابة بالذكاء الاصطناعي | تقدير احتمالي **صادق** لنسبة الكتابة الآلية (سقف ٨٥٪ لا يقين أبداً) + إشارات لغوية ملموسة من النص + نصيحة — مع تنبيه صريح أنه تقدير لا دليل |
| `/مراجعة` | مراجعات Karpathy | مراجعة كود (أي لغة): ما يفعله، مشاكل حقيقية (0–4، بلا اختلاق)، تبسيط، نسخة محسّنة فقط إن استحق |

**ما لم يُنفَّذ عمداً:** توليد الصور (مولّد القصص/البطاقات) — تراجعت الجولة 83 لأن مفتاح Gemini الحالي بلا وصول للصور، والقاعدة صارت: لا ميزة تعتمد مزوّداً غير متاح. التعليق الصوتي (TTS) أُجل لجولة لاحقة إن توفر مزوّد صوت. الخدمات غير الأكاديمية (بناء وكلاء، محركات سير عمل، باحث ويب عميق) خارج نطاق منصة طلابية.

## 2) مبادئ البناء (استمرارية قاعدة r85)

- **وحدة نقية واحدة** `src/lib/ai/study-tools.ts`: تُستورد `providers` و`isHarvestOrPhish` فقط — تُختبر بـ bun خارج Next.js (قاعدة bot-api)، صفر تخزين: الطلب يسافر للمزوّد ثم يُنسى.
- **ممرّ واحد لكل طلب** (باستثناء المخطط: فحص + تصحيح واحد كحد أقصى — بلا حلقات أبداً).
- **حدود مستقلة** عن الدردشة وعن /html: المخطط 15ث/12 يومياً (يُنتج ملفاً)، والأدوات الخفيفة الأربع تجمع في بركة واحدة 10ث/24 يومياً (نداء واحد لكل طلب).
- **حرس المالك الأمني قبل أي مزوّد وقبل أي حصة** على أدوات التوليد (`studyGuard`): حصاد أسرار/تصيّد (إعادة استخدام منطق r85 عبر `isHarvestOrPhish`) + كود اختراق/برمجيات خبيثة — برسالة سياقية واحدة. أدوات التحليل (ترجم/تحليل/كشف) بلا حرس — تعالج نص المستخدم كما تعالجه الدردشة العامة.
- **صدق التسليم:** كاشف الكتابة يقول حرفياً «لا يُعد دليلاً قاطعاً»؛ مراجع الكود يكتب «لا مشاكل جوهرية ظاهرة» بدل اختلاق مشاكل؛ المخطط يصرّح بجولة التصحيح إن حدثت.

## 3) تفاصيل تقنية لطيفة

- **Mermaid تحت القيود:** عقد مخرجات صارم (ترويسة من 8 أنواع معروفة، تسميات عربية بين تنصيص، بلا روابط ولا click)، استخراج يبدأ من أول سطر ترويسة معروفة، تحقق بنّي (ترويسة/حجم/روابط/تفاعل) وجولة تصحيح واحدة تحمل رسالة الفحص للمزوّد.
- **اكتشاف اللغة:** نسبة الحروف العربية ≥ 30٪ → فرنسي؛ وإلا عربي. الصريح يسبق التلقائي دائماً.
- **كاشف صادق بحكم بنائي:** بلا إشارتين ملموستين لا حكم يُصدر (null → رسالة صادقة) — الظن لا يُشحن. السقف 85٪ يمنع «اليقين» الزائف حتى مع النصوص الأشد آلية.
- **`sendDocumentWith` يقبل mime اختيارياً** — المخطط يُرفع `text/plain` بدل HTML الافتراضي (تغيير غير مكسور).

## 4) الفحص — 92/92 (+ انحدارات 38/38 و31/31)

- **المخطط (16):** محلّل الأمر (أنماط عربي/لاتيني، تلقائي، أطوال)، استخراج (أسوار، نص+كود، بلا ترويسة)، تحقق (ترويسة، http، click، قزم).
- **خط المخطط (5):** سليم من أول جولة؛ مكسور ← تصحيح واحد يرسل ملاحظة الفحص ← corrected=true؛ فاسد مرتين ← خطأ عربي صادق.
- **الترجمة (13):** لغات صريحة (en/فرنسي)، تلقائي، نسبة العربية، ترتيب الأولويات، خط الأنابيب.
- **التحليل (7):** أطوال، الأقسام الخمسة تصل كما هي.
- **الكاشف (12):** أرضية 120 حرفاً، JSON سليم/فاسد/ناقص الإشارات، تقييد 93→85، اشتقاق الحكم من النسبة، الرسالة تحمل التنبيه، فشل صادق.
- **المراجعة (9):** أسوار + وصف، كود مجرد، حصاد/برمجيات خبيثة رفض، دراسة دفاعية بريئة تمر (بلا إنذار كاذب).
- **الربط بالبوت (30):** خمس مساعدات بلا كلفة، تسليم ملف .mmd بتذييله، ترجمة/تحليل/كشف/مراجعة سليمة، فشل الكاشف الصادق، حرس قبل المزوّد وبالحصص، بركة مشتركة (فاصل 10ث بين أدوات)، السقف اليومي 24، /help يسرد الكل، بلا مفاتيح ← صفر نداءات.

## 5) الملفات

| الملف | الدور |
|---|---|
| `src/lib/ai/study-tools.ts` (جديد) | الأدوات الخمس: محلّلات، حرس، بناة أوامر، مستخرِجات، منظّمات، نصوص مساعدة وأخطاء |
| `src/lib/telegram/bot-chat.ts` | خمسة معالجات أوامر + حدود مستقلة (استُخلص `limitCheck` المشترك) + توجيه الأوامر + الترحيب و/help + رافقة اختبار `__resetToolLimits` |
| `src/lib/telegram/bot-api.ts` | `sendDocumentWith` بوسيط mime اختياري |
| `src/lib/ai/html-studio.ts` | `isHarvestOrPhish` منطق الحرس معاد استخدامه بلا رسالة |
| `scripts/r86-check.ts` (جديد) | 92 فحصاً ببُنٍّ معقّم |

## 6) حدود معروفة (صادقة)

- الحدود في الذاكرة لكل نسخة خادم (نفس منهج الدردشة و/html) — تقريبية بحكمها.
- عرض المخطط يحتاج أداة تدعم Mermaid (mermaid.live، GitHub، Obsidian) — مذكور في تذييل التسليم.
- أوامر تيليجرام الرسمية لاتينية؛ الأوامر العربية تصل نصاً عادياً والبوت يفهمها من أول كلمة — الاختبار غطى المسارين.
- الكاشف يفحص ما يُلصق فيه فقط — لا قراءة ملفات في هذه الجولة.

## 7) خطوات قادمة ممكنة (r87+)

- واجهة ويب لنفس الأدوات داخل «أدواتي» في التطبيق (الوحدات النقية جاهزة لإعادة الاستخدام).
- التعليق الصوتي إن توفر مزوّد TTS مفتاحه.
- تصدير المخطط SVG داخل الخادم إن توفرت بيئة عرض.
