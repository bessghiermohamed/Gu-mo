# تقرير إصلاحات الجولة 32 — نشر محاضرات من Drive إلى المكتبة

**التاريخ:** 5 سبتمبر 2026
**الطلب المباشر من المالك:** «لماذا لا نستخدم حسابي على Drive (١٥ جيجابايت)؟ الأفضل أن يستطيع المشرف ربط حساب Drive به لرفع الملفات — بدل استهلاك مساحة Supabase التي تبلغ ١ جيجابايت فقط.»

---

## 1) الفكرة والمبرر

في الجولة 31 صار لكل طالب تبويب «سحابتي» يرفع ملفاته إلى Drive الخاصه — لكن هذا لا يخدم حالة «محاضرة أرفعها مرة ليحمّلها كل الطلبة». هذه الجولة تسدّ الفجوة بميزة **«نشر إلى المكتبة»**:

- **المشرف** (الممثل/الإدارة/المالك) يربط حساب Google Drive الخاص به مرة واحدة — نفس الربط الموجود في «سحابتي» تماماً، دون أي إعداد إضافي.
- عند «إضافة ملف» في المكتبة يختار وضع **«رفع ملف (Drive)»**: يختار ملف PDF/Word/PowerPoint/صورة من جهازه.
- الملف يُرفع **من المتصفح مباشرة إلى حساب Drive الخاص بالمشرف** داخل مجلد فرعي جديد «📚 مكتبة طالب» داخل مجلد «طالب — Talib»، مع شريط تقدّم حي.
- يُشارك الملف تلقائياً **«أي شخص لديه الرابط»**، ثم تُحفظ بيانات صغيرة (سطر واحد) في جدول المكتبة.
- **كل طلبة التخصص** يرون الملف في تبويب «المكتبة» مع زر **تنزيل** مباشر — **دون أن يحتاجوا لربط أي حساب**.

### لماذا هذا أفضل من تخزين الملفات في Supabase؟

| | تخزين Supabase | نشر عبر Drive (المعتمد) |
|---|---|---|
| المساحة | ١ ج.ب مشتركة (مع قاعدة البيانات) | **١٥ ج.ب من حساب المشرف** |
| ما الذي يُخزَّن لدينا؟ | الملف كاملاً | **سطر بيانات فقط (~٠.٤ ك.ب)** |
| حد حجم الرفع عبر Vercel | ٤.٥ م.ب لكل طلب | **بلا حد عملي** (رفع مباشر من المتصفح) |
| إعداد إضافي | إنشاء Bucket + صلاحيات | **صفر** — نفس ربط «سحابتي» |

النتيجة: **صفر بايت من ملفات المحاضرات يمر عبر Supabase أو خوادمنا** — قاعدة البيانات تنمو بضع مئات بايت لكل ملف فقط.

## 2) التغييرات التقنية

### أ. `src/lib/drive.ts` — ثلاث إضافات
- `findOrCreateLibraryFolder()`: إنشاء/إيجاد المجلد الفرعي «📚 مكتبة طالب» داخل مجلد التطبيق.
- `getDriveShareLinks()`: جلب `webContentLink` (رابط التنزيل المباشر) بعد المشاركة، مع بناء الرابط القياني كاحتياط.
- `uploadToDrive()` يقبل الآن وسم مصدر `sourceTag` — ملفات المكتبة تُوسم `talib-library` داخل Drive لتصفيتها لاحقاً.

### ب. `src/app/api/library/route.ts`
- POST يقبل حقلي `driveFileId` و `fileSize` (اختياريين). **تدرّج آمن:** إن لم يكن عمودا `storage_path` و `file_size` موجودَين بعد في جدول Supabase، يُدرَج السطر الأساسي بدون عقبة — النشر لا يفشل أبداً بسبب ذلك.
- GET يعيد الحقلين الجديدين إن وُجدا.
- Prisma: حقلان اختياريان `fileSize` و `storagePath` في `LibraryReference` (للمسار المحلي أيضاً).

### ج. `src/components/talib/screens/files-screen.tsx`
- حوار «إضافة ملف» أصبح بوضعين: **«رابط خارجي»** (كما كان تماماً) و **«رفع ملف (Drive)»**:
  - إن لم يكن `NEXT_PUBLIC_GOOGLE_CLIENT_ID` مضبوطاً: بطاقة تُحوَّل إلى دليل الإعداد في تبويب سحابتي (دون تكرار الخطوات السبع — مصدر واحد للحقيقة)، مع إمكانية العودة لوضع الرابط.
  - إن لم يكن متصلاً: زر ربط بنقرة واحدة (نفس نافذة موافقة Google).
  - عند اختيار الملف: يُملأ العنوان من اسم الملف تلقائياً وتُستنتج الصيغة (PDF/DOCX/PPTX/صورة)، مع بطاقة حجم الملف.
  - زر «نشر إلى المكتبة» يرفع مع نسبة تقدم ثم يشارك ويحفظ البيانات.
- بطاقات المكتبة تُظهر: شارة الحجم، وشارة **«على Drive»** للملفات المنشورة، وزر **«تنزيل»** بدل «فتح» لهذه الملفات.
- حذف عنصر منشور من Drive: يُحاول حذف النسخة من Drive أولاً (إن كان الناشر متصلاً وملفه هو) — أفضل جهد، ولا يعطّل حذف السطر أبداً.
- `formatBytes()` توحيد في `src/lib/utils.ts` واستُبدلت بها النسخة المحلية في drive-tab (تفادي التكرار).

## 3) التحقق

- `tsc --noEmit`: **0 أخطاء** (النسخة الحقيقية المضافة في الجولة 31).
- `next build`: أخضر — جميع الصفحات العامة ما تزال SSG.
- تحقق يدوي من التسلسل المنطقي: رفع → مشاركة anyone-with-link → webContentLink → سطر بيانات → ظهور للطلبة بزر تنزيل.

## 4) ما يحتاجه المالك (اختياري جداً)

1. **لتفعيل الرفع أصلاً:** مفتاح `NEXT_PUBLIC_GOOGLE_CLIENT_ID` (نفس خطوة الجولة 31 — الدليل داخل التطبيق في تبويب سحابتي). إن كنت أنجزتها سابقاً فلا شيء إضافي مطلوب.
2. **اختياري — لعرض حجم الملفات وشارة «على Drive» في القائمة:** الصق هذا في Supabase → SQL Editor (سطران):

```sql
ALTER TABLE library_references ADD COLUMN IF NOT EXISTS file_size BIGINT;
ALTER TABLE library_references ADD COLUMN IF NOT EXISTS storage_path TEXT;
```

بدون هذه الخطوة يعمل كل شيء أيضاً — فقط تُخفى الحجم والشارة.

## 5) الخلاصة

المشرف يربط حسابه مرة واحدة → يرفع المحاضرة من التطبيق → تصل لطلبة تخصصه كلهم بزر تنزيل — والمساحة المستهلكة هي الـ١٥ ج.ب المجانية في حسابه هو، وليست الـ١ ج.ب الخاصة بـ Supabase.
