# تقرير الجولة 94 — استعادة بنية Supabase بعد حذف المشروع

**التاريخ:** 2026-09-21
**الطلب:** «حذفت مشروع Supabase تابعاً لمشروع الطلبة بالخطأ. هل يُستعاد أو يُعاد بناؤه؟» + مشروع جديد جاهز: `talib app` (ref: `qgpzbeqhdidaojrlorpo`، منطقة `eu-west-1`).

---

## 1. التشخيص

- حذف المشروع القديم يعني أن `gu-mo.vercel.app` فقد قاعدته: كل مسارات `/api` كانت ستفشل (المصادقة، الدروس، الإشعارات، الفوج…).
- فحص التكامل في المستودع أظهر أن التطبيق يعتمد على Supabase بالكامل لكن بشكل يُعيد البناء ممكناً بلا عقبات:
  - **المصادقة مخصّصة بالكامل** (جدول `app_users` + `device_sessions` + كوكي `talib_session`) — **لا يستخدم Supabase Auth إطلاقاً**، إذن لا يحتاج المشروع الجديد أي إعداد مزوّدي (Google أو غيره) في لوحة Supabase.
  - **لا يوجد استخدام لـ Supabase Storage** (الرفع يعتمد روابط Google Drive وأعمدة قاعدة البيانات).
  - البوتات (محرك المجتمع) عديمة الحالة — ذاكرة دافئة فقط، لا جداول إضافية.
  - المخطط الكامل موثّق في المستودع: 14 ملف SQL تراكمي + مقطع SQL الجولة 93.

## 2. الجواب الصادق: هل يُستعاد المشروع المحذوف؟

- **لا توجد استعادة ذاتية**: المشروع المحذوف لا يظهر في اللوحة ولا يمكن للمستخدم إرجاعه بنفسه.
- **توجد نافذة استعادة عبر الدعم الرسمي**: المصادر تتفاوت (30–90 يوماً حسب الحالة) — فرصة حقيقية لكن **غير مضمونة**. كلما أُرسلت التذكرة أسبكر كانت أحسن.
- **فوراً**: فتح تذكرة من <https://supabase.com/dashboard/support/new> بعنوان «Accidentally deleted project — restore request» مع **ref المشروع القديم**.
- **كيف يجد المالك ref القديم**: من Vercel ← Settings ← Environment Variables (قيمة `NEXT_PUBLIC_SUPABASE_URL` القديمة تحتوي ref داخل الرابط)، أو من رسائل Supabase البريدية.
- لو استُعيد لاحقاً: يمكن الرجوع إليه (إعادة متغيرات Vercel) أو ترحيل بياناته للمشروع الجديد — قرار يومها.

**ملاحظة بيانات صادقة:** لا توجد نسخة احتياطية لبيانات المستخدمين في المستودع (سكربت `scripts/export-db.ts` موجود لكنه يدوي). هذه الجولة أعادت **البنية والبذور فقط** — الحسابات والمحتوى القديم لا يُستعادان إلا عبر استعادة الدعم للمشروع القديم.

## 3. ما نُفّذ في هذه الجولة

1. **`download/supabase_full_rebuild.sql`** (جديد): دمج كل ملفات المخطط في ملف واحد مرتّب بالاعتماديات — القاعدة (23 جدولاً) + المسارات الأكاديمية + المجموعات وطلبات الانضمام + تيليجرام (المصادر/العناصر/المواضيع/سجل الذكاء `ai_events`) + `bot_config` + الإشعارات وتفضيلاتها + الجدول الشخصي + اشتراكات Web Push + أعمدة ر41/r56/r93 (`review_status`/`uploader_id`/`kind`/`reporter_id`…) + البذور + سياسات RLS بنفس النموذج الأمني القديم حرفياً. الملف idempotent (آمن لإعادة التشغيل) ويعمل من SQL Editor مباشرة.
2. **تطبيق فعلي** على المشروع الجديد عبر اتصال Postgres مباشر (pooler `aws-1-eu-west-1.pooler.supabase.com` جلسة 5432) — 122 عبارة نُفذت بنجاح.
3. **تحقق آلي شامل**:
   - 35/35 جدولاً موجوداً، لا ناقص ولا زائد عمّا يستدعيه الكود (31 جدولاً + جدولان سريان + `cached_course_materials`).
   - البذور: 1 مؤسسة، 1 تخصص، 5 سنوات، 15 فوجاً، 5 مسارات، 5 مجموعات افتراضية، ربط 15/15 فوجاً بـ `track_id` + `group_id`.
   - الأمان مطابق: `bot_config` مُسحوب من anon/authenticated (تجربة anon حقيقية رجعت 401)، `ai_events` RLS مفعّل بلا سياسات، باقي الجداول مقروءة بمفتاح anon كما يتوقع كود الخادم، service role يعمل.
4. **اختبار حي end-to-end للتطبيق نفسه** (لا للمخطط فقط): `next dev` محلياً موصول بالمشروع الجديد ← `/` ترجع 200 ← تسجيل حقيقي عبر `POST /api/auth/signup` أنشأ المستخدم الأول بدور **OWNER** (منطق أول مستخدم سليم) ← حُذف المستخدم التجريبي بمفتاح service role ← `app_users` فارغ ثانية كي يحصل المالك على OWNER بأول تسجيل حقيقي في الإنتاج.
5. نظافة الجولة: `.env.local` التجريبي حُذف (مغطى بـ .gitignore أصلاً) — لا أسرار داخل المستودع ولا في هذا الملف.

## 4. خطوات المالك (خارج الكود) — بالترتيب

1. **Vercel** ← مشروع `gu-mo` ← Settings ← Environment Variables، تحديث الثلاثة بقيم المشروع الجديد ثم **Redeploy** (بدونها يبقى الإنتاج موصولاً بالقديم):
   - `NEXT_PUBLIC_SUPABASE_URL` = `https://qgpzbeqhdidaojrlorpo.supabase.co`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = مفتاح anon القديم النمط (JWT) أو `sb_publishable_…` الجديد — كلاهما يعمل
   - `SUPABASE_SERVICE_ROLE_KEY` = مفتاح service role الجديد
2. **تذكرة دعم Supabase الآن** لاستعادة المشروع القديم (قبل انتهاء النافذة) — القالب في §2.
3. **أول تسجيل بعد النشر لك أنت** (يصبح OWNER تلقائياً بمنطق التطبيق).
4. **تدوير الأسرار المكشوفة في المحادثة** (بعد أن يستقر كل شيء):
   - كلمة مرور قاعدة البيانات: Supabase ← Settings ← Database ← Reset database password.
   - الانتقال للمفاتيح النمط الجديد (Settings ← API Keys: `sb_publishable_` / `sb_secret_`) بدل JWT القديمة وتحديث Vercel وفقه.
   - تنبيه قائم من جولات سابقة: توكنات البوتات الثلاثة مكتوبة حرفياً في `src/lib/community/config.ts` (قرار تصميمي سابق «كي لا يصمت البوت») — يُنصح بنقلها حصراً إلى متغيرات بيئة Vercel في جولة قادمة.

## 5. الأثر على الجولات القادمة

- قائمة r95+ باقية كما نوقشت: لوحة الاسم/الرتبة العليا «Transcendent» في الفوج، ونقاش تطوير بوت تيليجرام.
- **الأهم**: المشروع الجديد يعيد فتح الباب لقرار مؤجل — البيانات الخادمية (رُتب الأعضاء في قائمة الفوج، سجل نقاط مركزي اختياري) صارت ممكنة تقنياً متى أراد المالك مناقشة خصوصيتها، بعد أن كانت محجوبة بغياب الخادم… ثم أعقدها الحذف. لا شيء يُنفَّذ فيها دون قرار المالك.

---

## 6. تحديث لاحق بنفس الجولة — تنفيذ خطوة Vercel آلياً (بتوكن المالك)

بعد أن زوّد المالك الجلسة بتوكن Vercel، نُفّذت الخطوة ① من §4 برمجياً بدل يدوياً:

- **فُحصت متغيرات البيئة الفعلية** في مشروع `gu-mo` على Vercel: التطبيق يقرأ فعلياً `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` (61/4/6 مواضع)، و**بوت الوكيل المستقل** يقرأ `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` لمزامنة ملفات عقله في Storage. أما `DATABASE_URL`/`SUPABASE_JWT`/`SUPABASE_POOLER_HOST` فغير مقروءة في كود الإنتاج (فرع Prisma الخامل) — تُركت كما هي.
- **حُدّثت 5 متغيرات** بالقيم الجديدة (PATCH على نفس المعرفات مع الحفاظ على الأهداف production/preview/development)، وفشل صفر.
- **أعيد نشر الإنتاج** (redeploy من آخر إصدار READY بنفس كوميت 473d1bd → بناء جديد dpl_6bGtLefxX6jE7vwgTA1obPjTEBfj) — READY في ~90 ثانية.
- **تحقق إنتاج حي**:
  - `https://gu-mo.vercel.app/` → 200
  - `ads.txt` بمحتواه الصحيح (`pub-8081529487869617`)
  - `GET /api/institutions` → **البيان المبذور من المشروع الجديد نفسه** (1 مؤسسة) — الخادم يقرأ القاعدة المبنية عبر الشبكة فعلياً
  - `POST /api/auth/signin` → JSON سليم (طبقة المصادقة حية)
- **أنشئ مسبقاً bucket باسم `agent-memory`** (خاص، عبر مفتاح service) كي يبدأ بوت الوكيل نظيفاً دون محاولة إنشاء أولى (الكود يتحمل 409 أصلاً).
- `app_users` ما يزال فارغاً — **أول تسجيل حقيقي في الإنتاج يصبح OWNER** (للمالك).

ما تبقّى على المالك من §4: تذكرة دعم Supabase للمشروع القديم، والتسجيل أولاً، وتدوير الأسرار (كلمة مرور قاعدة البيانات + توكن Vercel هذا نفسه المكشوف بالمحادثة، والانتقال لمفاتيح `sb_publishable_` النمط الجديد لاحقاً).
