# تقرير الجولة 87 — «دفتر طالب»: مساحة دراسية بأسلوب NotebookLM داخل أدواتي

**التاريخ:** 17 سبتمبر 2026
**الحالة:** منجزة — فحص 97/97، انحدارات r86 92/92 وr85 38/38 وr81 31/31، تحقّق أنواعي (tsc) نظيف، فحص لين (eslint) نظيف، `next build` ✓.

---

## 1) الخلاصة التنفيذية

بطلب المالك — الذي أشار إلى أن منصات التوليد (Alborihi نموذجاً) تعتمد أدوات مجانية مفتوحة من طراز GPT4Free وتقدم قدرات «أصلها مدفوع» منها إنشاء الصور وأدوات شبيهة بـ NotebookLM — تضيف هذه الجولة **«دفتر طالب»**: أول تطبيق عملي لفكرة NotebookLM داخل المنصة، مبني على مفاتيح المنصة القائمة نفسها (Groq ← Gemini ← xAI) بلا أي خدمة خارجية جديدة ولا أي مفتاح إضافي ولا أي تخزين.

| القسم | ماذا يفعله |
|---|---|
| **المصادر** | الطالب يبني دفتره: لصق نص، ملفات `.txt/.md`، أو استخراج نص PDF **داخل جهازه** (pdfjs بعامل محلي — نفس عُرف أدوات الملفات). حتى ٨ مصادر، ٣٠ ألف حرف للمصدر، تفعيل/تعطيل بحرة، وحذف نهائي من الجهاز |
| **المحادثة** | حديث مُسنَد بحديث NotebookLM: الجواب يخرج **من مصادر الطالب حصراً** مع استشهاد موضعي `[م1] [م2]`، وإن لم تكفِّ المصادر يقول ذلك بصراحة ويميز «الاستنتاج» عن النص المنقول |
| **أدوات الدراسة** | ٨ مخرجات جاهزة بضغطة واحدة: ملخّص شامل، دليل دراسة (مفاهيم/أسئلة/نصائح)، اختبار تدريبي **تفاعلي بتصحيح فوري ونتيجة**، بطاقات استذكار **بقلب وخلط**، خط زمني، أسئلة شائعة، ملخّص صوتي حواري، خريطة ذهنية Mermaid |

**الاستجابة لنقطتَي طلب المالك:**
- **«مجاني بلا اشتراك»:** لا مفتاح جديد ولا خدمة مدفوعة — الدفتر يسير على سلسلة المزوّدين المجانية نفسها التي أثبتت عملها حياً منذ r44، وهندسة «مصادر الطالب + استرجاع محلي» تعني أن الطالب يحصل على تجربة NotebookLM الأثمن (حديث موثّق من وثائقه) بكلفة نداءات أقل من الدردشة الحرة لأن المقتطفات مقصوصة بسقوف.
- **«الملخص الصوتي» (أشهر ميزة في NotebookLM):** نُفِّذ **بصفر مزوّد صوت** — حوار «المذيع والخبيرة» يولَّد نصاً على المزوّد النصي القائم ثم يُقرأ بمحرّك `speechSynthesis` داخل جهاز الطالب (نفس فلسفة «أدوات داخل جهازك»): تشغيل/إيقاف مؤقت/إيقاف، وإبراز المقطع الجارٍ، ورسالة صادقة إن لم يتوفر صوت عربي في الجهاز. هذا يحل ما أُجّل في r86 («TTS لغاية توفر مزوّد») بحل لا يحتاج مزوّداً أصلاً.

**ما لم يُنفَّذ في هذه الجولة عمداً:** توليد الصور — تراجعت r83 بطلب المالك لغياب مفتاح صور، والقاعدة الصارمة: لا ميزة تعتمد مزوّداً غير متاح. (انظر §7: مقترح r88 عبر مزوّد بلا مفاتيح يعالج هذا الباب كاملاً).

## 2) مبادئ البناء (استمرارية قواعد r85/r86)

- **وحدة نقية واحدة** `src/lib/ai/notebook.ts`: تُستورد `providers` و`study-tools` فقط (لاستثمار فحص Mermaid المُجرَّب) — تُختبر بـ bun خارج Next.js (قاعدة bot-api)، صفر تخزين: المقتطفات يسافر بها الطلب ثم تُنسى.
- **انقسام عمل جهاز/خادم غير مسبوق في المنصة:** التقطيع والاسترجاع (IDF خفيف) يعملان **في متصفح الطالب** — نفس الملف النقي المستورد في العميل — فلا يرى الخادم مصادره كاملة أبداً؛ يستقبل لهذا الطلب فقط مقتطفات مختارة (≤ ١١ ألف حرف) أو مُجلَّداً مُقتطعاً بحد أقصى (≤ ٣٠ ألفاً). هذه أقسى صياغة ممكنة لوعد الخصوصية: ما لا يُرسَل أصلاً لا يُخزَّن ولا يُسرَّب.
- **ممرّ واحد لكل طلب، بلا حلقات إعادة:** المخرجات نداء واحد + مدقّق صارم؛ الخريطة الذهنية تستثمر فحص/تصحيح r86 (استخراج من أول ترويسة معروفة، بلا روابط، بلا click) مع اشتراط النوع `mindmap` حصراً.
- **حدود مستقلة** عن الدردشة وعن بوت تيليجرام: الحديث 4ث/٨٠ يومياً، وأدوات المخرجات الثماني بركة واحدة 12ث/٣٠ يومياً — في الذاكرة لكل نسخة (نفس منهج /api/ai المعلن بصدق).
- **الحصص بعد التحقق فقط** (درس r43): الطلب الفاسد يُرفض 400 قبل أن يلمس أي حصة.
- **needsConfig بعُرف r44:** لا مفاتيح على الخادم → المالك يرى تعليمات الإعداد (مع تطمين صريح: إن كان المساعد يعمل فالدفتر يعمل بلا أي إعداد) والطالب يرى «قريباً».
- **بلا حرس توليد** — الدفتر أداة **تحليل** لمحتوى الطالب نفسه (موضع r86: أدوات التحليل تعالج النص كالدردشة العامة)؛ لا يبني الدفتر محتوى من أمر حر خارج مصادر المستخدم.

## 3) تفاصيل تقنية لطيفة

- **استرجاع عربي بلا خدمة تضمين:** تطبيع (تشكيل/تطويل/ألف/ى/ة) + رموز `\p{L}\p{N}` + قائمة كلمات وظيفية عربية/فرنسية، وترجيح `1 + log(N/(1+df))` لكل توكن سؤال داخل القطعة — رخيص، محلي، وكافٍ لمستندات دراسية. بلا أي تطابق → توزيع دائري على بدايات المصادر بدل فراغ.
- **قطع بطرف فراغ:** الكسر عند آخر `\n` أو `". "` أو مسافة داخل ٦٠٪ الأخيرة من القطعة كي لا تُمزّق الكلمات والمعادلات، بتداخل ٨٠ حرفاً يمنع انقطاع المعنى بين قطعتين.
- **مدقّقات متسامحة صادقة:** استخراج JSON يزيل الأسوار والنص المحيط (النماذج تحب التمهيد)، والأسئلة المخالفة في الاختبار (٣ خيارات، index خارج 0-3) **تُسقط ولا تُفرّغ الصفحة**، وإن سقط تحت الحد الأدنى (٤ أسئلة) جاء خطأ عربي صادق بدل تجربة وهمية.
- **توحيد المتحدثين:** أي تسمية فيها «مذيع/host/مقدم» → «المذيع»، و«خبير/خبيره» → «الخبيرة»، والمجهول يتناوب بالموضع — الحوار لا ينهار من غرابة تسمية.
- **فشل مُكتشف أثناء الفحص:** قصّة أول مقتطف للميزانية لم تُحدّث عدّاد الميزانية فانزلقت قطعة صغيرة لاحقة فوق السقف — أصلحتها (`used = budgetChars`) والفحص C10 يحيكها الآن دائماً.
- **توقيع المزوّد للمالك فقط:** مثل المساعد، المالك يرى `provider · model` تحت كل إجابة ومخرج — والمستخدم العادي لا يرى شيئاً من البنية.

## 4) الفحص — 97/97 (+ انحدارات 92/92 و38/38 و31/31)

- **التطبيع والتقطيع (6):** تشكيل/ألف/ى/ة، كلمات وظيفية، لاتيني/أرقام، رموز من حرف واحد.
- **التقطيع (5):** فارغ، قصير، طويل متعدد، سقف الحجم مع كسر الفراغ، التداخل.
- **الاسترجاع (10):** اختيار المصدر الصحيح لسؤالين مختلفين، سؤال مقارن يجمع المصدرين، بلا تطابق ← توزيع، ميزانية الأحرف، مصدر فارغ، قصّة أول مقتطف مع إصلاح C10.
- **المُجلَّد (7):** حصص متساوية، علم الاقتصاص، ترويسة المصادر.
- **بنّاءات الحديث (6):** عقد الاستشهاد، صدق عدم الوجود، ترقيم [م1]/[م2]، التاريخ خام.
- **JSON (7):** مباشر/أسوار/نص محيط/مصفوفة/متداخل/مكسور/غائب.
- **المدقّقات (22):** الأفعال الثمانية سليمة + ناقصة صادقة + إسقاط أسئلة الاختبار المخالفة + توحيد المتحدثين + رفض نوع mindmap المخالف.
- **التفريغ والتصدير (8):** dispatch من جواب مزوّد ملفوف، تصدير .md/.mmd بتعليمة mermaid.live.
- **الحصص (7):** فاصل، سقف يومي، مستخدمان مستقلان، غد جديد، reset.
- **التحقق من الطلب (14):** أفعال مجهولة، سؤال فارغ، بلا مقتطفات، ترتيب تاريخ مخالف، مقتطفات/مُجلَّد فوق السقف، اكتمال أدوار الأنظمة الثمانية.
- **خط المزوّد (4):** مزوّد مُعقّم يجيب ← مدقّق ← نتيجة، ودور النظام يُمرَّر كما هو، و`isAiConfigured` ينعكس بالمفاتيح.
- **الانحدارات:** r86 (92/92 — أدوات البوت الخمس)، r85 (38/38 — استوديو HTML)، r81 (31/31 — عقل البوت).

## 5) الملفات

| الملف | الدور |
|---|---|
| `src/lib/ai/notebook.ts` (جديد، ~870 سطراً) | الوحدة النقية: حدود معلنة، تطبيع/تقطيع/استرجاع، مُجلَّد، أدوار الأنظمة الثمانية، استخراج JSON، مدقّقات، تصدير نصّي، مصانع الحصص، تحقق الطلب |
| `src/app/api/ai/notebook/route.ts` (جديد) | الجلسة، التحقق، حدّان مستقلان، حديث SSE بنفس عقد /api/ai، مخرجات JSON مدقّقة، needsConfig وأخطاء عربية (مع hints للمالك) |
| `src/components/talib/tools/notebook-tool.tsx` (جديد، ~1750 سطراً) | مساحة الدفتر كاملة: مصادر (لصق/ملف/PDF بتقدم صفحات)، حديث بث حي، شبكة الأدوات، عرض كل مخرج (اختبار تفاعلي، بطاقات قلّابة، مشغّل صوتي، خريطة)، حفظ localStorage لكل مستخدم |
| `src/components/talib/tools/tools-tab.tsx` | بطاقة مميزة ثانية «دفتر طالب» (هوية زمردية + معاينة مصدر←جواب موثّق) + توجيه + بحث عربي مطبّع |
| `scripts/r87-check.ts` (جديد) | 97 فحصاً ببُنٍّ معقّم وساعة محقونة |

## 6) حدود معروفة (صادقة)

- **الحدود في الذاكرة لكل نسخة خادم** — تقريبية بحكمها (نفس منهج المساعد و/html والمخرجات الخمس).
- **الاسترجاع كلماتي لا دلالي:** سؤال بصياغة بلا توافق لفظي مع المصدر يقع في التوزيع الاحتياطي — مقبول لمستندات دراسية، ومعلن في الواجهة («راجع الموضع المستشهد به»).
- **اقتصاص المُجلَّد:** مصادر تتجاوز ٢٦ ألف حرف مجتمعة تُعالج حصصها الأولى مع toast إعلامي صادق — الملخّص لا يدّعي تغطية ما لم يُرسَل.
- **الصوت = متصفح الطالب:** جودة speechSynthesis تختلف بالجهاز، وغياب صوت عربي يظهر تنبيهاً قبل التشغيل — لا وعد بجودة استوديو.
- **الخريطة الذهنية تُسلَّم كوداً** يُعرض في mermaid.live/GitHub/Obsidian (نفس تذييل /مخطط في البوت) — لا رسم SVG داخل الصفحة بعد.

## 7) خطوات قادمة ممكنة (r88+)

- **r88 — استوديو الصور المجاني:** معالجة باب r83 المتراجع من أصل الفكرة نفسها («مجاني بلا مفتاح»): مزوّد صور بلا مفاتيح من طراز Pollinations (flux) خلف حرس المالك والحدود (10ث/١٢ يومياً كما كانت r83) — يحوّل «إنشاء الصور» من ميزة معلّقة على مفتاح إلى ميزة تعمل اليوم.
- ربط «دفتر طالب» بالبوت: /ملخص و/اختبار من ملف يرسله الطالب في المحادثة الخاصة.
- تضمين Mermaid داخل الصفحة (SVG محلي) بدل الإحالة لمحررات خارجية.
- حفظ المخرجات كأوراق في الدفتر مع إعادة فتحها لاحقاً (حالياً: نسخ/تنزيل فقط).
