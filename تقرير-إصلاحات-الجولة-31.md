# تقرير الجولة ٣١ — فصل ملفاتي عن أدواتي + سحابتي (Google Drive)

**التاريخ:** ٥ سبتمبر ٢٠٢٦ · **النوع:** إصلاح تنقّل + ميزة سحابية · **الحالة:** مكتملة ومنشورة

---

## ١. إصلاح الازدواجية: أدواتي شاشة مستقلة

**المشكلة التي أبلغتَ عنها:** الفصل بين «ملفاتي» و«أدواتي» في الشبكة الرئيسية
أوجدى ازدواجية — أدواتي كانت **بلاطة في الرئيسية** و**في نفس الوقت تبويباً
داخل ملفاتي**، كما أن بلاطة أدواتي كانت تفتح شاشة «ملفاتي» نفسها (بعنوان
خاطئ) على التبويب مسبقاً.

**الحل:**

- شاشة جديدة مستقلة `tools-screen.tsx` بعنوان «أدواتي» ووصفها الخاص —
  الأدوات الآن في مكان واحد فقط.
- `ملفاتي` بدون تبويب الأدوات نهائياً؛ تبويباتها الثلاثة الجديدة:
  **المكتبة / ملاحظاتي / سحابتي**.
- مسار `#/tools` يفتح الشاشة المستقلة (ويستعيدها بعد التحديث — مُتحقق منه).

## ٢. سحابتي — ملفاتك في Google Drive

**طلبك:** «أضيف سحابة مثل Google Drive لرفع ملفاتي وربطها بالتطبيق، لأن
قاعدة Supabase صغيرة» — تم.

**كيف تعمل:** كل طالب يربط **حساب Google الخاص به** (١٥ ج.ب مجاناً)، وتُرفع
الملفات من المتصفح **مباشرة إلى Drive الخاص به** في مجلد «طالب — Talib» —
بدون أن تمر عبر خوادمنا أو تستهلك شيئاً من Supabase.

| القدرة | التفاصيل |
| --- | --- |
| رفع متعدد | أي عدد من الملفات مع شريط تقدم لكل ملف (multipart upload) |
| تصفّح وتحميل | قائمة الملفات بالحجم والتاريخ + تحميل للجهاز |
| مشاركة | «نسخ رابط» ينشئ رابط «أي شخص لديه الرابط» ويُنشئه في الحافظة |
| حذف | مع تأكيد — الملف يذهب لسلة مهملات Drive |
| المساحة | شريط يعرض استهلاك Drive الكلي |
| الخصوصية | صلاحية `drive.file` فقط: التطبيق يرى ملفاته هو حصراً، ولا يرى أي ملف آخر في حسابك + زر فصل فوري |

**اتصال ذكي:** الجلسة تُحفظ مع مدة صلاحيتها وتُعاد صامتةً عند الحاجة؛ عند
انتهائها تظهر رسالة «اضغط ربط Google Drive من جديد» بدل الأعطال.

## ٣. خطوة المالك الوحيدة (مرة واحدة — ٥ دقائق)

Google تشترط أن يكون مفتاح OAuth ملكاً لحسابك أنت. تبويب سحابتي يعرض
**دليلاً ذاتياً من ٧ خطوات** داخل التطبيق حتى يتم الإعداد، وخلاصته:

1. console.cloud.google.com → فعّل **Google Drive API**.
2. OAuth consent screen (External) + أضف بريدك كمستخدم تجريبي.
3. Credentials → OAuth client ID → **Web application**.
4. Authorized JavaScript origins: `https://gu-mo.vercel.app` (+ `http://localhost:3000` محلياً).
5. انسخ Client ID → في Vercel أضف المتغير `NEXT_PUBLIC_GOOGLE_CLIENT_ID` → Redeploy.

بعد ذلك يتحول التبويب تلقائياً من الدليل إلى زر «ربط Google Drive».

## ٤. التحقق العملي (مباشرة على الإنتاج)

- `tsc --noEmit` = 0 أخطاء (وأُضيف typescript كـ devDependency كان مفقوداً
  — التحقق السابق كان يعتمد على npx)؛ `next build` أخضر وكل الصفحات
  العامة بقيت ثابتة التوليد.
- حساب اختبار حقيقي على الإنتاج (`talib.round31.test@example.com` — يمكن
  حذفه بسكربت `cleanup-test-accounts.ts` الموجود):
  - إنهاء الـ onboarding كاملاً → الرئيسية سليمة.
  - **ملفاتي**: التبويبات [المكتبة، ملاحظاتي، سحابتي] — لا أثر لأدواتي.
  - **سحابتي**: الدليل الذاتي (٧ خطوات) يظهر كاملاً بدون أي خطأ console.
  - **أدواتي** (بلاطة الرئيسية): شاشة مستقلة بعنوانها، ٧ أدوات + لافتة
    الخصوصية، ولا توجد تبويبات ملفات.
  - `#/tools` يستعيد الشاشة بعد إعادة التحميل. لا أخطاء console إطلاقاً.
- لقطات 390×844: `download/verify-390/r31-*.png`.

## ٥. ملاحظات

- لا حاجة لأي إضافة قاعدة بيانات من سوق Vercel — سحابتي تحل مشكلة
  المساحة من جذورها (مساحة كل طالب هي حسابه هو).
- ملفات المكتبة (مراجع التخصص) ما تزال في Supabase كما هي — سحابتي
  للملفات **الشخصية** لكل طالب.
