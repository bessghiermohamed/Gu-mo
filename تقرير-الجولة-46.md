# تقرير الجولة 46 — الهوية البصرية تصبح ألوان فيسبوك

## الطلب
تغيير ألوان التطبيق إلى ألوان فيسبوك (الأزرق الرسمي + الرماديات المعتادة).

## ما نُفّذ

### 1) الثيم الافتراضي «أكاديمي» بالكامل → ألوان فيسبوك (`src/app/globals.css`)
الوضع الفاتح:
- الخلفية `#F0F2F5` (رمادي فيسبوك المعتاد)، البطاقات أبيض، النص `#050505`
- اللون الأساسي `#1877F2` (فيسبوك بلو)، الثانوي `#0866FF` (أزرق الأزرار الإجرائية)
- الأسطح الثانوية والتمرير `#E4E6EB`، النص الثانوي `#65676B`، الفواصل `#DDDDE1`
- لون الخطأ `#E41E3F` (أحمر فيسبوك)

الوضع الداكن (مطابق للوضع الداكن الرسمي لفيسبوك):
- الخلفية `#18191A`، البطاقات `#242526`، الأسطح `#3A3B3C`
- النص `#E4E6EB`، النص الثانوي `#B0B3B8`، الفواصل `#3E4042`
- الأساسي بقى `#1877F2`، الخطأ `#FF6968`

مخططات الرسوم (chart-1..5): أزرق فيسبوك، أخضر `#42B72A/45BD62`، كهرماني `#F7B928`، بنفسجي `#A033FF`، أحمر.

### 2) الأصول البصرية
- `public/talib/icon.svg` (الشعار + الفافيكون): التدرج الأخضر → تدرج `#0866FF → #1877F2` مع شرابة زرقاء فاتحة
- `public/talib/hero-banner.svg` + `hero-banner.jpg`: أعيد توليدهما بتدرج أزرق فيسبوك (الـJPG الجديد مولَّد من الـSVG المعاد تلوينه بنفس الأبعاد 1536×768)
- صفحة الهبوط (`src/app/page.tsx`): استبدال `hero-banner.jpg` بـ `hero-banner.svg` (أوضح وأخف، ونفس نقطة التبديل الموثقة في الكود)

### 3) بقايا الألوان القديمة في الكود
- `files-screen.tsx`: اللون الافتراضي للملاحظات `#1B5E4B` → `#1877F2`
- `study-timer-tool.tsx`: حلقة المؤقت `#1B5E4B/#C8956C` → `#1877F2/#0866FF`

### 4) ما لم يُمَس (عمداً)
- الألوان الدلالية: `emerald` (نجاح/قبول — فيسبوك يستخدم الأخضر للنجاح أيضاً)، `amber` (نجوم/تحذيرات)، `red` (أخطاء)، `violet→fuchsia` (هوية المساعد الذكي، كتدرج Meta AI)
- الثيم البديل «عصري» (بنفسجي/قرمزي) — يبقى خياراً من الإعدادات لمن يبدّله

## التحقق
- `tsc` صفر أخطاء، `eslint` نفس أعداد المشاكل القديمة تماماً (1188 — كلها سابقة في ملفات مورّدة مصغّرة)، `next build` أخضر (68/68 صفحة)
- فحص بصري على Localhost بمتصفح حقيقي 430×932: الهبوط، الدخول، التسجيل، الإعداد (6 خطوات)، الرئيسية (فاتح + داكن)، الجدول (فاتح + داكن)، المقررات — كلها زرقاء فيسبوك بشكل سليم
- حساب الاختبار المحلي `r46-colors-test@ex.com` وُجد وحُذف من قاعدة البيانات المحلية المؤقتة فقط (قاعدة الإنتاج لم تُمَس)
- بعد النشر: CSS الإنتاج يحتوي `1877f2`×9 و`0866ff`×2 و`f0f2f5`×3 و`18191a`×1، وقيم الأخضر القديمة (oklch emerald) لم يعد لها أثر

## لقاطات
- `/home/z/my-project/download/r46/02-landing-v2.png` — الهبوط
- `03-app-entry.png` — شاشة الدخول
- `05-home-clean.png` / `06-home-dark.png` — الرئيسية فاتح/داكن
- `07-schedule.png` — الجدول
- `08-courses.png` — المقررات

## النشر
- كوميت `e03b25b` مرفوع، Vercel نجح، gu-mo.vercel.app يخدم الثيم الجديد.
