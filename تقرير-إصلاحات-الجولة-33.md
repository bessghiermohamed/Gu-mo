# تقرير إصلاحات الجولة 33 — زر رفع المواد في المقررات + مراجعة وتنظيف

**التاريخ:** 5 سبتمبر 2026
**طلبات المالك:** (1) «في مواد المقرر لم أجد زر رفع الملفات» — (2) «عند فصل Google Drive يجب أن تظهر تنبيه أولاً» — (3) مراجعة المشروع وإصلاح الأخطاء — (4) تنظيف المستودع مع نسخة احتياطية قبل التنظيف ثم المقارنة ثم حذف النسخة.

---

## 1) زر الرفع في مواد المقرر (المشكلة الرئيسية)

**السبب الجذري:** شاشة تفاصيل المقياس كانت تحتوي ثلاثة تبويبات فقط (الدروس/الاختبارات/الواجبات) — لا توجد أي مكان للملفات فيها، وزر الرفع كان حصراً في ملفاتي → المكتبة.

**الحل — تبويب جديد «المواد» في كل مقياس:**
- كل مقياس أصبح له تبويب رابع «المواد» يعرض مواد هذا المقياس تحديداً (من جدول المكتبة المرتبطة بمعرّف المقياس).
- للمشرفين: زر **«إضافة مادة للمقياس»** يفتح نفس نافذة النشر عبر Drive (رفع من حسابك ١٥ ج.ب → تنزيل مباشر للطلبة).
- المادة المنشورة من داخل المقياس تظهر في تبويب المواد **وفي المكتبة العامة** معاً (سطر واحد في قاعدة البيانات).
- التصنيف الافتراضي داخل المقياس: «محاضرة».

**تفادي التكرار:** نافذة النشر أُخرجت إلى ملف مشترك جديد `src/components/talib/cloud/publish-dialog.tsx` — مصدر واحد تستخدمه شاشتا المكتبة والمواد (بخاصيتي `moduleId` و`defaultCategory`). حُذفت النسخة المحلية القديمة من files-screen (−358 سطراً).

**التدرج الآمن:** عمود `module_id` في قاعدة البيانات اختياري — إن لم يُضف بعد، يعمل كل شيء (تُحفظ المادة في المكتبة العامة) وتظهر للمشرف بطاقة توضيحية بدل الخطأ. لتفعيل الربط الكامل، سطر واحد في Supabase → SQL Editor:

```sql
ALTER TABLE library_references ADD COLUMN IF NOT EXISTS module_id INTEGER;
```

## 2) تنبيه فصل Google Drive

زر «فصل الاتصال» في سحابتي كان يفصل فوراً بلا سؤال. الآن يفتح نافذة تأكيد توضح: **ملفاتك لن تُحذف** — ستبقى في مجلد «طالب — Talib» في حسابك، وتحتاج فقط لإعادة الربط لاحقاً. الفصل يتم فقط بعد ضغط «فصل الاتصال».

## 3) المراجعة وإصلاح الأخطاء

- `tsc --noEmit` (النسخة الحقيقية): **0 أخطاء** — `next build`: أخضر — `eslint` على الملفات المتغيّرة: نظيف.
- أُصلح: استيراد `Plus` المفقود في files-screen بعد إخراج النافذة (كان سيعطّل البناء).
- أُزيل كود ميت: خطاف `useI18n` غير المستخدم في course-detail (من جولات سابقة).
- فحص شامل: لا `console.log` متبقٍّ في الملفات المعدّلة، ولا مراجع كودية للمجلدات المحذوفة.

## 4) التنظيف — حسب بروتوكولك (upload/تنظيف-وقاعدة-حماية-git.md)

**خطوة الأمان (فحص المراجع لكل مجلد مرشّح في src/ + prisma/):** لا يوجد أي استدعاء كودي لـ `tool-results/` أو مجلدات لقطات التحقق — كل المطابقات كانت تعليقات نصية فقط.

**القائمة الكاملة لما حُذف من المستودع (40 ملفاً):**
- `tool-results/` — 6 ملفات سجلات تنفيذ داخلية للأداة (244 ك.ب) — ليست كوداً.
- `download/verify-390/` + `download/verify-site/` — 34 لقطة شاشة من جولات التحقق السابقة (~2.9 م.ب). **حُفظت نسخة كاملة خارج المستودع** (مجلد التنزيلات المحلي → talib-verify-screenshots/) وتبقى متاحة في تاريخ Git.

**ما فُحص ولم يُحذف:**
- **ملفات .sql السبعة في download/** — فُحصت بالبصمة (md5): كلها فريدة، لا تكرار → بقيت كاملة (سكربتات إنشاء وتحديث قاعدة Supabase).
- **`upload/`** — تعليماتك وقواعد العمل → بقيت كما هي.
- **`scripts/`** — ضمن قائمة «لا تحذف» → بقيت كاملة (بما فيها سكربتات التحقق القديمة).
- **`Caddyfile`** — ملف وكيل منصة z.ai (لا علاقة له بالتطبيق لكنه يخدم بيئة العمل) → بقي.
- ملفات الإعداد في الجذر، و`src/`، و`prisma/`، و`public/` — لم تُمسّ.

**فحص البيئة المحلية (إضافي إلزامي):**
- لا يوجد أي ملف قاعدة بيانات محلي (dev.db غير موجود — Prisma المحلي لم يُنشئه).
- أكبر العناصر: `node_modules` 1.3 ج.ب (تُعاد تلقائياً بـ bun install — حُذفت بعد الرفع)، النسخة الاحتياطية 102 م.ب (حُذفت بعد التحقق)، `skills/` 61 م.ب (أدوات منصة z.ai — خارج المستودع، بقيت لأنها من البنية التحتية للبيئة لا من المشروع).
- لا توجد ملفات محلية منشأة من الأداة ونسيان رفعها — شجرة العمل كانت نظيفة قبل التنظيف.

**النسخة الاحتياطية والمقارنة (طلبك):** استُنسخ المستودع كاملاً من GitHub قبل التنظيف (275 ملفاً، يشمل تعهدات هذه الجولة). بعد التنظيف قورنت الشجرتان ملفاً-ملفاً: **الفروق = الملفات الأربعون المحذوفة فقط + ملفان معدودان عمداً (README و .gitignore)** — و`src/` و`prisma/` و`scripts/` و`public/` و`upload/` متطابقة بايت-ببايت. كما تحقق قانون حماية الشاشات (قاعدتك رقم 10): 17 ملف شاشة قبل وبعد — لا فقدان. ثم حُذفت النسخة الاحتياطية.

**المساحة المتحررة التقريبية:** ~3.1 م.ب من المستودع (40 ملفاً) + ~1.4 ج.ب من بيئة العمل المحلية (node_modules + مخلفات البناء + النسخة الاحتياطية).

## 5) الخلاصة

المشرف الآن يرفع محاضرات أي مقياس من **داخل المقياس نفسه** (تبويب المواد) أو من المكتبة العامة — كلاهما نفس النافذة والمسار الآمن عبر حسابه على Drive، والطلبة يحمّلون بلا أي إعداد. وفصل Drive أصبح آمناً بتنبيه صريح، والمستودع نظيف وموثّق ببروتوكولك كاملاً.
