# تقرير إصلاحات الجولة 36 — حذف شامل للمالك + حوارات حذف فعّالة + أزرار ظاهرة + تغيير المسار الأكاديمي

**التاريخ:** 5 سبتمبر 2026
**النطاق:** الهيكل الإداري (لوحة الإشراف) + رحلة المستخدم (حسابي/التهيئة) + ثلاث واجهات API — مع إصلاح سباق جلب قائم مسبقاً اكتُشف أثناء التحقق.

---

## ١. الطلب الأصلي

أربعة أشكال من التذمر وردت من المالك أثناء عمله على بيانات تجريبية حقيقية:

1. **حذف المدارس أو التخصصات الممتلئة ببيانات تجريبية:** الحارس يمنع الحذف («احذف التخصصات أولاً») بلا أي مخرج — المالك محتجز خلف حماية صُمّمت لحمايته منه هو.
2. **طريق مسدود في الحوار:** الرسالة تقول «احذف التخصصات أولاً» ولا يوجد أي زر ينقلك إلى مكان الحذف فعلاً — بحث المالك عن «الصندوق» ولم يجده.
3. **أزرار تبدو نصاً:** أزرار تعديل/حذف في قوائم الهيكل كانت أيقونات شبحية بلا حدود (ghost) فقرأها المالك نصاً عادياً وتجاهلها.
4. **قيود على المالك:** ميزة «تغيير المسار كما أريد» اختفت — لا يوجد أي وسيلة لتغيير المؤسسة/التخصص/السنة بعد التسجيل، حتى للمالك.

## ٢. الحذف الشامل (force delete) — بئر نجاة المالك

**الواجهات الثلاث** (`/api/institutions`، `/api/specialties`، `/api/years`) قبلت الآن `?force=1` — **للمالك فقط**:

- **المؤسسات:** تجميع معرّفات الشجرة كاملة (تخصصات ← سنوات ← ملامح ← مجموعات ← أفواج) ثم:
  1. **فصل الحسابات أولاً** (ولا يُحذف أي حساب): `scope_cohort_group_id` و`scope_group_id` يُصفَّران لكل من ينتمي لأفواج/مجموعات الشجرة (FK من نوع RESTRICT في فرع Prisma — يجب فكّه قبل الحذف وإلا انهار الحذف المتسلسل)، ثم إعادة توجيه `assigned_specialty_id` إلى **تخصص ناجٍ خارج المؤسسة** مع تصفير كل أعمدة النطاق.
  2. حذف المؤسسة → قاعدة البيانات تمسح الشجرة المتسلسلة تلقائياً (CASCADE في كلا الفرعين — Supabase وPrisma).
  3. **حماية آمنة:** إن كانت المؤسسة تضم *آخر تخصصات المنصة* يُرفض الحذف برسالة واضحة (المخطط لا يسمح بمستخدم بلا تخصص — `assigned_specialty_id NOT NULL`).
- **التخصصات:** نفس المنطق، والناجي مفضّل من **نفس المؤسسة** أولاً ثم أي تخصص آخر.
- **السنوات:** فصل أفواج/مجموعات السنة ثم حذفها — القوة متاحة للمالك فقط، بينما الحذف العادي يبقى متاحاً للمشرفين كما كان.
- الرد الناجح يحمل `forced: true` ورسالة عربية توضح أن الحسابات أُعيد توجيهها ولم تُحذف.

**تحقُّق على قاعدة بيانات حقيقية (فرع Prisma المحلي):** حذف مؤسسة كاملة = 1 مؤسسة + 1 تخصص + 5 سنوات + 3 ملامح + 5 مجموعات + 10 أفواج اختفت دفعة واحدة؛ حساب المالك (المسجَّل في تخصص المؤسسة المحذوفة) نجا معاد توجيهه إلى التخصص الناجي وجميع نطاقاته مصفّرة — جاهز لإعادة اختيار مساره.

## ٣. حوارات الحذف الفعّالة — لا طريق مسدود بعد اليوم

عندما يمنع الحارس الحذف، يعيد الرد الآن `counts` منظّمة، والحوار يتفاعل:

- **رسالة الحارس** داخل صندوق أحمر كما كانت، + **شارات أعداد** (مستخدمون / سنوات / ملامح / مقاييس — أو تخصصات/مجموعات/أفواج/مقاييس حسب المستوى) تُظهر حجم المحتوى المرتبط بلمحة.
- **زر انتقال حقيقي:** «انتقل الآن إلى التخصصات» (من المؤسسات) ينقلك إلى تبويب التخصصات **مفلتراً مسبقاً على تلك المؤسسة**، و«انتقل الآن إلى السنوات» (من التخصصات) كذلك. لتحقيق ذلك رُفعت حالة التبويب الداخلي والفلاتر إلى `StructureManager` (نمط التبويب المضبوطة بدل `defaultValue`).
- **قسم «حل بديل للمالك — حذف شامل»** (يظهر للمالك فقط عند الانسداد): مربع إقرار إلزامي («أفهم أن هذا الحذف شامل ونهائي…») وزر **«حذف نهائي مع كل المحتوى المرتبط»** يظل معطلاً حتى يُؤشَّر الإقرار — ثم يستدعي `?force=1`.
- سنوات بدون تبويب أبناء داخل «الهيكل»: حوارها يكتفي بالإقرار + الحذف الشامل.

## ٤. أزرار ظاهرة في كل قوائم الهيكل

كل أزرار تعديل/حذف في القوائم الأربع (المؤسسات، التخصصات، الملامح، السنوات) انتقلت من `variant="ghost"` (بلا حدود — تُقرأ نصاً) إلى **`variant="outline"` بحدود كاملة**: تعديل بحدود عادية، وحذف بحدود وحبر أحمر (`border-destructive/40 text-destructive`) مع خلفية `bg-background` لتمييزه فوق أي بطاقة. تحقق بصري بحساب `borderTopWidth = 1px` ولون أحمر فعلي في المتصفح.

## ٥. «تغيير المسار الأكاديمي» — الميزة المستردة

- **زر جديد في حسابي** لكل الأدوار بما فيهم المالك: يفتح معالج التهيئة نفسه في **وضع التغيير**.
- **التهيئة بوضع التغيير** (`mode="change"`):
  - **لاختيار المسار الحالي تلقائياً:** المؤسسة والتخصص والملمح والسنة المحفوظة في الجلسة تُستعاد مباشرة عند تحميل كل قائمة (مرة واحدة، دون خرق قاعدة «لا اختيار صامت» R12-11 — هذه قيم المستخدم نفسه ويبقى free إعادة الاختيار).
  - **لافتة «مسارك الحالي»** كهرمانية أعلى كل الخطوات تعرض المسار الكامل بأسمائه (تُجلب من `/api/profile/details`؛ وتعرض «غير محدد بعد» بأناقة لحساب مفصول حديثاً).
  - **مخرج إلغاء:** «إلغاء والعودة إلى حسابي» بدل «تسجيل الخروج» — لا يُحفظ شيء.
  - زر الإنهاء يحمل عنواناً صريحاً: «حفظ المسار الجديد».
- **الهيكل (`/app/page.tsx`):** حالة `pathChange` + `startPathChange()` في سياق الواجهة؛ نفس بوابة التهيئة تُستخدم للوضعين، وبعد الحفظ: تحديث الجلسة ← العودة إلى حسابي مباشرة مع إشعار «تم تحديث مسارك الأكاديمي بنجاح».
- **API التهيئة نفسه** يدعم أي دور أصلاً — لم يلزم تغيير خلفي.

## ٦. إصلاحات مرافقة

- **توازي فرع Prisma في `/api/profile/details`:** كان يعيد «—» للملمح/السنة/المجموعة/الفوج محلياً (الإنتاج سليم) — أُكملت الأسماء الست كلها.
- **`src/lib/site.ts`:** `NEXT_PUBLIC_SITE_URL` الفارغ (=) كان يهزم مُشغّل `??` (السلسلة الفارغة ليست nullish) فيكسر canonical/sitemap — صار `(… ?? "").trim() || fallback`.
- **إصلاح سباق جلب (اكتُشف في التحقق):** جلب القائمة عند فتح التبويب (بلا فلتر) كان يسبق/يلحق الجلب المفلتر ويطغى عليه بنتيجة قديمة فيظهر تخصصان تحت فلتر مؤسسة واحدة — أُضيف حارس «الطلب الأحدث يفوز» (عداد تسلسل) لجلب التخصصات والملامح والسنوات.

## ٧. التحقق

- `tsc --noEmit` = 0 أخطاء · `eslint` نظيف على الملفات التسعة المتغيرة · `next build` أخضر (كل الصفحات العامة SSG كما هي).
- **جولة متصفح حقيقية 390×844** (قاعدة SQLite محلية + تسجيل مالك حقيقي من الواجهة):
  - التهيئة الست خطوات ← الرئيسية ← حسابي يعرض الزر الجديد.
  - تغيير المسار: اللافتة تعرض المسار الكامل، التحديد الحالي «مُحدد» في الخطوات الأربع، تغيير السنة إلى L3 وحفظها ← عودة لحسابي والسنة تظهر «السنة الثالثة (L3)» (تحقق DB: `scope_academic_year_id = 3`).
  - الإلغاء يعيد إلى حسابي دون أي كتابة.
  - لوحة الإشراف ← الهيكل: حذف مؤسسة ممتلئة ← انسداد + شارات + زر الانتقال (انتقل فعلاً وفلتر القائمة) + الإقرار + الحذف الشامل ← الشجرة اختفت من DB والقائمة، والحساب نجا معاد التوجيه.
  - حذف سنة ممتلئة ← انسداد (١ مجموعة، ٢ أفواج) + حذف شامل ← السنة وأبناؤها اختفت (مجموعات 5←4، أفواج 10←8).
  - `scrollWidth = 390` (لا فائض أفقي) ولا أخطاء console (تحذيرات Radix/AdSense موروثة فقط).
  - لقطات: `download/verify-390-r36/` (محلية فقط — لا تدخل المستودع منذ الجولة 33).

## ٨. ملاحظات للمالك

- على الإنتاج (Supabase): الفرعَان مكتوبان بالكامل — لا حاجة لأي SQL إضافي؛ كل عمليات الفصل تتم قبل الحذف المتسلسل تلقائياً.
- الحذف الشامل يظل محصوراً بالمالك ومخفياً خلف الإقرار؛ المشرفون يرون الانسداد والانتقال فقط.
- «آخر تخصص في المنصة» حالة محمية عمداً: أنشئ البديل أولاً ثم احذف القديم.
