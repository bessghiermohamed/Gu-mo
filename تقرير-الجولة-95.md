# تقرير الجولة 95 — تدقيق «اختراق بوت تيليجرام» والتحقق من استعادة الإعدادات الأصلية

**التاريخ:** 2026-09-21 · **النطاق:** @gu_mo_bot ومحرك المجلة في `telegram-ai-community` + الوكيل بيناري · **نوع الجولة:** تحقيق أمني + تحقق حي، بلا أي تعديل كود.

---

## 1) الخلاصة أولًا

**لا يوجد دليل على اختراق خارجي.** كل التغييرات التي رآها المالك على البوت («الكود تغيّر») مصدرها جلسات العمل السابقة للوكيل نفسه — بهويات `Z User <z@container>` و`murad-builder` (هوية حاويات العمل، وليست أطرافًا خارجية) — خلال ما نسميه «حقبة التبديل» صباح اليوم. **الاستعادة الكاملة تمت الساعة 12:14** (التزام `e848705` في المستودع المصدر → مزامنة `b299841` في Gu-mo) وهي **حيّة على الإنتاج الآن** (نشر Vercel بحالة READY يشغّل نفس الالتزام). هذه الجولة تحقّقت من كل طبقة حيًّا ولم تجد ما يستدعي إعادة أي إعداد.

## 2) الخط الزمني لليوم (من تاريخ `telegram-ai-community`)

| الوقت (UTC) | الالتزام | ماذا حدث |
|---|---|---|
| 08:39 | `a1207e9` (Z User) | «Talib v2»: الوكيل المستقل أُعيدت علامته إلى @gu_mo_bot — **البوت صار وكيلًا مؤقتًا** |
| 08:58 | `bbadc85` (Z User) | تحسين البحث العربي (ويكيبيديا أولًا، RSS أخبار الجزائر، ddg-lite) |
| 09:35 | `d178737` (Z User) | **التبديل**: بيناري @PaymonB_bot صار الوكيل المستقل، وGu Mo عاد بوت مجتمع بشخصية `student` المؤقتة |
| 11:27 | `7a68ba0` (Z User) | إضافة مزوّد pollinations (بلا مفتاح) كسقاطة أخيرة في سلسلة الـ LLM |
| **12:14** | **`e848705` (bessghiermohamed)** | **الاستعادة: Gu Mo عاد إلى مَلَقه الأصلي `sharp` + بوت أول للمجتمع، وحُذفت شخصية `student` من حقبة التبديل** |

## 3) ما تم التحقق منه حيًّا (Bot API + Vercel API + GitHub API)

| البوت | getMe | الويبهوك الحي | الحكم |
|---|---|---|---|
| @gu_mo_bot | «Gu Mo» ✓ | `gu-mo.vercel.app/api/webhook/gumo` (IP ضمن Vercel) ✓ | مطابق للأصل |
| @ens_reader_bot | «By me» ✓ | `…/api/webhook/reader` ✓ | سليم |
| @Gu_Yu_bot | «Gu Yu Yaoji» ✓ | `…/api/webhook/guyu` ✓ | سليم |
| @PaymonB_bot | «Binary» ✓ | `binary-agent.vercel.app/api/agent/webhook` — **مشروع قائم في حساب المالك نفسه** (أُنشئ اليوم لفصل الوكيل عن بوتات المجتمع) ✓ | سليم |

- **إعدادات Gu Mo الجانبية:** الاسم «Gu Mo»، الوصف «Sharp answers, zero fluff — real, substantive, confident.»، الوصف القصير والأوامر (`start/ping/status/help`) وزر القائمة — كلها منسجمة مع شخصية `sharp` الأصلية التي أعادها التزام 12:14.
- **الحراسة حيًّا:** POST على `/api/webhook/gumo` بلا سرّ → **401**، و`/api/agent/tick` بلا سرّ → **401**.
- **الإنتاج:** آخر نشر production READY يشغّل `b299841` = مزامنة التزام الاستعادة — أي أن الكود المستعاد هو الذي يجيب على تيليجرام الآن.
- **الوكالة:** `src/lib/agent/config.ts` يثبّت الوكيل = بيناري حصرًا، ونصّ صريح: «Gu Mo ليس الوكيل».
- **الوصول:** المتعاونون على المستودعات الثلاثة (Gu-mo، telegram-ai-community، agent-memory) = **المالك وحده**.
- **ذاكرة الوكيل (agent-memory):** المالك مقيّد على أول محادثة خاصة — اسم العرض الظاهر «Reverend insanity»، والنشاط كله تذكيرات عربية صباحية بتوقيت الجزائر (نمط استخدام يوحي أنه صاحب المالك نفسه — **يرجى من المالك تأكيد أن هذا اسمه هو**). سجل الموافقات نظيف: إجراء اختبار ذاتي واحد مرفوض.

## 4) المخاطر الحقيقية التي وجدناها (وهي ليست اختراقًا — لكنها الباب المفتوح له)

1. **التوكنات والأسرار كلها حرفية في `config.ts` داخل مستودع Gu-mo *العام*:** توكنات البوتات الأربعة + `WEBHOOK_SECRET` + `AGENT_TICK_SECRET`. أي غريب يقرأ المستودع يستطيع فعليًا التحكم بالبوتات (تغيير ويبهوك، انتحال الردود) — هذا هو السبيل الواقعي لأي «اختراق» مستقبلي. الكود مصمم ليُفضّل متغيرات البيئة إن وُجدت (`process.env.BOT_TOKEN_GUMO || '…'`)، فالإصلاح آمن ومنطقي.
2. **أسرار دُوّنت في المحادثة** خلال الجولات الأخيرة (توكن Vercel كامل الحساب، مفاتيح Supabase الثلاثة الجديدة، كلمة مرور قاعدة البيانات) — تحتاج تدويرًا من المالك.
3. **pollinations (بلا مفتاح)** في نهاية سلسلة الـ LLM يرسل نص محادثات الطلاب إلى خدمة طرف ثالث مجانية عند فشل المزودات الأولى — قرار خصوصية يعود للمالك (إبقاؤه كصمّام أمان أو حذفه).

## 5) خطة التدبير الموصى بها (على المالك — لا يمكن للوكيل تنفيذها وحده)

1. **تدوير توكنات البوتات من BotFather** (لا يوجد API للسحب): `/mybots` → اختر البوت → API Token → Revoke current token — للأربعة (@gu_mo_bot، @ens_reader_bot، @Gu_Yu_bot، @PaymonB_bot).
2. ضع التوكنات الجديدة في **Vercel env**: `BOT_TOKEN_GUMO` و`BOT_TOKEN_READER` و`BOT_TOKEN_GUYU` و`AGENT_BOT_TOKEN` (production/preview/development) — الكود يلتقطها تلقائيًا دون أي تعديل.
3. **بعد التأكد من عمل البوتات**، احذف القيم الحرفية من `src/lib/community/config.ts` و`src/lib/agent/config.ts` (التزام تنظيف جاهز للوكيل متى طُلب).
4. دوّر `WEBHOOK_SECRET` و`AGENT_TICK_SECRET` (Vercel env) وأعد تسجيل الويبهوك بالسر الجديد (`setWebhook` مع `secret_token`).
5. دوّر أسرار المحادثة: توكن Vercel، مفاتيح Supabase الثلاثة، كلمة مرور قاعدة البيانات.
6. قرّر مصير pollinations (إبقاء/حذف) — سطر واحد في `src/lib/community/ai.ts`.

## 6) قرار الجولة

لا تعديل كود — كل ما طلبه المالك («استعادة الإعدادات الأصلية») منعش في المستودع المصدر والمستضيف **ومفعّل على الإنتاج ومتحقق منه حيًّا**. بنود القائمة المؤجلة سابقة (الرتبة العليا «Transcendent»، لوحات الأسماء في الفوج، مناقشة تطوير بوت تيليجرام) ما زالت قائمة للجولات القادمة.
