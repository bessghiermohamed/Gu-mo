# تقرير إصلاحات الجولة 38 — قبول الفوج يظهر فوراً + الرفع إلى Drive أولاً

**التاريخ:** 6 سبتمبر 2026
**النطاق:** جلسة المستخدم (مزامنة حيّة) + حوار نشر الملفات — طلبان من المالك.

---

## ١. الطلب الأصلي

1. **قبول الفوج لا ينعكس على حساب الطالب:** بعد قبول الممثل طلب الانضمام، اسم الفوج يظهر في خانة «الفوج» في حسابي (لأنها تقرأ من API الحيّ)، لكن زر **«تصفح المجموعات والأفواج»** يبقى ظاهراً — تناقض محير: الحساب يقول «أنت في الفوج» والزر يقول «لم تُلحق بعد»، ولا يختفي الزر إلا بعد تحديث الصفحة كاملة.
2. **الرفع إلى Drive:** «أنا أستعمل حسابي لفتح وحدة التخزين، وعند رفع ملف في التطبيق لا يُرفع مباشرة إلى Google Drive — يجب أن يكون زر رفع الملفات هو المعروض، لا الرفع إلى خادم Supabase. حسابي على Drive مربوط».

## ٢. قبول الفوج يظهر فوراً — مزامنة الجلسة في ثلاث مواضع

**السبب الجذري:** جلسة المستخدم في المتصفح (`scope_cohort_group_id` وغيره) كانت تُقرأ مرة واحدة عند تسجيل الدخول فقط. قبول الطالب يحدث في **جلسة الممثل** ويعدّل قاعدة البيانات — وجلسة الطالب تبقى قديمة إلى تحديث يدوي.

الحل — تحديث الجلسة في اللحظات التي يعلم فيها الطالب بالقبول:

- **فتح الجرس 🔔:** لحظة قراءة إشعار «تم قبول طلبك — أنت الآن عضو في الفوج…» تُزامن الجلسة فوراً.
- **فتح حسابي:** كل مرة تُفتح الشاشة تُحدث الجلسة مرة واحدة (حارس مرجعي يمنع التكرار اللانهائي لأن تحديث الجلسة يغيّر كائن المستخدم نفسه).
- **العودة إلى التبويب:** عند تنشيط التبويب من جديد (`visibilitychange`) تُزامن الجلسة (مقيّدة: مرة كل ١٥ ثانية كحد أدنى) — تغطية عامة لأي تغيير إداري (قبول، ترقية، نزع إشراف) بلا أي تحديث يدوي.

**نتيجة التحقق الحقيقي:** طالبة تابعة في تبويب حيّ، المالك قَبِل طلبها من لوحة الإشراف في جلسة أخرى — الطالبة فتحت الجرس ثم حسابي **بلا أي إعادة تحميل**: الزر اختفى، و«الفوج 01» ظهر مرتبطاً بحسابها (تحقق قاعدة بياناتياً أيضاً: `scope_cohort_group_id = 21`).

## ٣. «رفع ملف (Drive)» هو الزر الافتراضي في نشر الملفات

حوار «إضافة ملف» (المكتبة + المواد داخل المقياس) كان يفتح على وضع **«رابط خارجي»** — فيبدو كأن التطبيق يخزّن الروابط في خادمه ولا يرفع ملفات فعليّة. التوضيح المهم: **الرفع الحقيقي إلى Drive كان موجوداً ويعمل** (من الجولة 32: الملف ينطلق من المتصفح إلى مجلد «📚 مكتبة طالب» في حساب Drive الخاص بالرافع — ١٥ ج.ب — ثم يُشارك «أي شخص لديه الرابط» وينزّله الطلبة مباشرة من Drive، وSupabase يحفظ صف بيانات وصفيّاً فقط، **صفر بايتات ملفات**)، لكنه كان الخيار الثاني المخفيّاً.

- الآن الحوار **يفتح على «رفع ملف (Drive)»** مباشرة، والزرّان مبدّلان: الرفع أولاً والرابط ثانوياً.
- داخل وضع الرفع: لو لم يكن Drive مربوطاً تظهر بطاقة «ربط Google Drive» بضغطة واحدة (نفس إذن سحابتي، نطاق `drive.file` — التطبيق لا يرى إلا ما يرفعه هو).
- ملاحظة تحقق: مفتاح `NEXT_PUBLIC_GOOGLE_CLIENT_ID` **مثبّت فعلاً في نشر Vercel الحالي** (تحقق من حزمة JS المنشورة) — أي أن الرفع إلى Drive يعمل على الإنتاج اليوم كما هو.

**ما لم يتغير:** تبويب سحابتي (رفع ملفات خاصة إلى Drive)، وصورة الجدول (محلية للجلسة فقط) كما هي.

## ٤. التحقق

- **tsc** بلا أخطاء، **eslint** نظيف على الملفات الأربعة المتغيرة، **بناء الإنتاج** ناجح.
- **جولة متصفح حقيقية 390×844 بجلسة متوازية (مالك + طالبة):**
  1. الطالبة تسجّل وتهيّئ وتُرسل طلب انضمام لـ«الفوج 01» (حالتها: زر التصفح ظاهر + «بلا فوج»).
  2. المالك في جلسة أخرى يقبل الطلب من لوحة الإشراف ← قاعدة البيانات تؤكد الإلحاق.
  3. الطالبة **دون تحديث الصفحة**: الجرس يُظهر «تم قبول طلبك» ← حسابي: **الزر اختفى** + «الفوج 01» في الخانة.
  4. حوار «إضافة ملف» يفتح على «رفع ملف (Drive)».
  5. `scrollWidth = 390` — لا فيض أفقي، لا أخطاء كونسول.
- **تنظيف:** حسابا التجربة والطلب محذوفان (users: 0, join_requests: 0) والهيكل سليم.
- **ملاحظة تشغيل أثناء التحقق:** خادم قديم كان متمسكاً بالمنفذ 3000 ويقدّم حزم قديمة فأحدث خطأ عميل — قُتل بـ`next-server` (نفس مصيدة الجولة 33) وأُعيد التشغيل.
