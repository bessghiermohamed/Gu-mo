# تقرير الجولة 28 — الموقع العام المُهيّأ لـ Google AdSense

**التاريخ:** ٤ سبتمبر ٢٠٢٦ · **النوع:** موقع عام + تهيئة إعلانية · **الحالة:** مكتملة

---

## المشكلة

انتهت الجولة السابقة إلى حكم قاطع: الموقع **غير جاهز** لمراجعة AdSense —
التطبيق SPA خلف جدار تسجيل الدخول، وHTML الخام للزائر المجهول لا يحوي سوى
«جاري التحميل...» (١٥ حرفاً). مراجعة Google كانت سترفضه حتماً بذريعة
«محتوى منخفض القيمة».

## الحل المطبق

بناء **موقع عام كامل مُصيَّر من الخادم** (SSG) كبطاقة المراجعة، مع إبقاء
التطبيق كما هو خلف مسار `/app`:

| البند | التفاصيل |
| --- | --- |
| نقل التطبيق | `src/app/page.tsx` → `src/app/app/page.tsx` (استيرادات `useShell` وأخواتها حُدّثت في ٨ ملفات) |
| صفحة الهبوط `/` | Hero بصورة موجودة + ٨ بطاقات مميزات + لقطات حقيقية للتطبيق + ٣ خطوات + معاينة أسئلة + CTA — ٧٢٢ كلمة قابلة للفهرسة |
| صفحات المحتوى | `/features` (٨٠٠ كلمة) · `/guide` (٥٩٨) · `/faq` (٦٩٧) · `/about` (٤٣٤) · `/contact` (٢٧٩) |
| الصفحات القانونية | `/privacy` (٨٠٧ كلمة) ببنود AdSense الكاملة (ملفات ارتباط Google، DoubleClick، خيار إيقاف الإعلانات المخصصة، بند القاصرين) · `/terms` (٤٩٩) |
| SEO | `sitemap.xml` ديناميكي + `robots.txt` جديد (يسمح للروبوتات، يمنع `/app` و`/ads-test` و`/api/` + مرجع الخريطة) + canonical لكل صفحة + `metadataBase` + OG/Twitter |
| بيانات منظمة | JSON-LD: `WebSite` + `SoftwareApplication` في الرئيسية، `FAQPage` كامل في صفحة الأسئلة |
| مسارات إعلانية | وحدات Mimo حقيقية (بدون وضع الاختبار) في الرئيسية والمميزات والدليل — تُفعَّل تلقائياً بعد الموافقة |
| التصميم | نفس نظام هوية التطبيق: أخضر مريمي/كريمي/برونزي، خط القاهرة، RTL، `<details>` بدون JS للأسئلة |

**الإجمالي: ~٤٨٠٠ كلمة محتوى عام قابل للفهرسة** (كان ١٥ حرفاً).

## التحقق (إنتاج حقيقي + متصفح ٣٩٠×٨٤٤)

- `tsc --noEmit`: صفر أخطاء · البناء أخضر، كل الصفحات العامة **مُصيَّر مسبقاً (static)**
- curl: `google-adsense-account` meta + سكربت AdSense + canonical + JSON-LD كلها في HTML الخام
- `/ads.txt` يخدم السطر الصحيح · `/robots.txt` و`/sitemap.xml` يخدمان صحيحين
- متصفح حقيقي ٣٩٠px: لا تجاوز أفقي، كل الصور تُحمَّل (lazy تحت الطية)، SPA في `/app` يفتح شاشة الدخول سليمة
- لقطات في `download/verify-site/r28-*.png`

## المطلوب من المالك

1. **اضبط `NEXT_PUBLIC_SITE_URL`** في Vercel إن كان لديك نطاق خاص (الافتراضي: `gu-mo.vercel.app`)
2. **أكّد بريد التواصل** في `src/lib/site.ts` (`CONTACT_EMAIL`) — وضعتُ بريداً مبدئياً
3. بعد نشر التحديث: أرسل الموقع للمراجعة في AdSense، وأضفه في Search Console (سيسحب الخريطة تلقائياً)
