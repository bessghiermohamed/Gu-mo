# تقرير إصلاحات الجولة 37 — المسار للمالك فقط + اختصار اسم المؤسسة

**التاريخ:** 6 سبتمبر 2026
**النطاق:** شاشة حسابي + حارس خادم في تهيئة الحساب — جولة قصيرة من طلبين محددين.

---

## ١. الطلب الأصلي

ورد من المالك طلبان:

1. **«تغيير المسار» للمالك فقط:** الجولة 36 أتاحت زر «تغيير المسار الأكاديمي» لكل الأدوار. القرار الجديد: تبديل المؤسسة/التخصص/السنة يبقى **سلطة مالك المنصة** — لا يجوز أن ينقل الطالب نفسه بين التخصصات والمجموعات من دون إشراف.
2. **اختصار اسم المؤسسة في حسابي:** الأسماء الرسمية الطويلة («المدرسة العليا للأساتذة - بوزريعة») تُقتطع في الخانة الصغيرة بشرطة «…» في منتصف الكلمة، فتضيع الكلمة المميِّزة التي يتعرف عليها الناس — **اللقب الأخير**. والاختصار نفسه مطلوب أن يعمل أيضاً مع الأسماء المكتوبة **بالأحرف الإنجليزية**.

## ٢. «تغيير المسار الأكاديمي» متاح للمالك فقط — ثلاث طبقات

- **الزر نفسه (واجهة حسابي):** يُعرض الآن بشرط `role === "OWNER"` — يختفي تماماً عن الطالب والممثل ومشرف التخصص.
- **حارس الصدفة (`startPathChange`):** لو استُدعي من أي مسار آخر، لا يفتح وضع «التغيير» إلا للمالك.
- **حارس الخادم (`POST /api/onboarding/complete`):** وضع «التغيير» يُرسل الآن حقل `mode: "change"` صريحاً من شاشة التهيئة؛ الخادم يرفض أي طلب `mode="change"` من غير المالك بـ **403** ورسالة «تغيير المسار الأكاديمي متاح للمالك فقط» — فحتى عميل مُلاعب لا يستطيع تنفيذ التبديل.

**ما لم يتغير:** التهيئة الأولى (`mode="initial"`) تبقى مفتوحة لكل الأدوار كما هي — الجهاز الجديد يعيد إكمال الملف بحرية، وقاعدة الجولة 9 (الحفاظ على عضوية المجموعة عند إعادة التهيئة) باقية.

## ٣. اختصار اسم المؤسسة — «اللقب الأخير» يبقى ظاهراً

دالة جديدة `src/lib/abbreviate.ts` → `abbreviateOrgName()` تُطبَّق على خانة «المؤسسة» في بطاقة الهوية الأكاديمية، والقاعدة: **الاسم القصير يبقى كما هو، والطويل (أكثر من 16 حرفاً) يُختصر**:

- **أسماء عربية:** حرف أول كلمة النوع + الكلمة الأخيرة المميِّزة:
  - «ثانوية الشهيد محمد بوصوف» ← **ث. بوصوف**
  - «المدرسة العليا للأساتذة - بوزريعة» ← **م. بوزريعة** (يُتجاهل سابقة «ال» عند أخذ الحرف الأول)
  - «ثانوية دالي إبراهيم (القرار 105)» ← **ث. إبراهيم** (المتعلقات بين قوسين لا تُختار أبداً كلقب)
- **أسماء بالأحرف الإنجليزية:** أوائل الكلمات + اللقب:
  - «Lycée Frères Bousouf» ← **L.F. Bousouf**
  - «École Normale Supérieure de Bouzaréah» ← **ENSD Bouzaréah** (مع تجريد التشكيل اللاتيني É←E)
- **مختلط:** يُعامل معاملة العربية («ENS بوزريعة» القصير يبقى كما هو لأنه لا يتجاوز الحد).

**الاسم الرسمي الكامل لا يضيع:** خزَّن في خاصية `title` للخانة (يظهر عند التمرير بالمؤشر)، والاختصار مسألة عرض فقط — البيانات في قاعدة البيانات كما هي بلا أي تعديل على المخطط.

## ٤. التحقق

- **اختبارات الدالة:** 12 حالة (عربية/لاتينية/مختلطة/قوسين/فراغات) — كلها ناجحة (`scripts/test-abbreviate.ts`).
- **tsc** بلا أخطاء، **eslint** نظيف على الملفات الخمسة المتغيرة، **بناء الإنتاج** ناجح والصفحات العامة ما تزال ثابتة.
- **جولة متصفح حقيقية 390×844** (فرع Prisma المحلي):
  - مالك جديد: تسجيل ← تهيئة ← حسابي **يظهر فيه الزر** + المؤسسة معروضة «م. بوزريعة» والكامل في الـtitle ← فتح «تغيير المسار»: لافتة الوضع، المؤسسة الحالية موسومة «مُحدد»، «إلغاء والعودة إلى حسابي» يعمل.
  - طالبة جديدة: تسجيل ← تهيئة أولية تنجح (`initial` غير مقيّد) ← حسابي **بلا زر تغيير المسار** + مؤسستها معروضة «ج. للآداب» والكامل في الـtitle.
  - **حارس الخادم بالتجربة:** طلب `mode:"change"` بجلسة الطالبة ← **403** «تغيير المسار الأكاديمي متاح للمالك فقط»؛ نفس الطلب بجلسة المالك بقيمه الصحيحة ← **200 ok**.
  - بلا أي خطأ في الكونسول، `scrollWidth = 390` — لا فيض أفقي.
- **تنظيف:** حسابا التجربة محذوفان (`users: 0`) والهيكل المرجعي سليم (مؤسستان/تخصصان/10 سنوات).
