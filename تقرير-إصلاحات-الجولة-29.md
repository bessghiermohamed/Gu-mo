# تقرير الجولة 29 — مسار إعلاني قابل للتغيير، أدوات جديدة، وإزالة حاسبة الطالب

**التاريخ:** ٤ سبتمبر ٢٠٢٦ · **النوع:** ميزات + تنظيف واجهة · **الحالة:** مكتملة

---

## ١. بريد التواصل محدَّث

`besseghiermohamed719@gmail.com` أصبح المرجع الوحيد في `src/lib/site.ts` —
يسري تلقائياً على صفحتي الاتصال وسياسة الخصوصية.

## ٢. صفحة اختبار الإعلانات: مسار يتحكم فيه المالك

حُذف المسار الثابت `/ads-test` كما طلبت، وحلّ محله نظام جديد:

| المتغير | الوظيفة | القيمة الافتراضية |
| --- | --- | --- |
| `ADS_TEST_PATH` | مسار صفحة الاختبار — غيّره متى شئبت من إعدادات Vercel دون لمس الكود، والمسارات القديمة تُصبح 404 تلقائياً | `ads-test` |
| `ADS_TEST_REAL` | اجعله `true` لتظهر إعلانات حقيقية (بعد موافقة Google) بدل لافتة «إعلان تجريبي» | فارغ |

**كيف يعمل:** مسار ديناميكي `src/app/[slug]/page.tsx` يقارن المسار المطلوب
بقيمة `ADS_TEST_PATH` — تطابق → صفحة الاختبار (مع `noindex`)، أي مسار آخر →
404 العربية المعتادة. `robots.txt` يمنع نفس المسار المضبوط (يتجدد مع كل
إعادة نشر يطلقها تغيير المتغير في Vercel).

**تم التحقق عملياً:** بالقيمة الافتراضية `/ads-test` = 200 و`/أي-شيء` = 404؛
ثم بـ`ADS_TEST_PATH=check-7f2k`: `/ads-test` صار 404 و`/check-7f2k` صار 200،
وكل صفحات الموقع العام سليمة.

## ٣. إزالة «حاسبة الطالب» + مسار جديد للأدوات

- **حُذفت** شاشة حاسبة الطالب كاملة (الملف + المسار `#/grades` + بلاطة
  الرئيسية + مفاتيح الترجمة) — بناءً على طلبك.
- **البديل:** الحاسبة الحقيقية الآن هي «حاسبة المعدل» داخل أدواتي — وهي
  أقوى أصلاً (مقاييس تخصصك بمعاملاتها الحقيقية + محاكاة «ماذا لو») وتقرأ
  وتكتب نفس مخزن `talib-grades`، فمعدلك المحفوظ يظهر فوراً في بطاقة
  الرئيسية كما كان.
- **بلاطة جديدة في الرئيسية:** «أدواتي — حاسبة، PDF، عدّاد ومؤقت» تفتح
  ملفاتي مباشرة على تبويب الأدوات (مسار `#/tools` قابل للمشاركة والاسترجاع
  بعد التحديث).

## ٤. ثلاث أدوات جديدة (الأدوات الآن ٧)

| الأداة | الوصف |
| --- | --- |
| **استخراج صفحات PDF** | شارك فقط الصفحات المطلوبة من ملف ضخم: اختر الملف، اكتب المجالات (1-3, 5, 8-10 — يدعم الأرقام العربية ٢-٤ أيضاً)، وانتج PDF جديداً. التحقق من الصيغ والأرقام الخارجة عن النطاق برسائل عربية واضحة |
| **عدّاد الكلمات** | كلمات/أحرف/جمل/فقرات + زمن قراءة تقديري، تتحدث فورياً أثناء الكتابة. يستبعد التشكيل حتى لا تتضخم الأرقام، مع أزرار لصق/نسخ/إفراغ |
| **مؤقّت المراجعة** | بومودورو كامل: حلقة تقدم، جلسات تركيز واستراحات قابلة للضبط، استراحة طويلة بعد كل ٤ جلسات، تنبيه صوتي، وعدّاد «تركيزك اليوم» المحفوظ محلياً. يعمل بطوابع زمنية فلا يتأخر ولو غيّرت التبويب |

جميعها تعمل داخل الجهاز دون إنترنت ودون رفع أي ملف — نفس ضمانة الخصوصية
المعلنة في تبويب الأدوات.

## ٥. توضيح حدود الصناديق

بعد تسطيح الجولة ٢٧ صارت البلاطات بلا حدود واضحة — أُضيفت حدود خفيفة
(`border`) بنفس لغة التصميم لكل من:

- بلاطات الخدمات الثماني في الرئيسية (مع تأكيد الحد عند التحويم)
- بطاقة «دروس تيليجرام» وبانرَي طلب الانضمام/لا فوج (حدود بلون كل واحدة)
- تلميح الترحيب، وحصص الجدول الرسمية (كانت الشخصية وحدها ذات حد)

## التحقق (٣٩٠×٨٤٤، متصفح حقيقي)

جولة كاملة عبر تسجيل حساب جديد → الإعداد → إنهاء الجولة التعريفية → بلاطة
أدواتي (`#/tools`) → فتح الأدوات السبع واحدة واحدة: عدّاد الكلمات أعطى
أرقاماً صحيحة لنص تجريبي (١١ كلمة / ٣ جمل)، المؤقّت نقر فعلاً (٢٤:٥٧ بعد
٣ ثوانٍ)، حاسبة المعدل فتحت بمقاييس التخصص الحقيقية — والوضع الليلي سليم
بلا أي تجاوز أفقي ولا أخطاء في الكونسول. لقطات في
`download/verify-390/r29-*.png`، وحساب الاختبار مُحذف بعد التحقق.

## المطلوب من المالك

1. في Vercel: اضبط `ADS_TEST_PATH` على مسار من اختيارك (وأضفه أيضاً في
   `.env` المحلي عند التجربة) — راجع `.env.example`
2. بعد موافقة AdSense: اجعل `ADS_TEST_REAL=true` لتجربة الإعلانات الحقيقية
