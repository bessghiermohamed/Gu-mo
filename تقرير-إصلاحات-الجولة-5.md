# تقرير إصلاحات الجولة 5 — طبقة التعديل المفقودة + تحصين الحذف

**التاريخ:** 2026-09-01 · **النطاق:** منطق أزرار الحذف والتعديل عبر المشروع كاملاً

## الخلاصة

الفحص المنطقي الشامل أكّد عيب التصميم الذي أبلغ عنه المالك: التطبيق كان يملك **إنشاء + قراءة + حذف** لكل العناصر، لكن **لا يوجد أي مسار تعديل (PATCH) في الـ API بأكمله** (صفر نقاط نهاية PATCH/PUT من بين 39 ملف مسارات). اجتمع هذا مع حراس الحذف (الذين يمنعون حذف سنة فيها مجموعات/أفواج/مقاييس، أو فوجاً فيه طلبة) في **مأزق منطقي**: بمجرد استخدام العنصر يصبح اسمه مجمّداً — لا يمكن حذفه ولا تعديله من الواجهة. الإعلانات كانت الحالة القصوى: لا حذف ولا تعديل إطلاقاً — الخطأ المنشور دائم.

كشف الفحص أيضاً ثغرة أمنية من فئة IDOR: حذف الاختبارات والجدول (وأيضاً السنوات/المجموعات/الأفواج) كان يتحقق من الدور فقط دون التحقق من ملكية السجل، فأي مشرف يستطيع حذف بيانات تخصص آخر بتزوير المعرّف — نفس فئة الثغرة (أ.3/أ.4) التي أُصلحت للقراءة في الجولة 3 وبقيت في الحذف.

## الإصلاحات المنفذة (10 ملفات)

### 1. إضافة طبقة التعديل — 6 نقاط PATCH جديدة
| المسار | ما يمكن تعديله |
|---|---|
| `PATCH /api/years` | اسم السنة + السداسي (حتى مع وجود تبعيات — يفكّ المأزق) |
| `PATCH /api/cohort` | اسم الفوج + القسم الفرعي (حتى مع طلبة ملحقين) |
| `PATCH /api/groups` | اسم المجموعة + الوصف |
| `PATCH /api/announcements` | العنوان/المحتوى/الأهمية (حسب الأهلية) |
| `PATCH /api/exams` | العنوان/التاريخ/الوقت/القاعة/المعامل/المقياس |
| `PATCH /api/schedule` | اليوم/الأوقات/المقياس/النوع/القاعة/الأستاذ |

كل PATCH يتحقق من: الدور ← نطاق التخصص (غير المالك) ← مدخلات غير فارغة ← منع تكرار الأسماء (409).

### 2. حذف/تعديل الإعلانات (كان مستحيلاً)
- `DELETE /api/announcements` + `PATCH`: المالك أيّ إعلان؛ مشرف التخصص: إعلانات تخصصه؛ الممثل: إعلاناته المؤلفة فقط.
- تنظيف `notification_read_states` عند الحذف حتى لا يختلّ عدّاد غير المقروء.

### 3. إغلاق ثغرة IDOR للحذف عبر التخصصات
كل عمليات الحذف (سنوات/مجموعات/أفواج/اختبارات/جدول) + كل PATCH تتحقق الآن أن السجل المستهدف ينتمي لتخصص المتصل (المالك مستثنى).

### 4. توحيد تجربة الحذف
استُبدل `window.confirm()` الأصلي (أزرار متصفح إنجليزية) بمربعات حوار عربية موحدة في: المجموعات (لوحة الإدارة)، الاختبارات، والجدول.

### 5. تنظيف المراجع المعلّقة
- حذف مجموعة يصفّر `app_users.scope_group_id` المعلّق.
- الفرع المحلي (Prisma) أصبح يطبّق نفس حراس الفرع السحابي (فحص الفoog داخل المجموعة + فحص النطاق).

### 6. الواجهة
أزرار تعديل (أيقونة قلم) + مربعات حوار تعديل في: لوحة الإدارة (السنوات/الأفواج/المجموعات)، شاشة الاختبارات، شاشة الجدول، وشاشة الإعلانات (تظهر لمن يسمح له الخادم فقط).

## التحقق
- `bun run lint`: نظيف ✓
- بناء إنتاج نظيف (clean-room build) ✓
- جناح القبول: **72/72** (48 اختباراً سابقاً + 24 اختبار جولة 5 تغطي: إعادة التسمية أثناء وجود التبعيات، رفض التكرار 409، رفض العابر للتخصصات 403 على الحذف والتعديل، أهلية الإعلانات، وصلاحيات الطلبة)

## مؤجّل (قرارات منتج)
- تعديل المؤسسات/التخصصات/الملامح/المقاييس (استخدام منخفض؛ سنة قادمة)
- ترحيل حقل `author` في الإعلانات من نصّ الاسم إلى `author_id` (مطابقة الاسم حالياً عملية مقبولة)
- RLS على مستوى قاعدة البيانات
