# تقرير الجولة ٤٣ — أدوات أذكى: ضغط الصور، صورة إلى نص، والمساعد الذكي

**الطلب الأصلي (مترجَم):** «أضف العناصر المفيدة إلى أدواتي، وأحضر أيضاً المشاريع التي تقدم قدرات ذكاء اصطناعي.» — تنفيذاً لبحث GitHub في الجولة السابقة (مكتبات مرخّصة MIT/Apache تم التحقق من تراخيصها ونشاطها).

## ما نُفِّذ — أدواتي من ٧ أدوات إلى ١٠

### ١. ضغط الصور (جديدة، تعمل داخل الجهاز)
- مكتبة `browser-image-compression` (MIT) داخل Web Worker — الضغط لا يجمّد الواجهة.
- ثلاث درجات (خفيف/متوازن/قوي) بنفس نمط «ضغط PDF»، حتى ٢٠ صورة في المرة، وحساب التوفير الإجمالي.
- **قاعدة النتيجة الصادقة** (نفس مبدأ ضغط PDF): إن لم تَصغُر الصورة (أصلاً خفيفة) تُوسَم «الأصل أفضل» ويبقى زرا المشاركة/التنزيل على الأصل.
- الاستخدام الطبيعي: تصغير صور السبورة والوثائق قبل إرسالها للمجموعة أو رفعها إلى Drive.

### ٢. صورة إلى نص — OCR (جديدة، تعمل داخل الجهاز)
- مكتبة `tesseract.js` (Apache-2.0، محرك WASM) — الطالب يصوّر السبورة أو الورقة فيستخرج نصاً قابلاً للنسخ.
- أربع لغات: عربية، عربية+إنجليزية (الأنسب لصور السبورات المختلطة)، فرنسية، إنجليزية.
- **خصوصية صادقة ومعلَنة**: الصورة نفسها لا تغادر الجهاز إطلاقاً؛ فقط محرك القراءة وبيانات اللغة تُحمَّل مرة واحدة من CDN عام (~بضعة ميغا) ثم تُخزَّن في ذاكرة المتصفح — والملاحظة مكتوبة في الشاشة نفسها.
- شريط تقدم بمراحل عربية («تحميل المحرك مرة واحدة»، «قراءة الصورة…»)، نتيجة قابلة للنسخ أو التنزيل كملف نصي أو المشاركة مباشرة.
- تنظيف تلقائي للأسطر الفارغة الزائدة، ورسالة عربية واضحة إن لم يُعثر على نص واضح.

### ٣. المساعد الذكي (جديدة — أول أداة ذكاء اصطناعي في المنصة)
- أربع مهام دراسية مركزة بدل محادثة حرة فوضوية: **لخّص لي / اشرح ببساطة / اختبرني / اسأل حرّاً** (الأسئلة الحرة تقبل نصاً داعماً اختيارياً).
- **API جديد `/api/ai`** بلا أي SDK ثقيل — استدعاء REST مباشر بسلسلة مزوّدين:
  1. `GROQ_API_KEY` → llama-3.3-70b-versatile (المستوى المجاني: 14,400 طلب/يوم، الأسرع)
  2. `GEMINI_API_KEY` → gemini-2.0-flash (احتياطي مجاني)
  3. لا مفتاح → `200 {needsConfig:true}` — **نفس اصطلاح `needsSchema`** المعروف في المنصة: البطاقة تُعرض بدل الخطأ الخام.
- حمايات: تسجيل دخول إلزامي، حد ٨٠٠٠ حرف للنص، حد يومي ٦٠ طلباً/IP + فاصل ٥ ثوانٍ بين الطلبات (في الذاكرة، أفضل جهد على Serverless — يمنع الحلقات والانفجارات)، مهلة ٤٥ ثانية، والمطالبات (prompts) عربية بالكامل مع تعليمات بعدم اختلاق المعلومات.
- **خصوصية معلنة**: النص الملصق يُرسل لطلب واحد ثم يُنسى — لا يُخزَّن في أي قاعدة بيانات، ولا يظهر لأحد.
- **بطاقة تفعيل حسب الدور**: عند غياب المفتاح يرى المالك تعليمات الإعداد (متغير البيئة في Vercel) بينما يرى الطلبة «غير مفعّل بعد — قريباً» — لا أحد يرى رسائل تقنية.
- لغوياً: تمييز بصري كامل (بنفسجي + شارة «جديد» + شارة «يحتاج إنترنت») حتى لا تختلط الأداةُ الأونلاين بوعود الأدوات المحلية، وتحذير دائم «راجع المعلومات العلمية مع مصدرك».

### ٤. لمسات مصاحبة
- عنوان بطاقة أدواتي في الرئيسية صار: «حاسبة، PDF، صورة إلى نص، مساعد ذكي».
- كرتا الأداتين الجديدتين المحليتين داخل نفس نمط البطاقات القديم دون أي نمط تصميم جديد.

## التحقق
- `tsc --noEmit`: صفر أخطاء. ESLint نظيف على كل الملفات الجديدة. `next build`: أخضر (٦٨ صفحة).
- **اختبار وظيفي حي على خادم تطوير محلي** (`scripts/r43-api-probe.ts`)، خمس نقاط كلها خضراء:
  1. طلب مجهول إلى `/api/ai` → **401** برسالة عربية.
  2. تسجيل طالب اختبار → **200**.
  3. طالب مسجّل بلا مفتاح مزوّد → **200 {needsConfig:true}** (الاصطلاح يعمل).
  4. مهمة «اسأل» بلا سؤال → **400** برسالة عربية.
  5. حذف حساب الاختبار ذاتياً → **200**.
- **الأداة اكتشفت خطأً وأصلحته**: كان حد المعدل (429) يُطبَّق قبل فحص الصلاحية فتستنزف الطلباتُ غير الصالحة فترةَ التهدئة — صُحّح الترتيب: الفحص أولاً ثم العدّ (الطلبات غير الصالحة لا تحرق التهدئة).
- تنظيف كامل: حساب الاختبار حُذف؛ قاعدة SQLite المحلية والبيانات المزروعة المحلية خارج Git (`*.db` و`.env*.local` مُستثناة أصلاً).
- سكربتات الجولة محفوظة للمرة القادمة: `scripts/r43-api-probe.ts` و`scripts/r43-seed-min.ts` (بذر أدنى محلي: مؤسسة + تخصص + سنة).

## ملاحظة تشغيلية للمالك (تفعيل المساعد الذكي — مرة واحدة، مجاناً)
1. أنشئ مفتاحاً مجانياً في **console.groq.com** (الأسرع) أو **aistudio.google.com/apikey** (احتياطي).
2. أضفه في Vercel: Settings → Environment Variables → `GROQ_API_KEY` (و/أو `GEMINI_API_KEY`).
3. أعد النشر (أو أرسل تعديلاً) — البطاقة عند المالك والطلبة تتحول تلقائياً إلى الأداة العاملة.
- بدون المفتاح لا شيء يتكسر: الطلبة يرون بطاقة «قريباً» أنيقة فقط.

## القرارات
- مهام مركزة بدل محادثة حرة: أوضح للطالب، أرخص في الاستهلاك، وأسهل ضبطاً للجودة — المحادثة الحرة خطوة لاحقة إن طلبها الاستخدام.
- Groq أولاً ثم Gemini: كلاهما مجاني بلا بطاقة، والأول أسرع بكثير للعربية بجرعات قصيرة.
- OCR بمحرك من CDN بدل ضخ ميغابايتات في المستودع: أول تشغيل يحتاج إنترنت (ومعلوم في الواجهة)، ثم يعمل من الذاكرة.
