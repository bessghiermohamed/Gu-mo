# تقرير إصلاحات الجولة ٤١ — مواد المقياس في موقعها، لا في المكتبة

**الطلب الأصلي (مترجَم):** «ما هذا؟ مواد ودروس المقياس تُرفع إلى المكتبة — لماذا أنشأنا المقياس وقسم الرفع أصلاً إذا لم تكن الملفات ستنتهي إلى موقع المقياس نفسه؟»

**التشخيص:** كان للنظام تسريب حقيقي في مسار المواد — ملاحظة المالك صحيحة:
1. **تسريب القائمة:** واجهة «المكتبة» (ملفاتي) تعرض كل مراجع التخصص — بما فيها المواد المرفوعة من داخل مقياس بعينه. فالمادة المرفوعة لمقياس النحو كانت تظهر في المكتبة العامة وكأنها «ملف مكتبة».
2. **التخفي الصامت:** عند غياب عمود الربط `module_id` في قاعدة بيانات المُنتَج، كان خادم الـAPI يُدرج الصف **بدون** ربط بالمقياس بدل أن يفشل — فتذهب المادة إلى المكتبة العامة بصمت، ولا تظهر في تبويب مواد المقياس أبداً.
3. **Drive بلا هيكلة:** كل الرفع يذهب إلى مجلد واحد «📚 مكتبة طالب» في حساب Drive — فلا يميّز المالك ملفات مقياس من مقياس.

## ما نُفِّذ

### ١. مواد المقياس تعيش في المقياس فقط
- **GET /api/library** (قائمة المكتبة العامة): يستثني الآن أي مادة لها `module_id` — تظهر في تبويب «المواد» داخل المقياس **حصراً**. عند قاعدة بيانات لم تُحدَّث بعد يتراجع تلقائياً للسلوك القديم (بدون فلتر) كي لا تفارغ المكتبة.
- **POST /api/library** يصبح صارماً مع مواد المقاييس: إن غاب عمود الربط **لا يُدرج شيئاً** (لا معادة صامتة إلى المكتبة) بل يرجع `needsSchema` + مقطع SQL الجاهز. مادة المقياس إما تُربط بمقياسها أو لا تُحفظ أصلاً — أما رفع المكتبة العامة فيبقى متسامحاً كما كان.

### ٢. Drive يعكس هيكل التطبيق ١:١
- دالة جديدة `findOrCreateCourseFolder`: الرفع من داخل مقياس ينشئ/يستخدم مجلد **«📘 اسم المقياس»** تحت مجلد التطبيق «طالب — Talib» في Drive الخاص بالمشرف، مع وسم `moduleId` في خصائص المجلد. ملفات المكتبة العامة تبقى في «📚 مكتبة طالب».
- مثال: مقياس «النحو» ← مجلد «📘 النحو» في Drive؛ الطلبة ينزّلون من هناك مباشرة (مشاركة «أي شخص لديه الرابط») — صفر بايتات على Supabase.

### ٣. التحديث المطلوب لمرة واحدة صار ذاتي الخدمة
- بطاقة جديدة `NeedsSchemaCard` (في الحوار وفي تبويب المواد): عنوان «تحديث قاعدة بيانات لمرة واحدة مطلوب» + شرح أن «المادة لم تُحفظ (ولا تذهب إلى المكتبة العامة)» + مقطع SQL بزر **نسخ** + زر **«نفّذته — أعد المحاولة»** — يكمل الرفع بنقرة بعد تنفيذ المقطع، دون إعادة تعبئة النموذج.
- كشف غياب العمود في **الفرعين معاً** (Supabase وPrisma/SQLite) — كان الفرع المحلي يبتلع الخطأ بصمت قبلها.
- ملف SQL الجاهز: `download/supabase_course_materials.sql` (ثلاثة أعمدة + فهرس للفلتر).

### ٤. لغة الواجهة تساير الموقع
- زر الحفظ في الحوار: «نشر إلى **المقياس**» عندما يكون الرفع من داخل مقياس (كان «نشر إلى المكتبة» في كل الحالات).
- تذييل الحوار يذكر مجلد المقياس بالاسم: «يُرفع الملف إلى مجلد المقياس «📘 علم اللغة العام»… **لا علاقة له بالمكتبة العامة ولا Supabase**».
- توست النجاح: «أُضيفت المادة إلى مقياس «X» — في تبويب المواد ولدى كل الطلبة».
- تلميح بطاقة الرفع في رأس المقياس يذكر مجلد المقياس، وترحيب المكتبة الفارغة يوضح أن «ملفات المقاييس تُرفع من داخل المقياس نفسه فتظهر في مواده».

**ملاحظة الدروس:** الدروس ليست مرفوعة أصلاً — هي منشورات تيليجرام المرتبطة بالمقياس تلقائياً (بمجرد ربط القناة بالمقياس يظهر درسها في تبويب الدروس داخله). لم تمسّها المكتبة قط، ولا حاجة لأزرار رفع فيها.

## التحقق
- tsc: صفر أخطاء. `next build`: أخضر.
- **جولة متصفح حقيقية 390×844 (SQLite محلي):**
  - **الاستبعاد:** صفان اختباريان (مادة بمقياس + مرجع عام) → المكتبة عرضت «المرجع العام» **فقط**؛ تبويب مواد المقياس عرض المادتين المرتبطتين فقط (مع شارة «على Drive»).
  - **الدورة الذاتية كاملة:** حُذف عمود `moduleId` فعلياً من قاعدة SQLite (محاكاة المُنتَج غير المحدَّث) → بطاقة التحديث ظهرت في تبويب المواد وفي الحوار بالمقطع الكامل؛ محاولة حفظ مادة للمقياس **لم تُنشئ صفاً** (تحقق مباشر في القاعدة: 0 صفوف)؛ ثم أُعيد العمود (محاكاة تنفيذ المالك للـSQL) → «نفّذته — أعد المحاولة» حفظ الصف **مرتبطاً** (`moduleId=5`) بتوست «تمت إضافة المادة للمقياس».
  - **فصل الـAPI نهائياً:** `GET /api/library` ← الصف العام فقط؛ `GET /api/library?moduleId=5` ← مواد المقياس الثلاث فقط.
  - تذييل الحوار بوضع المقياس يعرض «مجلد المقياس «📘 علم اللغة العام»»، وزر الحفظ «نشر إلى المقياس». `scrollWidth=390`، صفر أخطاء console. لقطات في `download/verify-390-r41/`.
- **التنظيف:** حُذفت حسابات وبيانات التجربة كلها — users: 0، courses: 0، library: 0.
- **النشر:** دفع `caf8650..6ac2638` → chunk المُنتَج `6fa9452f237b1ea5.js` يحوي «نشر إلى المقياس» و«لا علاقة له بالمكتبة العامة» ووسم `talib-course` — الكود حيّ على gu-mo.vercel.app.

## خطوة تشغيلية واحدة (للمالك، لمرة واحدة)
كي تعمل روابط المقاييس على المُنتَج: افتح محرر SQL في Supabase ونفّذ محتوى `download/supabase_course_materials.sql` (نفس المقطع الذي تعرضه بطاقة التحديث داخل التطبيق مع زر نسخ). بعدها: أي رفع من داخل مقياس يظهر في مواده فوراً، ويظهر في Drive داخل مجلد المقياس، وتختفي من المكتبة العامة.
