# تقرير الجولة 44 — المساعد الذكي يصبح محادثة كاملة (+ إصلاح جذري لمشكلة «الخدمة مشغولة»)

## ما طلبته وما وجدته

ثلاث ملاحظات وردت هذه الجولة:

1. **«تعذّر الحصول على إجابة الآن — الخدمة مشغولة أو تجاوزت الحد»**: هذه هي الرسالة التي يراها الطالب فعلياً في المساعد الذكي. شخصتُ السبب الجذري في الكود المُنشر (ر43).
2. **مفتاحان بالخطأ — GROK و GROQ**: مفتاح Grok (يبدأ بـ `xai-`، من x.ai) وُضع في خانة GROQ_API_KEY. ليستا نفس الخدمة إطلاقاً.
3. **«لم تعجبني مبنتها إطلاقاً — لماذا لا نضيف فقاعات كلام وتجعلها بواجهة مثل ChatGPT و DeepSeek؟»** — وهذا ما بُني بالكامل.

## 🔍 التشخيص الحقيقي لخطأ «الخدمة مشغولة»

فحص `/api/ai` كما نُشر في الجولة 43 كشف أن «سلسلة المزوّدين» المذكورة في التعليقات **لم تكن موجودة في الكود أصلاً**:

```ts
// r43 — السطر الفعلي:
const answer = groqKey
  ? await callGroq(...)          // ← لو وُجد أي مفتاح GROQ ولو خاطئ
  : await callGemini(...);       // ← Gemini لا يُجرَّب أبداً عند فشل Groq!
```

- بمجرد وجود قيمة في GROQ_API_KEY (حتى لو كانت مفتاح Grok الخاطئ) يُستدعى Groq وحده، وعند فشله يُرجَع 502 مباشرة — **بدون تجربة Gemini أبداً**.
- كما كان النموذج `gemini-2.0-flash` مكتوباً بشكل ثابت، ومفحوصاً عملياً في خط تيليجرام أنه يرجع 404 مع مفتاحكم (النماذج العاملة المثبتة: `gemini-3.5-flash` و `gemini-3.1-flash-lite`).
- النتيجة: مزوّد خاطئ + نموذج خاطئ + لا يوجد fallback = خطأ دائم لكل طالب.

## 🛠️ الإصلاح الجذري (`src/lib/ai/providers.ts` — جديد)

**سلسلة حقيقية** تمشي محاولة-بمحاولة (Groq ← Gemini ← Grok) مع تصنيف لكل فشل:

| نوع الفشل | التصرف |
|---|---|
| مفتاح مرفوض 401/403 | تخطّي **كل** نماذج هذا المزوّد والانتقال للتالي |
| نموذج غير متاح 404/400 | تجربة النموذج التالي في السلسلة |
| تجاوز حد 429 | الانتقال للمزوّد التالي |
| خطأ خادم 5xx / انقطاع | تجربة النموذج التالي |

**التعرف الذكي على المفاتيح الخاطئة**: إن وُضع مفتاح `xai-` (Grok) في خانة GROQ_API_KEY بالخطأ — يكتشفه النظام تلقائياً ويوجّهه إلى x.ai كمزوّد ثالث صحيح. خطؤكم تحوّل إلى احتياطي إضافي بدل عطل. سلاسل النماذج: Groq (`llama-3.3-70b-versatile` ← `llama-3.1-8b-instant`)، Gemini (السلسلة المثبتة عملياً نفسها المستخدمة في التصنيف)، Grok (`grok-4-fast` ← `grok-3-mini` ← `grok-2-1212`)، وكلها قابلة للتجاوز بمتغيرات `GROQ_MODEL` / `GEMINI_MODEL` / `XAI_MODEL`.

**أخطاء عربية صادقة حسب السبب**: مفتاح مرفوض ← رسالة مختلفة للطالب، ولمالك المنصة تلميح تقني يشرح أي مفتاح يجب أن يبدأ بـ `gsk_` وأيهما بـ `xai-` وأيهما بـ `AIza`.

## 💬 الواجهة الجديدة — محادثة بأسلوب ChatGPT/DeepSeek

إعادة بناء كاملة لـ `ai-assistant-tool.tsx` (نفس نقطة الدخول في أدواتي):

- **فقاعات كلام**: رسالة الطالب بفقاعة بنفسجية على جهة النهاية، ورد المساعد بفقاعة رمادية هادئة مع صورة رمزية متدرجة — كلها بمنطق RTL صحيح.
- **شريط جانبي للمحادثات** (مثل ChatGPT): زر «محادثة جديدة»، قائمة بعناوين المحادثات السابقة مع وقت نسبي وزر حذف — على الحاسوب عمود ثابت، وعلى الهاتف درج منزلق من جهة البداية مع تعتيم خلفية.
- **بث حي للإجابة (Streaming)**: الرد يصل حرفاً بحرف مع مؤشر كتابة (ثلاث نقاط) ثم مؤشر نبض أثناء التوليد — نفس إحساس ChatGPT/DeepSeek. زر «إيقاف» يوقف التوليد ويحتفظ بالجزء الواصل.
- **تاريخ المحادثات على جهاز الطالب فقط** (localStorage، حتى 40 محادثة) — صفر حمل على قاعدة البيانات، والخصوصية كما هي: المحتوى لا يُخزَّن عندنا إطلاقاً.
- **Markdown كامل**: عناوين وقوائم وجداول وأكواد برمجية (react-markdown + remark-gfm، كلاهما MIT).
- **أزرار لكل رد**: نسخ، إعادة توليد لآخر رد، وزر «إعادة المحاولة» داخل فقاعة الخطأ بدل التوستات المفقودة.
- **المهام الأربع السابقة لم تُفقد**: لخّص لي / اشرح ببساطة / اختبرني / اسأل حرّاً أصبحت بطاقات اقتراحات في الشاشة الفارغة تعبّئ صندوق الكتابة تلقائياً.
- **حالة «غير مفعّل»** تحتفظ بمنطق ر43 (المالك يرى التعليمات، الطالب يرى «قريباً») مع إضافة تنبيه explaining GROK ≠ GROQ.
- **حدود استخدام عادلة للمحادثة**: 3 ثوانٍ بين الرسائل، 150 رسالة/يوم لكل حساب، آخر 20 رسالة تُرسل كسياق مع سقف 24 ألف حرف.

## ✅ التحقق (نفس سلسلة الجولات السابقة)

- `tsc --noEmit` → 0 أخطاء · eslint نظيف · `next build` أخضر (68 صفحة).
- مسبار وظيفي محلي (`scripts/r44-api-probe.ts` على `next start` + SQLite جاهزة): مجهول 401 ← تسجيل 200 ← رسائل فارغة 400 ← آخر رسالة مساعد 400 ← سيناريوهان حسب المفاتيح: بدون مفاتيح `{needsConfig:true}`، وبمفاتيح وهمية **تدفق SSE يمر بالسلسلة كاملة وينتهي بحدث خطأ عربي رشيق** (أثبت المشي الفعلي: مفتاح Groq الوهمي رُفض فتخطّي مزوّده كله، ثم جُرِّبت نماذج Gemini كلها) ← حذف ذاتي للحساب. ALL PASS.
- اختبار واجهة بمتصفح حقيقي (1280px و 390×844): الشاشة الفارغة + بطاقات الاقتراحات، إرسال رسالة، فقاعة الخطأ مع زر إعادة المحاولة، ظهور المحادثة في الشريط الجانبي بعنوانها، **بقاء المحادثة بعد إعادة تحميل الصفحة**، درج الهاتف المنزلق، وزر الرجوع — كلها مصوّرة ومؤكدة.
- تنظيف: حسابات الاختبار المحلية حُذفت بالكامل (0 مستخدمين في قاعدة التطوير).

## ⚡ خطوة واحدة متبقية لك (دقيقتان، مجانية)

المساعد سيعمل فوراً بعد تنظيف المفاتيح في Vercel ← Settings ← Environment Variables:

1. **GROQ_API_KEY** ← خذ مفتاحاً من **console.groq.com** (يبدأ بـ `gsk_`) — الأسرع والموصى به.
2. **GEMINI_API_KEY** ← من aistudio.google.com/apikey (موجود عندك ويشتغل مع هذه الجولة تلقائياً — لا تغيّره).
3. مفتاح **Grok** الذي لديك (`xai-`): إما وضعه في **XAI_API_KEY** ليصبح احتياطياً ثالثاً رسمياً، أو اتركه في GROQ_API_KEY والنظام سيتعرف عليه تلقائياً ويستخدمه عبر x.ai.
4. أعد النشر (Redeploy).

ما لم تفعله بالخطأ سابقاً كان «كسر» المساعد؛ بعد هذه الجولة أي تركيبة من مفاتيحك ستجد طريقها الصحيح.
