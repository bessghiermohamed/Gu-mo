# تقرير إصلاحات الجولة ٤٢ — إخراج أدوات التطوير من مسار بناء الإنتاج

**الطلب الأصلي (مترجَم):** «بناء الإنتاج يفشل بخطأ TypeScript في `scripts/r41-prod-verify.ts:13:7 — Cannot redeclare block-scoped variable 'name'`. أصلح الملف ثم فكّر: هل ينبغي أصلاً أن تُفحص سكربتات `scripts/` ضمن بناء الإنتاج؟ ثم راجع المنطق ونظّف كالعادة.»

**التشخيص:** ملف `r41-prod-verify.ts` (سكربت تحققٍ لمرة واحدة أُضيف في ختام الجولة ٤١) لا يحتوي أي `import`/`export` في مستواه الأعلى، فيعامله TypeScript كملف نصّي عام (global script) — وأي `const` فيه يعلن في النطاق العام. تعريف `const name` اصطدم بالمتغير العام `window.name` المعرَّف في مكتبة `lib.dom` → خطأ `TS2451` فشلَ به بناء Vercel. ملاحظة: سكربتات الجولة ٤١ السابقة (`r41-verify.ts`، `r41-repro.ts`) نجاها الصدُف لا الحذر — العملية كشفت أن **أي** سكربت مستقبلي كسول يمكنه حجب نشر الإنتاج كله.

## ما نُفِّذ (إصلاحان متكاملان)

### ١. الإصلاح الفوري داخل الملف
- إعادة تسمية `name` → `studentName` في مواضعها الثلاثة (التعريف سطر ١٣ + الاستخدام في `/api/auth/signup` + في `/api/onboarding/complete`).

### ٢. الإصلاح البنيوي في tsconfig.json (المقترح الأنسب)
- أُضيف `"scripts"` إلى مصفوفة `exclude` في `tsconfig.json` — بذلك لا يدخل أي سكربت تطوير في فحص `next build` إطلاقاً، فلا يمكن لهذه الفئة من الأخطاء أن تحجب الإنتاج مرة أخرى.
- **التبرير:** كل ملفات `scripts/` الـ١٩ أدوات تشغيل/فحص لمرة واحدة (بذر، تنظيف حسابات تجريبية، تحريات، تحقق إنتاج) — لا واحد منها جزء من تشغيل التطبيق. تبقى قابلة للتنفيذ بـ`bun scripts/x.ts` (bun يترجم مستقلاً عن tsconfig).
- **فحص السلامة:** تأكد بالبحث أن لا ملف في `src/` يستورد أي شيء من `scripts/` — الاستثناء لا يكسر شيئاً.

## التحقق
- `bunx tsc --noEmit`: **صفر أخطاء** (كان الخطأ الوحيد في r41-prod-verify.ts:13).
- `next build`: **أخضر** — جدول المسارات كاملاً، الصفحات العامة ثابتة (SSG) كما هي، لا تغيير بأي مسار.
- بعد الدفع: نشر Vercel نجح (تتبع عبر GitHub commit status API)، وفحص صحة الموقع الحيّ نظيف.

## القرارات
- اعتماد الإصلاحين معاً لا واحد منهما: إعادة التسمية تجعل الملف سليماً مهما أُعيد تضمينه مستقبلاً، والاستثناء البنيوي يحمي من الخطأ نفسه في كل السكربتات القادمة.
- سكربتات التطوير بلا فحص أنواع ضمن البناء مقبول تماماً: هي أدوات تشغيل يدوي، وقد تُفحص وقت الحاجة بأمر tsc منفصل.

## ملاحظة تشغيلية
- نتيجة سكربت التحقق الإنتاجي `r41-prod-verify.ts` مسجَّلة منذ الجولة ٤١ (طالب التخصص ٦/السنة ٨ يرى مادة المقياس «محاضرات نحو» #١٠ حياً على gu-mo.vercel.app ثم حُذف حسابه) — لم يُعاد تشغيله هذه الجولة لأن الإصلاح الذي يتحقق منه نشرٌ ومدقَّقٌ أصلاً.
