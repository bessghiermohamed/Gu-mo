# تقرير الجولة 93 — فصل تيليجرام عن الدروس + رفع الطلبة بمراجعة المشرف + سمّ الاختبارات

**التاريخ:** 19 سبتمبر 2026
**الحالة:** منجزة — فحص r93 57/57، انحدارات r92 51/51 وr91 35/35 وr90 53/53 وr88 34/34 وr87 97/97 وr86 92/92 وr85 38/38 وr81 31/31، tsc 0، `next build` ✓، ملفات الجولة الثمانية بلا أي ملاحظة eslint.

---

## 1) الخلاصة التنفيذية

ثلاثة أهداف من طلب المالك حرفياً:

1. **«في قسم الدروس لا تظهر دروس تيليجرام»** — أُزيلت بوابة «دروس تيليجرام» من قائمة المقاييس (بطاقة r7)، وأُزيل تبويب «الدروس» في تفاصيل المقياس بمنشورات تيليجرام كلها (TgItem/LessonCard/تتبّع الجديد/أزرار «تصفّح دروس تيليجرام الكاملة») ليحل محله تبويب **«الملفات»** بملفات المقياس المرفوعة فقط. شاشة تيليجرام نفسها لم تُمسّ — هي قسم مستقل بذاته.
2. **«المشرف يرفع الملفات وهذا موجود» + «تمكين الطلبة من رفع الملفات لكن تتم مراجعتها من طرف المشرف أولاً»** — رفع المشرف باقٍ كما هو (مستوى المقياس + المكتبة)، وجديد: **كل طالب يستطيع الرفع الآن** بنفس النافذة (من جهازه إلى Drive الخاصه أو رابط خارجي)، لكن ملفه يُخزَّن بحالة **«بانتظار المراجعة»** ولا يراه الطلبة الآخرون حتى **يوافق المشرف** (أو يرفضه). للمشرفين شريط مراجعة داخل «ملفاتي» بموافقة/رفض بضغطة، ومع الموافقة يُطلق الإشعار العام للمحتوى — لا عند الإرسال.
3. **«سمّ الاختبارات… الاختبار القصير مع العمل الموجه»** — كل اختبار يحمل الآن **نوعاً مُسمّى**: «اختبار» أو «اختبار قصير» أو «عمل موجه»، يُختار من نافذتي الإضافة والتعديل ويظهر شارة على البطاقة (وفي تبويب اختبارات المقياس)، والإشعار يسمّي النوع — «عمل موجه» لم يعد يُرسل كـ«اختبار جديد».

## 2) التفاصيل التقنية

### أ) إزالة تيليجرام من قسم الدروس

| الملف | التغيير |
|---|---|
| `courses-screen.tsx` | حذف بطاقة «دروس تيليجرام» (r7) + استيراد `Send` + أي `navigate("TELEGRAM")` — الشاشة الآن قائمة مقاييس صرفة |
| `course-detail-screen.tsx` | حذف كامل لواجهة دروس تيليجرام: `TgItem`، جلب `/api/telegram/items?moduleId`، تتبّع «جديد منذ آخر زيارة» (`talib-course-visit-*`)، `LessonCard` و`kindIcon`، الشارات المميزة — التبويب أصبح «الملفات» ويعرض ملفات «محاضرة» المرفوعة فقط، والمؤشر العلوي أصبح «الملفات» بعدد الملفات |

### ب) رفع الطلبة بمراجعة المشرف

- **المخطط** (Prisma + SQLite دُفعت محلياً بـ `db push`؛ وSupabase عبر SQL لمرة واحدة بنمط r41):
  - `library_references.review_status TEXT DEFAULT 'approved'` — الصفوف القديمة ورفع المشرفين «مقبولة» تلقائياً، بلا أي ترحيل بيانات.
  - `library_references.uploader_id INTEGER` — هوية الرافع (يُبقى ملف الطالب مرئياً له وهو معلّق/مرفوض).
- **POST `/api/library`**: الحرس الأول صار «أي مستعمل مسجّل»؛ `isSupervisor` يحدّد المسار:
  - مشرف → `review_status='approved'` فوراً + إشعار المحتوى المعتاد (r24)، مع attempts تتدرّج على الأعمدة (قواعد ما قبل r93 تظل تعمل).
  - طالب → `review_status='pending'` حصرياً؛ الأعمدة الاختيارية (Drive/المقياس) تتدرّج، لكن **أعمدة المراجعة صارمة**: قاعدة بلا الأعمدة تعيد `needsSchema` مع مقطع SQL بدلاً من نشر الملف بصمت — العقد المراجعي لا يُكسر. ولا إشعار محتوى عند الإرسال؛ بدلها `notifyFileReviewRequested` للمشرفين في نطاق التخصص (generic، غير قابلة للكتم — مهمة لا محتوى).
- **GET**: يُرجع `reviewStatus/uploaderId`؛ الطلبة يرون المقبول + مرفوعاتهم فقط (تصفية موحّدة في المسارين بعد التعيين — تخدم المكتبة وتبويبات المقياس معاً).
- **PATCH**: `reviewAction: "approve" | "reject"` (للمشرفين فقط وبنطاق التخصص). الموافقة تُحدّث الحالة + `notifyFileReviewed` للرافع + إشعار المحتوى للنطاق؛ الرفض يُخفي الملف ويخبر صاحبه ليحذفه أو يرفع نسخة محسّنة.
- **DELETE**: المشرفون كما هم؛ والطالب يحذف **ملفه هو** ما دام غير مقبول.
- **الواجهات**: نافذة الرفع الواحدة (`publish-dialog.tsx`) تدرك الدور — بتنبيه كهرماني «ملفك سيُراجع من طرف المشرف أولاً» في الوضعين ورسائل نجاح تصفّ المراجعة؛ زر «إضافة ملف» ظاهر للجميع في المكتبة؛ صف الرفع في تفاصيل المقياس صار للجميع بلسانين (مشرف/طالب)؛ شارات «بانتظار مراجعة المشرف» و«لم تتم المواقفة» في «ملفاتي» وتبويبات المقياس؛ شريط المراجعة الكهرماني في «ملفاتي» للمشرفين.

### ج) سمّ الاختبارات

- `exams.kind TEXT DEFAULT 'اختبار'` (Prisma + SQL لمرة واحدة `EXAM_KIND_SQL`)؛ الأنواع: «اختبار» / «اختبار قصير» / «عمل موجه».
- GET يقرأ النوع بقيمة افتراضية آمنة؛ POST يحاول مع النوع ثم بدون (قواعد ما قبل SQL تظل تعمل بلا انقطاع)؛ PATCH يقبل تعديل النوع ويتدرّج كذلك.
- النافذتان تضيفان حقل «النوع» وعنوان الحقل يتبعه («عنوان عمل موجه»…) مع تلميح مخصص؛ الشارة البنفسجية تظهر على البطاقات لأنواع غير «اختبار» (الصفوف القديمة تبقى صامتة بلا ضجيج).

## 3) فحص r93 (57/57) — `bun scripts/r93-check.ts`

- **(أ) 12 فحصاً**: صفر أثر لتيليجرام في المقاييس وتفاصيل المقياس (12 رمزاً ممنوعاً: TgItem/LessonCard/api/telegram/items/أزرار التصفح…)، تبويب «الملفات» محل «الدروس»، شاشة تيليجرام مستقلة باقية، رفع المشرف باقٍ للجميع بلسان المراجعة.
- **(ب) 18 فحصاً**: منطق المراجعة الكامل في المسار والواجهات والإشعارات (pending/approved، الصرامة للطلبة، التدرّج للمشرفين، تصفية GET، موافقة/رفض، حذف الذات، SQL لمرة واحدة).
- **(ج) 17 فحصاً**: الأنواع المسمّاة في المسار والنافذتين والشارات والإشعار.
- **(د) 3 فحوص**: أعمدة المخطط الثلاثة.
- **(هـ) 7 فحوص**: تأمين r90 (ads.txt/البوابة) وr91 (سطح API) وr92 (أدواتي مضغوطة + شارة الفوج).

**بوابة الجودة الكاملة:** `tsc --noEmit` = 0، eslint نظيف للملفات الثمانية، `next build` ✓ (سطح API: `/api/ai/notebook` وحيدة كما هي — هذه الجولة لم تضف مسارات جديدة، وسّعت مساري library وexams فقط).

## 4) الصدق والحدود

- **خطوة مالك لمرة واحدة على Supabase** (كما هو عُرف r41): لتشغيل المراجعة والأنواع على الإنتاج يُنفَّذ المقطع التالي مرة واحدة في محرر SQL:
  ```sql
  ALTER TABLE library_references ADD COLUMN IF NOT EXISTS review_status TEXT DEFAULT 'approved';
  ALTER TABLE library_references ADD COLUMN IF NOT EXISTS uploader_id INTEGER;
  ALTER TABLE exams ADD COLUMN IF NOT EXISTS kind TEXT DEFAULT 'اختبار';
  ```
  قبل التنفيذ: المشرفون يرفعون ويعملون كالسابق تماماً (تدرّج تلقائي)، لكن رفع الطلبة يعرض بطاقة التحديث وواجهة النوع تتدرّج بصمت (كل صف يُقرأ «اختبار»). محلياً وبعد النشر المباشر للأعمدة لا حاجة لأي شيء.
- **دافع مقصود للعدل:** رفع الطالب من Drive الخاص به (١٥ ج.ب مجاناً) أو رابط خارجي — نفس عقد «السيرفر لا يخزّن بايتات».
- **الإشعارات:** الإرسال يُخطر المشرفين حصراً (بلا إشعار محتوى للطلبة — الملف ليس عاماً بعد)؛ الموافقة تُخطر الرافع + النطاق. الرفض يُخطر الرافع فقط.
- **«قسم الدروس» في شريط التنقّل السفلي** هو شاشة المقاييس (courses) — وهذا ما نظّفناه؛ إن قصد المالك لاحقاً إزالة بوابة تيليجرام من موضع آخر فيسجل ذلك في جولة قادمة.
- قاعدة «الدروس» في تحديث «talib-activity-v1» (r92) لم تُمس — النقاط تُحتسب من مصادر الدفتر والواجبات والملاحظات والأيام كما هي.

## 5) خطوات المالك (خارج الكود)

1. **مقطع SQL الأعلاه مرة واحدة في Supabase** — يفعّل مراجعة الطلبة والأنواع المسمّاة على الإنتاج.
2. معلّقات r90 كما هي (الهوية/PIN، قائمة المواقع `gu-mo.vercel.app` حرفياً، إعادة المراجعة).
