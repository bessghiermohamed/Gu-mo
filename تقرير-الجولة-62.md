# تقرير الجولة 62 — تغيير البوت إلى @gu_mo_bot + عقل يفكّر ويرتّب

## طلب المالك

«أريد تغيير بوت تيليجرام الحالي إلى هذا البوت؛ أريده بوت ترتيب، وأريد أن يملك ذكاءً وقدرة على التفكير. إن أمكن؛ وإن لم يمكن فلا مشكلة.»

قد أرسل المالك توكن البوت الجديد، وتحققنا منه مباشرة لدى تيليجرام: البوت هو **@gu_mo_bot** (اسمه «Talib_app») — توكن صحيح ومقبول، ولا يوجد ربط (ويبهوك) عليه بعد.

## الإجابة المباشرة: نعم، أمكن — وتمّ

البوت الجديد صار **بوت ترتيب ذكي يفكّر** فعلاً، مع قدرة جديدة كلياً لم تكن موجودة: **محادثة خاصة ذكية**.

## ماذا أصبح يفعله البوت؟

### ١. الترتيب الذكي للقنوات (كما كان، وأفضل)
منشورات القنوات الجامعية المرتبطة تُرتَّب تلقائياً: تصنيف كل منشور (محاضرة، أعمال موجهة TD، تمارين، امتحان، ملخص، كتاب…)، عنوان عربي نظيف، وقراءة نص الصور بالذكاء الاصطناعي — كل ذلك يدخل تطبيق طالب مرتباً وجاهزاً للبحث.

### ٢. التفكير في المحادثة الخاصة (جديد كلياً)
أي طالب يراسل البوت خاصة يحصل على:
- **أجوبة ذكية حقيقية**: نفس سلسلة الذكاء الاصطناعي التي تشغّل «المساعد الذكي» داخل التطبيق، مكيّفة لتيليجرام (رياضيات نصية واضحة بلا رموز LaTeX التي لا يعرضها تيليجرام)، مع ذاكرة للمحادثة (يذكر آخر ٨ أدوار)، ومؤشر «يكتب الآن…» قبل الرد.
- **ترتيب الملفات**: أرسل له ملفاً أو صورة (درس، تمرين، امتحان…) فيرد فوراً: «التصنيف الذكي: امتحان — العنوان المقترح: …» بنفس مصنّف القنوات، وبقراءة بصرية للصور. **لا يُحفظ شيء أبداً** — تصنيف للعرض فقط.
- **أوامر**: `/start` بترحيب يشرح قدراته، و`/help` بدليل الاستخدام.
- حماية من الاستنزاف: مهلة ٣ ثوانٍ بين الرسائل و٤٠ رسالة يومياً لكل مستخدم.

### ٣. تغيير البوت من داخل التطبيق (جديد كلياً)
بطاقة جديدة في لوحة الإدارة ← تبويب تيليجرام: **«تغيير البوت — بدون Vercel»**:
- الصق توكن البوت الجديد ← يتحقق التطبيق منه لدى تيليجرام فوراً ← يُحفظ بأمان ← يُفعَّل الربط تلقائياً خلال ثوانٍ.
- يُظهر البوت الفعّال الحالي ومن أين جاء (محفوظ داخل التطبيق / من متغير Vercel)، مع زر «العودة إلى بوت Vercel» بضغطة واحدة.
- التوكن يُخزَّن في جدول خاص **لا يمكن قراءته من المتصفح نهائياً** (مفتاح الخدمة فقط) — ولذلك لن يُعرض التوكن مرة أخرى بعد حفظه.

## لماذا هذا التصميم؟

المستودع على GitHub **عام**، لذا لا يجوز وضع التوكن في الكود أبداً. وضبط متغيرات Vercel يتطلب الداشبورد وRedeploy. الحل: التوكن يُحفظ في قاعدة البيانات من الواجهة مباشرة — أفضل جهد أمان وأسهل استخدام، وتبديل البوتات مستقبلاً يصبح لصقاً واحداً.

## ما الذي أحتاجه منك لإتمام التبديل على الإنتاج؟ (مرة واحدة، ~دقيقتان)

1. **أنشئ الجدول الصغير**: في Supabase ← SQL Editor ← الصق محتوى ملف `download/supabase_bot_config.sql` من المستودع ← Run.
2. **الصق التوكن**: لوحة الإدارة ← تيليجرام ← بطاقة «تغيير البوت» ← الصق توكن @gu_mo_bot ← «حفظ وتفعيل البوت الجديد». سترى: «تم التبديل إلى @gu_mo_bot وتفعيل الربط ✅».
   (إن أخبرتك البطاقة أن `SUPABASE_SERVICE_ROLE_KEY` غير مضبوط: أضفه أولاً في Vercel ← Environment Variables ← Redeploy — مرة واحدة.)
3. **أضف البوت مشرفاً**: في كل قناة تريد استيراد منشوراتها، أضف @gu_mo_bot مشرفاً (تكفي صلاحية قراءة المنشورات) — هذا شرط تيليجرام نفسه ولا يمكن لأحد فعله بدلاً منك.

بعد الخطوة ٢ مباشرة يبدأ البوت بالإجابة في الخاص — الخطوة ٣ خاصة بترتيب القنوات.

**طريق بديل كلاسيكي** (إن فضّلت Vercel): ضع التوكن في `TELEGRAM_BOT_TOKEN` ← Redeploy ← زر «تفعيل الربط» في بطاقة الحالة. لكن بطاقة «تغيير البوت» أسرع ولا تحتاج شيئاً من هذا.

## الاختبار

- **وحدات (33/33)**: ترحيب /start، مسار التفكير مع الذاكرة، ترتيب الملفات والصور (بصرية)، الصوتيات، فشل المزوّد، حدود الاستخدام، تقسيم الأجوبة الطويلة (9000 حرف ← ٣ رسائل مضبوطة)، فشل الإرسال بلا استثناءات.
- **متكامل محلي على بناء الإنتاج (29/29)**: بمشاركة التوكن الحقيقي عبر متغير بيئة (لم يُكتب في أي ملف) — تحقق حي: التعرف على @gu_mo_bot، حفظه، توليد سرّ الويبهوك وقبوله، بقاء سرّ البيئة القديم مقبولاً، عدم تسريب التوكن في أي استجابة، عودة نظيفة لبوت Vercel، وسلامة خط استيراد القنوات القديم.
- **الإنتاج المباشر**: النشر اكتمل (بناء 70/70 صفحة)، المسار الجديد يعمل (403 بدون جلسة = مُشغَّل ومحمي)، وواجهة «تغيير البوت» حيّة في الملفات المُقدَّمة فعلاً.
- `tsc` صفر أخطاء، `eslint` نظيف، وتوكن البوت الحقيقي غير موجود في أي ملف متعقب (تدقيق تسريب نظيف).

## ملاحظات

- البوت الحالي القديم (إن كان ويبهوكه مفعّلاً) **لن ينكسر** عند التبديل — السرّان يعملان معاً حتى تُنهي البوت القديم بنفسك.
- رسائل الخاصة **لا تدخل تطبيق طالب أبداً** ولا تُسجَّل في أي قاعدة بيانات — خصوصية كاملة للطلبة.
- لدى البوت ٣ رسائل معلّقة في تيليجرام (غالباً /start تجريبية أرسلتها) — ستُتجاهل عند التفعيل (نتعامل مع الجديد فقط، باتفاقنا).
