# تقرير الجولة 73 — إصلاح كتابة روابط المواضيع (RLS) على الإنتاج

## المشكلة التي أبلغ عنها المالك
«نفّذت supabase_telegram_topics.sql يدوياً في Supabase ورجعت للتطبيق — ويقول إن جدول المواضيع لم يُنشأ». وتكرر الرسالة رغم تنفيذ الملف أكثر من مرة.

## التشخيص الحي (بمفاتيح المالك، 2026-09-11)
- جدول `telegram_topics` **منشأ فعلاً** على الإنتاج (PostgREST يرد 200 بكل أعمدته العشرة) — ملف r65 كان منفّذاً منذ حادثة r72.
- لكن **كل مسارات الكتابة** عليه كانت مرفوضة:
  - `POST /api/telegram/topics` (حوار «#» لإضافة ربط) → `500` برسالة «جدول المواضيع غير منشأ — نفّذ supabase_telegram_topics.sql» (مُعاد إنتاجها حياً بجلسة مالك مؤقتة).
  - `PATCH` و`DELETE` في المسار نفسه — العميل anon بدون صلاحيات كتابة.
  - `upsertTopicBinding` في مسار المصادر (لصق رابط قسم t.me/قناة/موضوع) — والإدراج المرفوض كان **يُوقف** حتى المسار البديل r66 (قسم كمصدر مستقل) لأن خطأ RLS لا يُصنَّف «missing-table».
- **الجذر:** ملف `supabase_telegram_topics.sql` (r65) منح anon صلاحية `SELECT` فقط، بينما مسارات الخادم تكتب بمفتاح anon العام → رفض `42501 new row violates row-level security policy for table "telegram_topics"`.
- **لماذا الرسالة المضللة؟** نص خطأ RLS يحوي اسم الجدول، وشرط `error.message.includes("telegram_topics")` في المسار فعنه عرض «الجدول غير منشأ» — فيظن المالك أن تنفيذه لم يُسلَّم ويعيد المحاولة بلا فائدة. (خطأ تصنيف أخ شاشة r72: `42501` لم يدخل تصنيفاتها الثلاثية).

## الإصلاح (طبقتان مستقلتان)
1. **قاعدة البيانات — الملف الحاسم:** `download/supabase_topics_write_policies.sql` (جديد، idempotent) — يمنح anon صلاحيات INSERT/UPDATE/DELETE على `telegram_topics` بنفس نمط `telegram_sources`/`telegram_items` (الترخيص الفعلي داخل مسارات الخادم: جلسة + `canManageTopics`). يُنفَّذ مرة واحدة في محرر SQL ويصلح التطبيق المنشور **فوراً بلا إعادة نشر**.
2. **الكود — تحصين وصدق (هذه الجولة):**
   - `table-state.ts`: حالة رابعة منطقية جديدة `permissionDenied` + `isPermissionDeniedError` (42501/row-level security/permission denied) — رسالتها تذكر **ملف السياسات** ولا تتهم ملف إنشاء الجدول.
   - `topics/route.ts`: عميل كتابة `getTopicsWriteClient()` يفضّل service role عند توفر `SUPABASE_SERVICE_ROLE_KEY` (يتجاوز RLS) ويعود إلى anon بلا رمي؛ أخطاء POST/PATCH/DELETE تمر عبر `tableStateFromError` بدل فحص `includes("telegram_topics")` المضلل.
   - `sources/route.ts` `upsertTopicBinding`: تفضيل service role للكتابة، ورسائل الأخطاء الصادقة عبر `tableStateFromError`.
   - `scripts/r73-check.ts`: 18/18 (التصنيف الثلاثي الجديد + تراجع r72) — `bun run scripts/r73-check.ts`.

## الفحوص
- `bunx tsc --noEmit` → 0 أخطاء. `bunx eslint` على الملفات الأربعة المتغيّرة → نظيف.
- تصنيف الأخطاء: `r73-check.ts` 18/18.
- حي على الإنتاج قبل الإصلاح: GET topics = 200 صحي، POST = 500 برسالة مضللة (مُثبَّت أعلاه) — وبعد تنفيذ ملف السياسات يعمل POST/PATCH/DELETE فوراً (الكود المنشور يكتب بـ anon والسياسات الجديدة تسمح).

## خطوات المالك
1. **مطلوب (مرة واحدة):** نفّذ `download/supabase_topics_write_policies.sql` في محرر SQL داخل Supabase — ثم أعد فتح حوار «#» وأضف روابط المواضيع مباشرة.
2. **اختياري (تحصين إضافي):** أضف `SUPABASE_SERVICE_ROLE_KEY` في متغيرات Vercel (Settings ← Environment Variables ثم Redeploy) — عندها تكتب مسارات المواضيع بعميل service role بغضّ النظر عن سياسات RLS.
3. **اختياري (جداول ناقصة معروفة):** `supabase_bot_config.sql` و`supabase_push_subscriptions.sql` و`supabase_course_materials.sql` ما زالت غير منفّذة على الإنتاج — الميزات المرتبطة تعمل بوضع تدهور آمن حتى تنفيذها.
