# تقرير إصلاحات الجولة الرابعة — بسيطة اللغة

هذا التقرير مكتوب بلغة بسيطة لغير المبرمجين، حسب قواعد العمل المعتمدة. يمكن عرضه على مراجع تقني للتحقق من التفاصيل.

## ما الذي حدث في هذه الجولة؟

أرسلتَ تقرير فحص خارجي (UX/UI Audit) لتطبيق الويب، وطلبت رأيي فيه ثم قلت: «نفّذ ما تراه مناسباً واختم بتقريرك». هذه الجولة تغطي: **رأيي في تقرير الفحص + ما نفّذته بناءً عليه**.

استثنيت ملاحظات C-2 و C-3 و C-4 من التنفيذ كما طلبت (منها ما هو مسألة تنظيف بيانات في قاعدة البيانات، لا كود).

---

## أولاً: رأيي في تقرير الفحص الخارجي

**التقييم العام: تقرير جدي ومحترف (أغلب ملاحظاته صحيحة)، لكن فيه 3 أخطاء تشخيص وبعض الخلط بين «ميزة غير مبنية أصلاً» و«ميزة معطلة».**

### ما صحّ فيه الفحص (تأكدت منه في الكود بنفسي):
1. **بطاقات المقررات غير قابلة للنقر** (C-1) — صحيح 100%، ولم تكن هناك شاشة تفاصيل أصلاً.
2. **زر اللغة ينتج واجهة مختلطة** (H-1) — صحيح، وقسته: حوالي **450 سطراً عربياً مكتوباً مباشرة في الكود** مقابل 125 نصاً مترجماً فقط. لذلك عند التبديل للإنجليزية يبقى أكثر الواجهة عربياً.
3. **صفحة الخطأ 404 إنجليزية** (M-4) — صحيح، لم تكن موجودة أصلاً.
4. **كلمة Close الإنجليزية** (H-4) — صحيح، لكنها ليست زراً مرئياً بل نص خفي يقرأه قارئ الشاشة فقط.
5. **إظهار رقم النسخة** (M-6) و**النسبة المئوية الخام في الإعداد** (L-1) — كلها صحيحة.

### ما أخطأ فيه الفحص:
1. **«فشل الدخول بلا رسالة» (H-6)** — الرسالة موجودة وتظهر فعلاً (اختبرت الخادم الحقيقي: يرفض ويعيد سبب الرفض، والكود يعرضه). الباحث الآلي غالباً التقط الشاشة قبل ظهور الرسالة. البقية الحقيقية الوحيدة: لو انقطع الإنترنت نفسه أثناء الدخول، كان الكود القديم يسكت صمتاً تاماً — أصلحتُها.
2. **«زر الرجوع يعود دائماً للرئيسية»** — غير صحيح، يوجد سجل رجوع حقيقي يعيدك للصفحة السابقة.
3. **«رمز المقياس يظهر للأول فقط» (M-5)** — الكود يعرضه للجميع؛ المشكلة أن خانة الرمز **فارغة في قاعدة البيانات** لبقية المقاييس. أي: مسألة بيانات، لا برمجة.

### ما لم يذكره الفحص مع أنه مهم:
- **الدخول بلا كلمة مرور يعني أن حماية أي حساب = معرفة الاسم والبريد فقط.** زميلك في الفوج يعرفهما. هذا خيار مقبول لتطبيق طلابي، لكن كان يجب على الفاحص تنبيهك بدل أن يمدحه.
- **لوحة الإشراف** (أهم شاشة لديك) لم تُفحص إطلاقاً لأن الفاحص لم يستطع الدخول بحساب مشرف (جرّب اسماً غير مسجّل).

---

## ثانياً: ما تم تنفيذه فعلياً (ملف ملف)

### 1. إخفاء زر اللغة حتى اكتمال الترجمة (إصلاح H-1)
- `src/app/page.tsx` — أُزيل زر تبديل اللغة من الشريط العلوي (مع تعليق في الكود يشرح كيفية إعادته).
- `src/components/talib/i18n-provider.tsx` — التطبيق يفتح دائماً بالعربية، ويُلغى أي اختيار إنجليزي قديم محفوظ، حتى لا يعلق أي مستخدم في واجهة نصف إنجليزية بلا زر عودة.
- **النتيجة:** لا مزيد من الواجهة المختلطة. الترجمة الكاملة تبقى مطلوبة قبل إعادة الزر (انظر «غير المنفَّذ»).

### 2. رسالة خطأ عند انقطاع الاتصال أثناء الدخول (تتمة إصلاح H-6)
- `src/components/talib/auth-provider.tsx` — أي فشل شبكي أثناء الدخول أو إنشاء الحساب يعرض الآن رسالة واضحة: «خطأ في الاتصال بالخادم...» بدل الصمت.
- `src/messages/ar.json` + `src/messages/en.json` — أُضيف نص الرسالة الجديد.

### 3. بطاقات المقررات صارت قابلة للنقر وتتوسّع (إصلاح C-1 بالحل الخفيف)
- `src/components/talib/screens/courses-screen.tsx` — البطاقة الآن زر حقيقي:
  - النقر عليها يفتح **وصف المقياس** (يُقرأ من قاعدة البيانات) وشارة السداسي.
  - تعمل باللمس وبالماوس وبلوحة المفاتيح (لمستخدمي قارئ الشاشة).
  - عند غياب الوصف: رسالة مهذبة بدل فراغ.
  - زر «تبليغ عن مشكلة» بقي مستقلاً (نقره لا يفتح البطاقة).
  - إصلاح M-5 ضمنياً: رمز المقياس الفارغ لم يعد يظهر كشارة فارغة.
- **ملاحظة صريحة:** هذا الحل يعرض الوصف فقط. شاشة «محاضرات وملفات المقياس» الكاملة (الملاحظة H-5) ميزة جديدة تحتاج قراراً منك — لم تُبنَ (انظر «غير المنفَّذ»).

### 4. صفحة 404 عربية جديدة (إصلاح M-4)
- `src/app/not-found.tsx` — ملف جديد: صفحة عربية كاملة الاتجاه فيها زران: «العودة إلى الرئيسية» و«فتح التطبيق من جديد».

### 5. إغلاق النوافذ بالعربية (إصلاح H-4)
- `src/components/ui/dialog.tsx` — النص الخفي لزر الإغلاق في كل نوافذ التطبيق صار «إغلاق» بدل «Close». تعديل واحد أصلح كل النوافذ لأنها تشترك في نفس المكوّن.

### 6. بنر جديد للواجهة الرئيسية
- `public/talib/hero-banner.jpg` — **تنبيه مهم وشفاف:** أرفقتَ صورة بنر باسم talib_hero_banner لكنها **لم تصل إلى الخادم إطلاقاً** (مشكلة في نظام رفع الملفات في المحادثة — حدثت مرتين سابقاً مع ملف PDF). ولأنك خوّلتني القرار، أنشأتُ بنراً مؤقتاً بنفس ألوان التطبيق (الأخضر الداكن + لمسات نحاسية، صورة تعليمية بلا أي كتابة داخلها — تحققت من ذلك بفحص بصري آلي).
- `src/components/talib/screens/home-screen.tsx` — استُبدل البنر القديم (SVG) بالجديد.
- **لتستبدله بصورتك لاحقاً:** أعد إرسالها في المحادثة، أو ضعها بنفسك في مكان `public/talib/hero-banner.jpg` داخل المستودع — **بلا أي تعديل كود**. البنر القديم (SVG) محفوظ ولم يُحذف.

### 7. تحسينات شكلية صغيرة (M-2، M-3، M-6، L-1)
- `src/app/page.tsx` — أزرار الشريط العلوي كبرت (40px بدل 36px) لتناسب اللمس (M-2).
- `src/components/talib/screens/courses-screen.tsx` — التبويب النشط صار بخط عريض (M-3).
- `src/components/talib/screens/profile-screen.tsx` — أُزيل «نسخة 2.0 ويب»، واستُبدل بالعبارة نفسها المستخدمة في شاشة الدخول (M-6).
- `src/components/talib/screens/onboarding-screen.tsx` — أُزيلت النسبة المئوية الخام (83%، 100%)، وبقي العداد الأوضح «الخطوة X من 6» + شريط التقدم (L-1).

### 8. حذف كود ميت (مذكور صراحة حسب القواعد)
- حُذفت 3 ملفات لم يعد أحد يستدعيها (تحققت بالبحث في كل الكود قبل الحذف): `src/components/ui/toaster.tsx`، `src/components/ui/toast.tsx`، `src/hooks/use-toast.ts`. كانت بقايا نظام إشعارات قديم غير مستخدم — التطبيق يستخدم نظاماً آخر مثبتاً في `src/app/page.tsx`.
- `src/app/layout.tsx` — أُزيل استدعاؤها من هيكل الصفحة.

### 9. إصلاح بذور قاعدة بيانات الاختبار (ترميم خفي — ليس من ملاحظات الفحص)
- `scripts/seed.ts` — اكتشفت أن ملف تهيئة بيانات الاختبار **المحلي** ناقص (مؤسسة واحدة وتخصص واحد، بلا ملامح ولا مجموعات)، بينما حزمة الاختبارات الآلية تفترض وجود مؤسسة ثانية (وهران) وتخصص فرنسي وملامح ومجموعات افتراضية — أي أن نتيجة «48/48» المعلنة سابقاً كانت تعتمد على بيانات بيئة قديمة غير محفوظة في المستودع. **أكملت البذور لتطابق ما تفرضه الاختبارات** (بيانات هيكلية فقط: مؤسسات/تخصصات/سنوات/ملامح/مجموعات — بلا أي مستخدمين وهميين، التزاماً بالقواعد). النتيجة الآن: **48/48 ناجحة** على بيئة نظيفة قابلة لإعادة الإنتاج في أي وقت.

---

## ثالثاً: التحقق الذاتي (قبل الادّعاء بأن شيئاً «تم»)

- كل بند أعلاه فُحص بعد تنفيذه: بنيتُ التطبيق كاملاً (بناء الإنتاج نجح بلا أخطاء)، وفحصت الكود بأدوات الفحص (صفر ملاحظات)، وشغّلت حزمة الاختبارات الآلية كاملة: **48/48**.
- فتحتُ التطبيق في متصفح آلي حقيقي ومشيت الرحلة كاملة: إنشاء حساب ← الإعداد الأولي بمراحله الست ← الرئيسية ← المقررات ← نقر البطاقة فتوسّعت ظاهرة الوصف والسداسي ← صفحة 404 تظهر عربية بزرين عاملين. لا أخطاء في وحدة تحكم المتصفح. لقطات الشاشة محفوظة عند: `download/home-round4.png`، `download/courses-expanded-round4.png`، `download/notfound-round4.png`.
- تحققت أن زر اللغة لم يعد موجوداً في الشريط العلوي، وأن البنر الجديد يظهر فعلاً (فحصت ألوانه في لقطة الشاشة).

## رابعاً: ما لم يُنفَّذ بعد (ويحتاج قرارك — ليس نسياناً)

1. **شاشة تفاصيل المقياس الكاملة (محاضرات + ملفات)** — H-5: ميزة جديدة كاملة (قاعدة بيانات + واجهة + صلاحيات). الحل الخفيف المنفَّذ يعرض الوصف فقط. تحتاج أن تخبرني: هل يرفع المشرف المحاضرات؟ بأي صيغ؟
2. **إكمال الترجمة الإنجليزية (450 نصاً)** أو إلغاء فكرة الإنجليزية نهائياً — قرار منتج.
3. **إعادة تنظيم التنقل (H-2):** هل تبقي شبكة الخدمات في الرئيسية أم شريط التبويب السفلي؟ الفاحص يقترح عدم الاثنين معاً.
4. **جرس الإشعارات (H-3):** حالياً ينقل للإعلانات. هل تريد لوحة إشعارات منبثقة؟
5. **البحث العام في التطبيق (M-8)** و**تحسين تباين النصوص الثانوية (M-1)** — مؤجلان.
6. **تنظيف بيانات الاختبار في قاعدة البيانات الحقيقية** (الملاحظات C-3/C-4 المستثناة من نطاقك): مقياس باسم شخص ومحاضرات باسم مدينة — هذه إدخالات بيانات تحتاج تعديلها من لوحة الإشراف أو محرر SQL، لا إصلاح كود.
7. **فحص لوحة الإشراف** — لم يشملها الفحص الخارجي. لو أردت جولة فحص لها، أنشئ لي حساباً تجريبياً مشرفاً (أو صحّح بيانات الدخول).

## هل هناك خطوة مطلوبة منك؟

**لا شيء إلزامي.** لا يوجد سكربت SQL هذه الجولة — كل التغييرات في الكود فقط. بعد الدفع، ينشر Vercel التحديث تلقائياً خلال دقيقة أو دقيقتين.

**اختياري:** أعد إرسال صورة البنر في المحادثة (لم تصل) وسأركّبها مكان المؤقت بلا أي تعديل آخر.
